# Specs

Durable specs for work that outlives a single session and has acceptance criteria, one
directory per spec, named `NNN-slug/` containing `NNN-spec.md`, `NNN-plan.md`, `NNN-tasks.md`.

- (nothing yet)
