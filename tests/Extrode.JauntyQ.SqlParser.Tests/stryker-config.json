{
  "stryker-config": {
    "project": "Extrode.JauntyQ.SqlParser.csproj",
    "reporters": ["progress", "json"],
    "verbosity": "info",
    "thresholds": { "high": 80, "low": 65, "break": 0 }
  }
}
