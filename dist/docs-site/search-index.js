window.__docsIndex=[{"u":"constitution.html","t":"Constitution — jauntyq","s":"General","h":[{"i":"stack-constraints","t":"Stack constraints"}],"b":"Constitution — jauntyq Binding project rules. These are the WON'T-change decisions. Contributors obey them; changing one is a deliberate governance act, not a casual edit. This is NOT lessons-learned (see docs/lessons/) and NOT mechanical conventions (see docs/conventions.md). Status: draft, not yet populated with governance decisions Stack constraints Zero dependencies in the runtime. Extrode.JauntyQ.Runtime has none. The generator, CLI, and schema-extraction packages carry their own dependencies separately. NativeAOT-compatible. No runtime reflection in the runtime's hot paths. Extrode.JauntyQ.Runtime is marked IsAotCompatible. ADO.NET only. All database integration goes through IDbConnection and DbDataReader. Providers are validated per-dialect (sqlserver, postgres, mysql, sqlite), never special cases hardcoded into the generator. Compile-time validation, not runtime. Queries are validated against a committed schema snapshot at build time. The runtime package does no schema inspection of its own. This file is a stub. Populate it with the project's actual won't-change decisions as they are made, following the same structure as docs/conventions.md."},{"u":"conventions.html","t":"Conventions — jauntyq","s":"General","h":[{"i":"file--directory-naming","t":"File & directory naming"},{"i":"git-workflow","t":"Git workflow"}],"b":"Conventions — jauntyq Mechanical how-we-work rules. Defaults are project-local; this file records project-specific deltas. Binding principles live in docs/constitution.md; project memory lives in docs/lessons/. File & directory naming kebab-case for files and dirs: mail-pipeline.md, data-model.sql. Except where an established convention dictates otherwise: README.md, CHANGELOG.md, LICENSE.md, Directory.Build.props, or language-mandated casing (C# PascalCase.cs). Unsure? Match the nearest existing sibling file. Git workflow main is release-only; dev is integration. Never commit directly to either. All work on named branches off dev (feat/ fix/ chore/ docs/ refactor/ test/). Commit often. Merge completed branches back to dev with --no-ff. Always merge once a branch is complete — never leave finished work unmerged. Release: dev -> main --no-ff + semver tag. Ask before deleting branches. This file is a stub. Populate it with project-specific mechanical conventions as they are established, following the same structure as jaunty's docs/conventions.md."},{"u":"00-overview-readme.html","t":"JauntyQ Documentation","s":"Overview","h":[{"i":"how-it-works","t":"How it works"},{"i":"these-docs","t":"These docs"}],"b":"JauntyQ Documentation JauntyQ is a compile-time SQL-to-C# source-generator ORM. You write real SQL in .sql files; JauntyQ validates it at build time against a committed schema snapshot and emits the fastest possible C#, typed getters by ordinal, no reflection, zero per-row boxing, no runtime SQL parsing. The generated code carries everything; the runtime package is intentionally near-empty. How it works Pull a schema snapshot from your database into a JSON file you commit (sqlserver, postgres, mysql, or sqlite). Write queries as .sql files, versioned alongside the code that uses them. At build time the generator parses those files against the snapshot and emits typed C# methods. If a column is renamed or a table drops, the build breaks, not production. Because there is no reflection or runtime code generation, the emitted code is Native AOT and trim compatible. These docs Why JauntyQ (and why not), the honest pitch: problems solved, problems deliberately not solved, and how to choose between JauntyQ, Jaunty, and EF Core. Pricing, free core, paid team-safety tier; flat per-organization pricing (Community, Team, Business, Enterprise); how the licensing works. Getting started, requirements, installing the packages, wiring the generator, and your first build. Learn by doing, a guided first hour against SQLite plus coding exercises, no database server required. Guides, task-focused walkthroughs: DDL as schema source, sequences, bulk insert, and migrating from Dapper/EF. Reference, the CLI, snapshot format, configuration, directives, dialect differences, diagnostics, and troubleshooting. Roadmap, where JauntyQ is headed. For quickstart instructions and the full feature walkthrough, see the repository README.md."},{"u":"00-overview-pricing.html","t":"Pricing","s":"Overview","h":[{"i":"free-everything-that-ships-an-app","t":"Free, everything that ships an app"},{"i":"paid-team-safety","t":"Paid, team safety"},{"i":"tiers","t":"Tiers"},{"i":"lapse-your-tooling-keeps-working","t":"Lapse, your tooling keeps working"},{"i":"licensing","t":"Licensing"},{"i":"distribution","t":"Distribution"},{"i":"ordering","t":"Ordering"}],"b":"Pricing JauntyQ is a commercial, source-available product with a free core and a paid team-safety tier. This page states the commercial terms. Pricing questions and orders go to Extrode via https://extrode.com/jauntyq. Pre-1.0 preview. While JauntyQ is 0.x, the paid commands run free under a Preview license that Extrode issues on request. A Preview license is honored by every 0.x build and by no 1.0 build. Nothing is revoked at 1.0: a 0.x binary keeps accepting it. The free core is not licensed by the file at all and is unaffected either way. Free, everything that ships an app The core is free for everyone, with no feature flags, no trial clocks, and no telemetry: the source generator and runtime in full: codegen, auto-CRUD with optimistic concurrency, upserts, bulk fast paths, sequences, enum capture, batched IN; all build-time analysis, JNT1xxx to JNT8xxx, including the deep query analysis (FK-graph joins, ORDER-BY-index, N+1 detection); jauntyq schema pull, migration simulation, and DDL-as-source. Code the generator emits into your project is yours to modify, compile, ship, and sell as part of your applications (see Licensing). Paid, team safety The paid tier is the tooling that catches schema drift and breaking migrations before they reach production. It ships as Extrode.JauntyQ.Cli.Premium, which installs the same jauntyq command with these verbs added: Migration impact analysis, jauntyq migrate impact: SAFE/RISKY/BREAKING classification of pending migrations against your actual query corpus. Database contract testing, jauntyq schema verify in CI, the Extrode.JauntyQ.Schema.Contract assertion API for your own test suite, and the boot-time startup guard. Per-service contracts and the schema registry for shared databases. Usage-aware severity: drift on objects your queries provably never touch will not fail your gate. Live EXPLAIN, jauntyq explain plan analysis over your query corpus. Tiers Tier Price Covers Support Community free Organizations under $1M annual revenue, non-commercial use, education GitHub issues, no SLA Team $349/year Up to 10 developers Email, 2-business-day response target Business $999/year Up to 50 developers Email, 2-business-day response target Enterprise custom Unlimited developers Priority support, custom SLA, named contact Pricing is flat per organization: one signed jaunty.license.json per org, no seat counting, no named-developer administration. CI and build agents never count against anything. Support is bundled into every paid tier; there is no separate support SKU, and the response targets above are commitments, not aspirations. Lapse, your tooling keeps working If a subscription ends, the features you were entitled to keep working on the versions you have; they print a renewal reminder, never a hard failure. Renewing restores updates and support. A lapse never turns exit codes red in CI; see licensing and activation. Licensing The core is licensed under the Islamic Software License - Restricted (ISL-R), Version 1.2: source viewable, use permitted, no modification or redistribution of the tool itself. Text: LICENSE.md, published at https://islamiclicense.org/isl-r/1.2/LICENSE.md. Generated output is covered by the Islamic Software License - Output Exception (ISL-OE), Version 1.2, adopted alongside the license: you may modify, compile, distribute and sell the code, schema snapshots and reports JauntyQ emits into your project, as part of your own work, with no source disclosure and no obligation to reproduce the license text. The license's ethical use restrictions continue to apply to that output. Text: EXCEPTION.md, published at https://islamiclicense.org/isl-oe/1.2/EXCEPTION.md. The adoption notice is NOTICE.md, and all three files ship inside every package. The paid components ship under the Islamic Software End User License Agreement (ISL-EULA), Version 1.0, published at https://islamiclicense.org/isl-eula/1.0/LICENSE.md; the grant is conditioned on an Order per the tiers above. The Output Exception applies under it too, so it does not matter which half emitted an artifact. Neither license is OSI-approved; JauntyQ is source-available, not open source, as a deliberate values decision. Distribution The free core is published to NuGet.org; the paid packages ship from a private feed to subscribers. A paid license is a signed file verified fully offline, and nothing phones home, ever. Mechanics: licensing and activation. Ordering To place an order, or to ask about evaluation or Enterprise terms, contact Extrode via https://extrode.com. Jaunty's commercial model is documented with Jaunty, not here."},{"u":"00-overview-why-jauntyq.html","t":"Why JauntyQ (and Why Not)","s":"Overview","h":[{"i":"the-core-idea-in-one-diagram","t":"The core idea in one diagram"},{"i":"the-problems-jauntyq-solves","t":"The problems JauntyQ solves"},{"i":"what-jauntyq-deliberately-does-not-do","t":"What JauntyQ deliberately does not do"},{"i":"jauntyq-or-jaunty","t":"JauntyQ or Jaunty?"},{"i":"next-steps","t":"Next steps"}],"b":"Why JauntyQ (and Why Not) JauntyQ moves the ORM to compile time. You write real SQL in .sql files; a Roslyn source generator validates every query against a schema snapshot you committed, and emits the fastest C# it knows how to write: typed getters by ordinal, explicit parameter types, no reflection, no runtime SQL parsing. The runtime package is intentionally near-empty - the generated code carries everything. This page is the honest pitch: the problems JauntyQ solves, the ones it deliberately does not, and how to know quickly if it fits. The core idea in one diagram Rename a column, drop a table, shrink a varchar - the build breaks at your desk, with the query file and reason in the error list. Not in staging. Not at 2 a.m. The problems JauntyQ solves Schema drift discovered too late. The classic ORM failure: the database changes, the code compiles fine, and the mismatch surfaces as a runtime exception or - worse - silently wrong data. In JauntyQ the schema snapshot is part of your source tree and every query is parsed against it on every build. jauntyq schema verify closes the loop against the live database (exit 0 = match, exit 2 = drift, differences listed); it is a paid verb, and the free tool exits 3 pointing at the install line. Runtime mapping cost. There is nothing to optimize at runtime because nothing happens at runtime: no reflection, no per-row boxing, no SQL parsing, typed ordinal getters emitted per query. A one-time shape guard validates each result set on first use, then gets out of the way. The generated code is Native AOT and trim compatible by construction. CRUD boilerplate. From the snapshot alone - zero .sql files - every table gets synthesized GetAll, GetById, Insert (returning the new identity), Update, Delete, Upsert (dialect-native MERGE / ON CONFLICT / ON DUPLICATE KEY), BulkInsert, and GetBy<ForeignKey> loaders, in sync and async forms, with POCO overloads. Write .sql only for the queries that are actually yours; a user file with the same entity and method name overrides the synthetic. Bad SQL that \"works\". The build also reviews your queries: cartesian joins (JNT8001), non-sargable predicates like functions on filtered columns (JNT8002), leading-wildcard LIKE (JNT8003), filters and joins on unindexed columns (JNT8004), duplicate queries (JNT8005), joins that match no declared foreign key (JNT8006), ORDER BY on an unindexed column (JNT8007), N+1 access patterns (JNT8008), and pages taken without a deterministic row order (JNT8009/JNT8010, a LIMIT/OFFSET whose ORDER BY is not tiebroken on something unique, so rows silently repeat or vanish between pages) surface as build warnings, using the index metadata and foreign-key graph in your snapshot. Values the database will reject or truncate. String longer than the column's nvarchar(40)? Decimal that cannot fit decimal(19,4)? Those are build errors (JNT5001/JNT5002) and guarded client-side with exact messages, not a SqlException after the round trip. Migrations as a blind spot. Put pending DDL in db/migrations/*.sql and the generator simulates it onto the snapshot before validating - so a query against a column your next migration drops is a build error today, while the migration is still in review. Lost updates. SQL Server rowversion columns are understood natively: excluded from inserts, appended to UPDATE/DELETE predicates, zero rows affected = conflict. Optimistic concurrency without a tracking layer. What JauntyQ deliberately does not do It does not hide SQL. There is no LINQ provider and no query builder. If writing SQL is a problem for your team, JauntyQ is not your tool - EF Core is over there. No runtime query composition. Queries are known at compile time, period. Dynamic search screens that assemble SQL from user input need a different tool (its sibling Jaunty handles runtime shapes). No change tracking, no identity map, no lazy loading. Row POCOs are plain data. SELECT * is a build error (JNT3002), with a paste-ready explicit column list in the message. Star projections are how shape drift sneaks in; JauntyQ closes that door on purpose. It demands snapshot discipline. The schema snapshot is committed and re-pulled when the database changes. If your team cannot keep that habit, the build errors will feel like friction rather than protection. .NET SDK 8.0+ to build. The generator runs inside Roslyn 4.12 (SDK 8.0.400+/VS 17.12+); your consuming project's own TargetFramework isn't restricted to net8.0 - Extrode.JauntyQ.Runtime also ships a netstandard2.0 target. One honest boxing caveat. Parameter values box once per call at the ADO.NET boundary (DbParameter.Value is object) - every data library pays this; on PostgreSQL JauntyQ avoids even that with typed NpgsqlParameter<T>. The read path allocates nothing per row beyond your POCO. Not open source. JauntyQ is source-available, not OSI-approved: the free core is licensed under the ISL-R (source viewable, use permitted) with a Generated Output Exception covering the code it emits into your project, and the paid team-safety tooling ships under the ISL-EULA. See pricing. Hard OSS requirement? Use Dapper or EF Core. JauntyQ or Jaunty? Same philosophy - SQL is yours, mapping is strict, reflection is not welcome - different binding time. You have Pick Queries known at build time, team willing to commit a schema snapshot JauntyQ Dynamic SQL, runtime result shapes, gradual migration from Dapper Jaunty A greenfield service that wants maximum build-time safety JauntyQ The desire for the framework to write SQL and track changes EF Core, honestly Next steps Overview - what is in these docs. Learn by doing - guided tutorial and coding exercises against SQLite, no server required. The repository README.md - full quickstart, directives reference, and write semantics."},{"u":"01-getting-started-readme.html","t":"Getting started","s":"Getting started","h":[{"i":"requirements","t":"Requirements"},{"i":"install-the-packages","t":"Install the packages"},{"i":"consuming-from-source-in-repo","t":"Consuming from source (in-repo)"},{"i":"point-the-generator-at-your-files","t":"Point the generator at your files"},{"i":"the-db-folder-layout","t":"The db/ folder layout"},{"i":"first-build","t":"First build"}],"b":"Getting started This section covers installing JauntyQ, the requirements it has on your project, and the minimum wiring to get generated code out of a build. Once you are set up, work through the first-hour tutorial and the feature guides. Requirements .NET SDK 8.0 or later. The generator is a Roslyn incremental source generator and runs inside the compiler; your project targets any framework Roslyn supports (net8.0+ recommended). A database provider package appropriate to your dialect, referenced by the consuming project, JauntyQ emits ADO.NET calls but does not bundle a driver: SQL Server / Azure SQL: Microsoft.Data.SqlClient PostgreSQL: Npgsql MySQL / MariaDB: MySqlConnector SQLite: Microsoft.Data.Sqlite A schema source, either a committed .schema.json snapshot pulled with the jauntyq CLI, or checked-in DDL (see DDL as schema source). The Extrode.JauntyQ.Runtime package is IsAotCompatible; the generated code uses no reflection and no runtime SQL parsing, so it is Native AOT and trim compatible provided your database provider is too. Install the packages The core packages are on NuGet.org. Reference both: <ItemGroup> <PackageReference Include=\"Extrode.JauntyQ.Generator\" Version=\"0.5.1\" PrivateAssets=\"all\" /> <PackageReference Include=\"Extrode.JauntyQ.Runtime\" Version=\"0.5.1\" /> <\/ItemGroup> Extrode.JauntyQ.Generator installs as a Roslyn analyzer automatically; the package ships the MSBuild props that register JauntyQDialect and JauntyQAutoCrud, so no OutputItemType=\"Analyzer\" or <CompilerVisibleProperty> entries are needed when consuming the package. Extrode.JauntyQ.Runtime carries the handful of types the generated code depends on and must be a normal (non-private) reference. Consuming from source (in-repo) If you build against the repository directly rather than the package, reference the projects instead, the generator as an analyzer, the runtime as a normal reference: <ItemGroup> <ProjectReference Include=\"..\\Extrode.JauntyQ.Generator\\Extrode.JauntyQ.Generator.csproj\" OutputItemType=\"Analyzer\" ReferenceOutputAssembly=\"false\" /> <ProjectReference Include=\"..\\Extrode.JauntyQ.Runtime\\Extrode.JauntyQ.Runtime.csproj\" /> <\/ItemGroup> Point the generator at your files The generator reads your SQL and schema through MSBuild AdditionalFiles. The standard glob picks up every .sql file under db/ (queries, migrations, and DDL alike, they are classified by folder) plus the snapshot: <ItemGroup> <AdditionalFiles Include=\"db\\**\\*.sql\" /> <AdditionalFiles Include=\"db\\schema\\*.schema.json\" /> <\/ItemGroup> The full set of properties, globs, and folder conventions is in the configuration reference. The db/ folder layout db/ schema/ jaunty.schema.json committed snapshot (authoritative schema source) tables/ Products/ GetById.sql one file per query; file name = method name GetByCategory.sql migrations/ 0001_add_column.sql pending DDL, applied on top of the snapshot Folder name under tables/ = entity name = generated accessor class (db.Products). File name (without .sql) = method name. Auto-CRUD methods (GetAll, GetById, Insert, Update, Delete, Upsert, BulkInsert) are synthesised for every table unless a same-named .sql file overrides them. First build Pull a snapshot (see the CLI reference). The CLI ships as a .NET global tool, dotnet tool install --global Extrode.JauntyQ.Cli installs it as jauntyq (the premium verbs come with Extrode.JauntyQ.Cli.Premium, same command). Working inside a clone of this repository, the equivalent without installing is: dotnet run --project src/Extrode.JauntyQ.Cli -f net8.0 -- schema pull --provider sqlserver \\ --connection-env MYDB_CONN \\ --output db/schema/jaunty.schema.json Add a query file, e.g. db/tables/Products/GetById.sql: -- @first SELECT ProductId, ProductName, UnitPrice FROM Products WHERE ProductId = @ProductId Build. Then use the generated surface: using var conn = new SqlConnection(connectionString); var db = new JauntyDb(conn); Product? p = db.Products.GetById(42); If a query does not match the schema, the build fails with a JNTxxxx diagnostic rather than compiling. If something does not generate as expected, see troubleshooting."},{"u":"02-learn-readme.html","t":"Your first hour with JauntyQ","s":"Learn","h":[{"i":"what-youll-build","t":"What you'll build"},{"i":"how-the-pieces-fit-together","t":"How the pieces fit together"},{"i":"step-1-create-the-database","t":"Step 1: Create the database"},{"i":"step-2-pull-a-schema-snapshot","t":"Step 2: Pull a schema snapshot"},{"i":"step-3-wire-up-the-generator","t":"Step 3: Wire up the generator"},{"i":"step-4-zero-sql-full-crud","t":"Step 4: Zero SQL, full CRUD"},{"i":"step-5-your-first-custom-query","t":"Step 5: Your first custom query"},{"i":"step-6-one-row-not-a-list------first","t":"Step 6: One row, not a list - -- @first"},{"i":"step-7-getting-the-new-id-back------identity","t":"Step 7: Getting the new id back - -- @identity"},{"i":"step-8-break-the-build-on-purpose","t":"Step 8: Break the build on purpose"},{"i":"step-9-transactions-with-jauntydb","t":"Step 9: Transactions with JauntyDb"},{"i":"step-10-a-value-safety-error-on-purpose","t":"Step 10: A value-safety error, on purpose"},{"i":"what-you-now-have","t":"What you now have"}],"b":"Your first hour with JauntyQ This is a hands-on walkthrough, not a reference page. You will create a tiny SQLite database, point JauntyQ at it, and watch typed C# appear before you write a single .sql file. Then you will write your first query, break the build on purpose (twice, for two different reasons), and see why both breaks are good news. Everything here runs against SQLite, so there is no server to install and no connection string to fight with. The APIs and directives are identical on SQL Server, PostgreSQL, and MySQL - only the --provider flag and the connection string change. What you'll build A tutorial project with: A tutorial.db SQLite file with two tables and a foreign key. A committed schema snapshot (db/schema/jaunty.schema.json). Zero hand-written SQL at first, then two small .sql files. A generator wired into the .csproj that turns both of those into typed C#. How the pieces fit together JauntyQ never talks to your database at build time. It reads two things you committed - your .sql files and a schema snapshot - and emits C#. Nothing here requires a live connection during dotnet build. The snapshot is produced separately, by Extrode.JauntyQ.Cli, against a real database (or a file, for SQLite). You commit it like any other source file. Step 1: Create the database Create tutorial.db from this DDL (schema.sql), for example with the sqlite3 CLI: sqlite3 tutorial.db < schema.sql. CREATE TABLE Categories ( CategoryId INTEGER PRIMARY KEY, CategoryName VARCHAR(30) NOT NULL ); CREATE TABLE Products ( ProductId INTEGER PRIMARY KEY, ProductName VARCHAR(40) NOT NULL, CategoryId INTEGER NULL REFERENCES Categories(CategoryId), UnitPrice NUMERIC NULL ); CREATE TABLE Shippers ( ShipperId INTEGER PRIMARY KEY, CompanyName VARCHAR(40) NOT NULL, Phone VARCHAR(24) NULL ); INSERT INTO Categories (CategoryName) VALUES ('Beverages'), ('Condiments'); INSERT INTO Products (ProductName, CategoryId, UnitPrice) VALUES ('Chai', 1, 18.00), ('Chang', 1, 19.00), ('Aniseed Syrup', 2, 10.00); INSERT INTO Shippers (CompanyName, Phone) VALUES ('Speedy Express', '(503) 555-9831'); INTEGER PRIMARY KEY in SQLite is the rowid alias (auto-increment). JauntyQ's SQLite extractor recognizes a single-column integer primary key as an identity column, the same as IDENTITY on SQL Server. The explicit VARCHAR(n) facets matter too: SQLite has no real column-length enforcement, so the snapshot only records a max length when the declared type carries one - that is what makes the value-safety step later in this guide possible. Step 2: Pull a schema snapshot dotnet run --project src/Extrode.JauntyQ.Cli -f net8.0 -- schema pull \\ --provider sqlite \\ --connection \"Data Source=tutorial.db\" \\ --output db/schema/jaunty.schema.json This writes db/schema/jaunty.schema.json: table names, column names, types, nullability, primary key and identity flags, plus the foreign key from Products.CategoryId to Categories.CategoryId. Commit this file. It is the contract the generator validates every query against - no database connection is needed at build time from here on. Step 3: Wire up the generator <ItemGroup> <AdditionalFiles Include=\"db\\**\\*.sql\" /> <AdditionalFiles Include=\"db\\schema\\*.schema.json\" /> <\/ItemGroup> <ItemGroup> <ProjectReference Include=\"..\\Extrode.JauntyQ.Generator\\Extrode.JauntyQ.Generator.csproj\" OutputItemType=\"Analyzer\" ReferenceOutputAssembly=\"false\" /> <ProjectReference Include=\"..\\Extrode.JauntyQ.Runtime\\Extrode.JauntyQ.Runtime.csproj\" /> <\/ItemGroup> Step 4: Zero SQL, full CRUD Build the project. You have not written a single .sql file yet, and you already have a typed data layer. For every table in the snapshot, JauntyQ synthesizes GetAll, GetById, Insert, Update, and Delete: using Microsoft.Data.Sqlite; using Extrode.JauntyQ.Generated; using var conn = new SqliteConnection(\"Data Source=tutorial.db\"); var db = new JauntyDb(conn); List<Product> products = db.Products.GetAll(); Product? chai = db.Products.GetById(1); // Products.CategoryId is a foreign key: you also get a loader for free. List<Product> beverages = db.Products.GetByCategoryId(1); int newId = db.Categories.Insert(\"Grains/Cereals\"); db.Categories.Delete(newId); This is the moment worth pausing on: nothing above came from a .sql file. Every method is generated from the schema snapshot alone, with ordinal reads, typed parameters, and an async twin (GetAllAsync, GetByIdAsync, ...) for each one. Step 5: Your first custom query Auto-CRUD covers full-table reads and single-column writes; anything with a WHERE, JOIN, or projection is a query you write yourself. Create db/tables/Products/GetByCategory.sql: select ProductId, ProductName, CategoryId, UnitPrice from Products where CategoryId = @CategoryId Rebuild. JauntyQ parses the file, validates every column against the snapshot, and emits List<Product> GetByCategory(int categoryId) (plus GetByCategoryAsync): List<Product> beverages = db.Products.GetByCategory(1); Folder name = entity, file name (without .sql) = method name. That is the entire convention. Step 6: One row, not a list - -- @first Directives are -- comment lines at the top of the file, stripped before the SQL reaches the database. Create db/tables/Products/GetByName.sql: -- @first select ProductId, ProductName, CategoryId, UnitPrice from Products where ProductName = @ProductName -- @first changes the return type from List<Row> to Row? - a single row or null, instead of a list you would only ever use the first element of: Product? chai = db.Products.GetByName(\"Chai\"); Step 7: Getting the new id back - -- @identity Auto-CRUD's synthetic Insert already carries -- @identity automatically on tables with exactly one identity column, which is why db.Categories.Insert(...) above returned an int. You can also write that explicitly, and override the synthetic in the process. Create db/tables/Categories/Insert.sql: -- @identity insert into Categories (CategoryName) values (@CategoryName) On SQLite (and PostgreSQL) this compiles to an insert with a RETURNING clause; on SQL Server it is OUTPUT INSERTED.<col>; on MySQL, a narrowed SELECT LAST_INSERT_ID(). You do not choose the dialect-specific SQL - the snapshot's dialect field decides that for you, and the C# signature is the same on every provider: int id = db.Categories.Insert(\"Grains/Cereals\"); Step 8: Break the build on purpose This is the step that should change how you think about ORMs. Rename a column directly in the database: sqlite3 tutorial.db \"ALTER TABLE Products RENAME COLUMN ProductName TO Name;\" Re-pull the snapshot so it reflects the live database: dotnet run --project src/Extrode.JauntyQ.Cli -f net8.0 -- schema pull \\ --provider sqlite \\ --connection \"Data Source=tutorial.db\" \\ --output db/schema/jaunty.schema.json Rebuild. GetByCategory.sql and GetByName.sql both still say ProductName, and that column no longer exists. The build fails with JNT2002, naming the missing column and the table it was expected on. This is the entire point of the design. A production system without this check would not fail here - it would fail the first time a real request hit that query, at 2am, in front of a customer. Here it fails on your machine, during dotnet build, before you ever deploy. Fix it by updating the .sql files to select Name instead of ProductName, and rebuild clean. Step 9: Transactions with JauntyDb JauntyDb exposes BeginTransaction() (and an async twin), returning a Transaction that wraps the underlying DbTransaction. Instance calls on db.* auto-enlist - no extra parameter to pass: using var tx = db.BeginTransaction(); int categoryId = db.Categories.Insert(\"Grains/Cereals\"); db.Products.Insert(\"Gnocchi di Nonna Alice\", categoryId, 38.00m); tx.Commit(); Disposing without calling Commit() rolls back - the safe default if something throws mid-flight. Static methods (Products.Insert(conn, ...)) never see the JauntyDb that holds the transaction, so they do not auto-enlist; stay on db.* inside a transaction block. Step 10: A value-safety error, on purpose Shippers.CompanyName is VARCHAR(40). Try inserting a literal that does not fit, in a throwaway .sql file or directly in a query you are testing: insert into Shippers (CompanyName, Phone) values ('Trans National Overnight Parcel Delivery Services', '555-0100') That literal is 49 characters. The build fails before it ever reaches a database: error JNT5001: String literal (49 chars) exceeds shippers.companyname max length of 40. It would truncate on write and can never match on read. The same guard runs at runtime for parameterized writes: Insert, Update, Upsert, and the POCO overloads all validate string and binary parameter lengths before opening the connection, raising an ArgumentException that names the exact column and limit instead of a round-trip that ends in a silent truncation. What you now have A database with two custom queries, one deliberate build break you fixed, one value-safety error you saw coming, and a transaction that touched two tables atomically - all without writing a single line of ADO.NET. The feedback loop is the whole product: rename a column, and the build tells you where it hurts, at compile time, with the exact query and column named. That is a fundamentally different failure mode than the one most data access code lives with. Continue to exercises.md to practice this on your own, without a script to follow."},{"u":"02-learn-exercises.html","t":"Exercises","s":"Learn","h":[{"i":"exercise-1-add-a-table-get-crud-for-free","t":"Exercise 1: Add a table, get CRUD for free"},{"i":"exercise-2-override-a-synthetic","t":"Exercise 2: Override a synthetic"},{"i":"exercise-3-fix-a-join-the-analyzer-flags","t":"Exercise 3: Fix a JOIN the analyzer flags"},{"i":"exercise-4-atomic-parent--children-insert","t":"Exercise 4: Atomic parent + children insert"},{"i":"exercise-5-simulate-schema-drift","t":"Exercise 5: Simulate schema drift"}],"b":"Exercises These build directly on the tutorial database from README.md: Categories, Products, and Shippers in tutorial.db, with the schema snapshot at db/schema/jaunty.schema.json. Work through them in order - each one assumes the previous one's schema changes are in place and re-pulled. Exercise 1: Add a table, get CRUD for free Goal: Add a Suppliers table and use its full auto-CRUD surface without writing a single .sql file. Starter: -- Add to your schema (e.g. run against tutorial.db with sqlite3): CREATE TABLE Suppliers ( SupplierId INTEGER PRIMARY KEY, CompanyName VARCHAR(40) NOT NULL, City VARCHAR(15) NULL -- TODO: no other columns needed yet ); -- TODO: give Products a foreign key to Suppliers so a loader gets generated: -- ALTER TABLE Products ADD COLUMN SupplierId INTEGER REFERENCES Suppliers(SupplierId); // TODO: re-pull the schema snapshot, rebuild, then: // - insert a supplier and capture its id // - fetch it back with GetById // - insert a product referencing it // - call the FK loader JauntyQ generated for you Hints: The command is the same one from the tutorial: dotnet run --project src/Extrode.JauntyQ.Cli -f net8.0 -- schema pull --provider sqlite --connection \"Data Source=tutorial.db\" --output db/schema/jaunty.schema.json. A foreign key only shows up in the snapshot if it is a real REFERENCES (or table-level FOREIGN KEY) constraint in the DDL - a same-named column alone is not enough. The loader is named GetBy<FkColumn>, so a SupplierId foreign key on Products gives you db.Products.GetBySupplierId(int). Solution CREATE TABLE Suppliers ( SupplierId INTEGER PRIMARY KEY, CompanyName VARCHAR(40) NOT NULL, City VARCHAR(15) NULL ); ALTER TABLE Products ADD COLUMN SupplierId INTEGER REFERENCES Suppliers(SupplierId); dotnet run --project src/Extrode.JauntyQ.Cli -f net8.0 -- schema pull \\ --provider sqlite \\ --connection \"Data Source=tutorial.db\" \\ --output db/schema/jaunty.schema.json int supplierId = db.Suppliers.Insert(\"Exotic Liquids\", \"London\"); Supplier? supplier = db.Suppliers.GetById(supplierId); int productId = db.Products.Insert(\"Chai\", 1, 18.00m, supplierId); List<Product> fromSupplier = db.Products.GetBySupplierId(supplierId); No .sql file was written for any of this: Insert, GetById, and the GetBySupplierId FK loader are all synthesized from the snapshot alone. Exercise 2: Override a synthetic Goal: Confirm that a user .sql file with the same entity and method name as a synthetic wins, silently. Starter: -- db/tables/Products/GetAll.sql -- TODO: select the same columns as the synthetic GetAll, but order the -- rows by UnitPrice descending instead of the synthetic's default order. select ProductId, ProductName, CategoryId, UnitPrice from Products -- TODO: add an ORDER BY here Hints: The rule from the tutorial: \"A user .sql file with the matching entity + method name overrides the synthetic.\" Folder name = entity, file name (without .sql) = method name - db/tables/Products/GetAll.sql overrides Products.GetAll. You do not need to change any C# call site: db.Products.GetAll() now runs your SQL instead of the generated one. Prove the override took effect by asserting the returned order, not just that the call compiles. Solution -- db/tables/Products/GetAll.sql select ProductId, ProductName, CategoryId, UnitPrice from Products order by UnitPrice desc List<Product> products = db.Products.GetAll(); // If the synthetic (unordered by price) were still in effect, this would // not reliably hold. With the override in place, it always does: for (int i = 1; i < products.Count; i++) { Debug.Assert(products[i - 1].UnitPrice >= products[i].UnitPrice); } Exercise 3: Fix a JOIN the analyzer flags Goal: Write a join between Products and Categories, trigger a JNT8002 warning by wrapping a filter column in a function, then fix it. Starter: -- db/tables/Products/GetByCategoryName.sql select p.ProductId, p.ProductName, p.UnitPrice, c.CategoryName from Products p join Categories c on c.CategoryId = p.CategoryId where upper(c.CategoryName) = @CategoryName -- TODO: this compiles, but rebuild and read the warning JauntyQ prints. -- TODO: rewrite the WHERE clause so the column is not wrapped in a function. Hints: This is JNT8002 from the performance analyzer: \"a function on the column defeats any index.\" It fires because upper(...) wraps c.CategoryName, not because of the join itself. The fix direction from the analyzer's own message: \"Compute on the parameter side instead.\" Keep the column bare in the WHERE clause and normalize the value before you bind it as a parameter (or pass a correctly-cased value directly, since the exercise data is small enough that exact-case matching is fine). JNT8002 is a warning, not an error - the query still compiles either way. The point of the exercise is to make the warning go away, not to work around a build failure. Solution -- db/tables/Products/GetByCategoryName.sql select p.ProductId, p.ProductName, p.UnitPrice, c.CategoryName from Products p join Categories c on c.CategoryId = p.CategoryId where c.CategoryName = @CategoryName // Normalize on the call side instead of wrapping the column in SQL: List<Result> beverages = db.Products.GetByCategoryName(\"Beverages\"); The column (c.CategoryName) is now bare in the WHERE clause, so nothing prevents an index seek on it. JNT8002 no longer fires because the check looks for a function wrapping the column, not the parameter. Exercise 4: Atomic parent + children insert Goal: Insert a new Category and several Products that reference it, all inside one transaction, using the identity JauntyQ hands back from the parent insert. Starter: public static void AddCategoryWithProducts(JauntyDb db) { // TODO: start a transaction on db // TODO: insert a new Category, capture its identity // TODO: insert two or more Products using that CategoryId // TODO: commit - and make sure a thrown exception rolls everything back } Hints: Categories.Insert already returns the new identity as an int - that is what -- @identity (implicit on auto-CRUD inserts, or explicit if you write it yourself) gives you. Stay on db.* for every call inside the transaction. Static methods (Products.Insert(conn, ...)) do not see the JauntyDb-held transaction and will not auto-enlist. Disposing the transaction without calling Commit() rolls back - you do not need a manual try/catch to undo a partial insert on failure, just don't call Commit() on the failing path. Solution public static void AddCategoryWithProducts(JauntyDb db) { using var tx = db.BeginTransaction(); int categoryId = db.Categories.Insert(\"Grains/Cereals\"); db.Products.Insert(\"Gnocchi di Nonna Alice\", categoryId, 38.00m); db.Products.Insert(\"Singaporean Hokkien Fried Mee\", categoryId, 14.00m); tx.Commit(); } If anything above throws before tx.Commit() runs, disposing tx at the end of the using block rolls back the category insert along with both product inserts - there is no partially-created category left behind. Exercise 5: Simulate schema drift Goal: Change the live database without re-pulling, run schema verify, read the reported drift, then reconcile it. Before you start: schema verify is gated by the contract-testing entitlement (see Licensing and activation). Without an activated license, it exits 3 with an entitlement-required message instead of the 0/2 outcomes below, that is expected, not a setup mistake. If you have an evaluation license, activate it first (jauntyq activate --license path/to/jaunty.license.json, from an installed Extrode.JauntyQ.Cli.Premium; that tool is not built from this repository); otherwise, read through this exercise to understand the behavior rather than running it directly. Starter: # TODO: alter tutorial.db directly, without touching db/schema/jaunty.schema.json sqlite3 tutorial.db \"ALTER TABLE Products ADD COLUMN Notes VARCHAR(100);\" # TODO: run schema verify against the now-drifted database and read its output jauntyq schema verify \\ --provider sqlite \\ --connection \"Data Source=tutorial.db\" \\ --output db/schema/jaunty.schema.json # TODO: what exit code did that produce? Check it, then fix the drift. Hints: schema verify never touches your .sql files or the generator - it only compares the live database against the committed snapshot and reports. Exit code 0 means match, exit code 2 means drift, with the differences listed. A brand-new table would print as an informational \"new table... not in snapshot\" line rather than counting as drift; a changed column on an existing table (like this one) counts as drift and produces exit code 2. The fix is the same command you already know: re-pull, then rebuild so the generator picks up the new column if you want to query it. Solution sqlite3 tutorial.db \"ALTER TABLE Products ADD COLUMN Notes VARCHAR(100);\" jauntyq schema verify \\ --provider sqlite \\ --connection \"Data Source=tutorial.db\" \\ --output db/schema/jaunty.schema.json # exit code 2 - \"SCHEMA DRIFT: 1 difference(s) between snapshot ... and the # live database\", listing the new Products.Notes column. # Reconcile the snapshot with reality: dotnet run --project src/Extrode.JauntyQ.Cli -f net8.0 -- schema pull \\ --provider sqlite \\ --connection \"Data Source=tutorial.db\" \\ --output db/schema/jaunty.schema.json # Re-running verify now exits 0: \"Snapshot ... matches the live database.\" Auto-CRUD's synthetic GetAll/Insert/etc. for Products now include Notes too, without any .sql file changes - the snapshot is the only thing that changed."},{"u":"03-guides-readme.html","t":"Guides","s":"Guides","h":[],"b":"Guides Task-focused walkthroughs for specific JauntyQ features. Each guide assumes you have the generator wired into a project (see the repository README.md Quickstart and the first-hour tutorial). DDL as schema source, build the schema from CREATE TABLE files instead of pulling a live snapshot. Sequences, typed accessors for database sequence objects (SQL Server and PostgreSQL). Bulk insert, insert many rows fast via the provider's native bulk-copy path. Migrating from Dapper or EF Core, how an existing data layer maps onto JauntyQ's SQL-first, compile-time model. Database contract testing, assert a live database matches the committed snapshot from your own test suite, gating CI on drift. Runtime startup schema verification, fail fast at boot (or warn, or skip, per policy) when the live database has drifted from the compiled snapshot. Central schema-dependency registry, name schema snapshots in jaunty.registry.json, declare which services depend on which tables, and query the blast radius (jauntyq registry ...). Live EXPLAIN plan analysis, opt-in jauntyq explain runs the query corpus through the database's estimate-only EXPLAIN and flags plan-level problems (large-table scans, missing-index sorts) by severity."},{"u":"03-guides-bulk-insert.html","t":"Bulk insert","s":"Guides","h":[{"i":"api","t":"API"},{"i":"per-dialect-fast-paths","t":"Per-dialect fast paths"},{"i":"mysql--mariadb-enable-local-infile","t":"MySQL / MariaDB: enable LOCAL INFILE"},{"i":"choosing-bulk-vs-looped-insert","t":"Choosing bulk vs looped insert"}],"b":"Bulk insert Every table gets a BulkInsert method that inserts many rows in one round of work, far faster than looping single-row Insert calls. Where the provider has a native bulk-copy path, JauntyQ uses it; otherwise it falls back to a single transaction with one prepared command. API var db = new JauntyDb(conn); int inserted = db.Products.BulkInsert(newProducts); // IEnumerable<Product> int inserted2 = await db.Products.BulkInsertAsync(newProducts, cancellationToken); // Static overloads take an explicit connection: int inserted3 = Products.BulkInsert(conn, newProducts); int inserted4 = await Products.BulkInsertAsync(conn, newProducts, cancellationToken); BulkInsert returns the number of rows inserted (int; async returns Task<int>). It accepts IEnumerable<Row>, so you can stream rows in without materializing them all first. Identity and rowversion columns are excluded from the insert (the database assigns them), exactly as with single-row Insert. Per-dialect fast paths The emitted body specializes on the snapshot dialect: Dialect Mechanism PostgreSQL Npgsql binary COPY (NpgsqlBinaryImporter) SQL Server SqlBulkCopy MySQL / MariaDB MySqlBulkCopy SQLite / other Portable single-transaction prepared-command loop SQL Server and MySQL feed their bulk-copy through a single reflection-free, AOT-safe DbDataReader-over-IEnumerable<Row> adapter emitted once per table (ordinal-based, no reflection), so the fast paths stay Native-AOT compatible. MySQL / MariaDB: enable LOCAL INFILE MySqlBulkCopy requires LOCAL INFILE to be enabled on both sides: Connection string: add AllowLoadLocalInfile=true. Server: local_infile must be ON (SET GLOBAL local_infile = 1, or the server/container configured with --local-infile=1). JauntyQ cannot set these for you and documents the requirement on the generated method. Note that MySqlConnector describes its MySqlBulkCopy API as experimental and subject to change. // Connection string must permit local infile for the MySQL fast path: var conn = new MySqlConnection(\"Server=...;Database=...;AllowLoadLocalInfile=true\"); Choosing bulk vs looped insert Use BulkInsert when you have many rows to add at once. For a handful of rows, or when you need each row's assigned identity back, use single-row Insert (which can return the identity via -- @identity); bulk copy does not return per-row generated keys."},{"u":"03-guides-contract-testing.html","t":"Database contract testing","s":"Guides","h":[{"i":"installing-the-package","t":"Installing the package"},{"i":"writing-a-contract-test","t":"Writing a contract test"},{"i":"the-severity-model","t":"The severity model"},{"i":"ignoring-objects","t":"Ignoring objects"},{"i":"per-service-contracts-scoping-to-your-slice","t":"Per-service contracts: scoping to your slice"},{"i":"usage-aware-severity-downgrading-drift-you-cannot-hit","t":"Usage-aware severity: downgrading drift you cannot hit"},{"i":"1-export-the-usage-manifest-from-your-corpus","t":"1. Export the usage manifest from your corpus"},{"i":"2-opt-in-at-verification-time","t":"2. Opt in at verification time"},{"i":"the-safety-invariant-downgrade-only-never-in-doubt","t":"The safety invariant: downgrade-only, never in doubt"},{"i":"inspecting-the-report","t":"Inspecting the report"},{"i":"where-to-point-the-test","t":"Where to point the test"},{"i":"failures-are-never-silent","t":"Failures are never silent"},{"i":"see-also","t":"See also"}],"b":"Database contract testing JauntyQ compiles your queries against jaunty.schema.json, a committed snapshot of the database structure. Every generated reader is a promise about the live database (for example, a column read with GetInt16 because the snapshot says it is smallint). That promise holds only while the live database still matches the snapshot. When a migration renames a column, tightens nullability, or drops a table and the snapshot is not re-pulled, the generated code keeps compiling but starts failing at runtime. Contract testing closes that gap: it reflects a live database, compares it to the committed snapshot, and classifies every difference as breaking or compatible, so drift fails CI before it reaches production. There are two entry points, both driven by the same classifier: The Extrode.JauntyQ.Schema.Contract package, a test-embeddable assertion API you drop into your own xUnit / NUnit / MSTest suite. The jauntyq schema verify CLI command, for pipelines without a test host (see the CLI reference). Installing the package Extrode.JauntyQ.Schema.Contract ships on the private feed. Add it to your test project (it carries the ADO.NET provider dependencies, so it is a test-time / tooling package, not part of the zero-dependency core): <PackageReference Include=\"Extrode.JauntyQ.Schema.Contract\" Version=\"x.y.z\" /> Writing a contract test using Extrode.JauntyQ.Schema.Contract; using Xunit; public class SchemaContractTests { [Fact] public async Task Database_matches_the_compiled_contract() { var connectionString = Environment.GetEnvironmentVariable(\"CONTRACT_DB\")!; var result = await SchemaContract .Against(connectionString, ContractDialect.SqlServer) .LoadSnapshotFromFile(\"db/schema/jaunty.schema.json\") .VerifyAsync(); result.AssertNoBreakingDrift(); } } AssertNoBreakingDrift() throws a SchemaContractException, failing the test, when any breaking difference is present, and lists each offending object. A compatible difference (an added table, a widened column, a loosened nullability) does not fail the assertion; use AssertNoDrift() if you want the strict \"exact match\" gate instead. The dialect is one of ContractDialect.SqlServer, Postgres, MySql, Sqlite. The severity model Severity is decided by a fixed structural rule set. It depends only on the shape change, never on which queries reference the object (unless you opt into usage-aware severity, which may downgrade, never raise, severity for unreferenced objects): Change Severity Table or column missing from the live database Breaking Column reorder / rename at a position (readers bind by ordinal) Breaking Type change, length narrowing, precision/scale change Breaking null → not null, primary-key / identity / row-version / unicode change Breaking Column added, length widening, not null → null Compatible Table present live but absent from the snapshot Compatible Ignoring objects Exclude tables or columns you do not want to gate on, a framework migration-history table, an ops-managed audit column, with case-insensitive ignore rules: var result = await SchemaContract .Against(connectionString, ContractDialect.Postgres) .LoadSnapshotFromFile(\"db/schema/jaunty.schema.json\") .IgnoreTables(\"__EFMigrationsHistory\", \"audit_log\") .IgnoreColumns(\"orders.internal_notes\") .VerifyAsync(); result.AssertNoBreakingDrift(); Per-service contracts: scoping to your slice A whole-snapshot contract is exactly right when one service owns its database. When several services share one database, it over-asserts: service B adding a column to its table would show up as drift in service A's contract even though A never touches that table. Per-service scoping narrows the contract to the slice a service actually depends on, its owned/consumed tables, so: drift inside the slice fails, with the exact same severity rules as an unscoped contract (the scope is a filter over the same classifier, never a second rule set); changes outside the slice produce no drift of any severity, another team's migration cannot fail your gate. Supply the scope explicitly: var result = await SchemaContract .Against(connectionString, ContractDialect.Postgres) .LoadSnapshotFromFile(\"db/schema/jaunty.schema.json\") .ForTables(\"orders\", \"order_items\") // only this slice is compared .VerifyAsync(); result.AssertNoBreakingDrift(); or derive it from the schema-dependency registry (feature 009), so the dependence is declared once, in jaunty.registry.json, and reused everywhere: var result = await SchemaContract .Against(connectionString, ContractDialect.Postgres) .LoadSnapshotFromFile(\"db/schema/jaunty.schema.json\") .ForService(\"billing\", \"jaunty.registry.json\") // scope = billing's `consumes` tables .VerifyAsync(); ForColumns(\"orders.total\") narrows further, to specific columns of a table, other columns of that table stop being your concern. Both compose with IgnoreTables/IgnoreColumns, which still exclude within the slice. Semantics worth knowing: An in-scope table or column missing or narrowed in the live database is breaking, a depended-on object vanished. An added column in a consumed in-scope table stays compatible. The report is never silent about scope: result.Report.Scope names the service and lists exactly which tables were compared, each tagged owned/consumed (annotation only, both are compared with the same rules). A scope naming a table the snapshot does not declare fails with a ServiceScopeException, a scope/registry error, distinct from drift. A service that declares no consumes yields an empty scope: the result is vacuously green and the report says \"no scope\" explicitly, never a pass-as-if-verified. Two services may both depend on orders; each verifies it independently. The CLI equivalent is jauntyq schema verify --service <id> [--registry <path>] (see the CLI reference). Usage-aware severity: downgrading drift you cannot hit The structural rules are deliberately conservative: a dropped column is breaking whether or not any query reads it. For a service that touches a narrow slice of a wide table, that fails the contract on changes the service is genuinely immune to. Usage-aware severity (opt-in) layers what JauntyQ already knows offline, exactly which (table, column) pairs the generated queries reference, on top of the structural classifier: a structurally-breaking change to an object no generated query references is downgraded to informational (it cannot break this app); a change to a referenced object keeps its structural severity; with no manifest, nothing changes, the structural rules stay the default. 1. Export the usage manifest from your corpus jauntyq usage export --queries db/queries --snapshot db/schema/jaunty.schema.json \\ --output jaunty.usage.json The manifest is the union of every referenced table and table.column across your hand-written .sql files and the auto-CRUD synthetics the generator emits (pass --no-auto-crud only if your project sets <JauntyQAutoCrud>false<\/JauntyQAutoCrud>), plus the snapshot hash it was built against. Commit it, and regenerate it in CI whenever queries or the snapshot change so it never drifts from the code. 2. Opt in at verification time var result = await SchemaContract .Against(connectionString, ContractDialect.Postgres) .LoadSnapshotFromFile(\"db/schema/jaunty.schema.json\") .WithUsage(\"jaunty.usage.json\") // opt-in: downgrade-only .VerifyAsync(); result.AssertNoBreakingDrift(); // downgraded drift no longer throws CLI equivalent: jauntyq schema verify ... --usage jaunty.usage.json. The safety invariant: downgrade-only, never in doubt Usage-aware mode can only ever soften a report, and only when the object is provably unreferenced. It never upgrades a severity, and under any uncertainty it keeps the structural verdict: an unqualified column in a multi-table query marks every in-scope table's columns potentially referenced (the same safe over-approximation the migration-impact classifier uses); a query that doesn't parse (or can't be attributed to a table) disables all downgrades, nothing is provable while it exists; a query invalid against the snapshot marks all its tables potentially referenced; a table drop is downgraded only when no column of that table is referenced anywhere; a column reorder is downgraded only when the whole table is unreferenced, a reorder can shift referenced columns. So a stale or over-broad manifest can make the mode over-conservative, never unsafe: a real breaking change on something you use always stays breaking. Downgrades are never silent: each downgraded entry stays in the report with Downgraded = true, its original StructuralSeverity, and the reason; the report's Usage block records the manifest hash and the downgrade count. If the manifest was built against a different snapshot than the one being verified, the report is flagged stale (and the CLI warns), re-run jauntyq usage export. Usage-aware severity composes with per-service scoping: the scope filters which objects are compared, usage adjusts the severity of what remains (scope-then-usage). Both only ever narrow failures. Inspecting the report For custom assertions or CI annotations, read the structured report instead of (or in addition to) asserting: var result = await SchemaContract.Against(conn, ContractDialect.MySql) .LoadSnapshotFromFile(\"db/schema/jaunty.schema.json\") .VerifyAsync(); foreach (var drift in result.Report.Drifts) Console.WriteLine($\"{drift.Severity}: {drift.Message}\"); string json = result.Report.ToJson(); Where to point the test Contract testing is only meaningful against a database that reflects what the app will run on. Good targets: A Testcontainers instance seeded with your migrations (per-run, hermetic). A CI service database the pipeline provisions. A staging database, as a pre-deploy smoke test. The verification reflects the live database over the network, so it belongs in an integration / end-to-end test stage, not a fast unit-test stage. Failures are never silent If the database cannot be reached, VerifyAsync() throws a SchemaContractConnectionException; if the snapshot file is missing or unreadable it throws too. A verification that could not actually run is never reported as a passing contract. If you want the test to be skipped when no database is available (e.g. Docker absent locally), guard it in your own test, the library itself does not soft-skip. See also Runtime startup schema verification, the same classifier at boot time: fail fast (or warn) when the database a deployment actually connects to has drifted. CLI reference, schema verify, the same classifier for pipelines without a test host. Schema snapshot format, the file the contract is checked against. Migration impact analysis, the offline migrate impact command for pending migration sets."},{"u":"03-guides-ddl-as-schema-source.html","t":"DDL as schema source","s":"Guides","h":[{"i":"when-to-use-it","t":"When to use it"},{"i":"setup","t":"Setup"},{"i":"precedence-json-snapshot-always-wins","t":"Precedence: JSON snapshot always wins"},{"i":"migrations-still-apply-on-top","t":"Migrations still apply on top"},{"i":"supported-ddl","t":"Supported DDL"},{"i":"diagnostics","t":"Diagnostics"},{"i":"limitations","t":"Limitations"}],"b":"DDL as schema source JauntyQ normally validates your queries against a schema snapshot you pull from a live database with jauntyq schema pull and commit as .schema.json. DDL mode is an alternative input for projects that would rather define the schema from CREATE TABLE files, with no database connection ever required, sqlc-style. When to use it You keep your schema as checked-in DDL and do not want a separate snapshot step. CI has no database to pull from and you would rather not commit a JSON snapshot. You are prototyping a schema before any database exists. If you already pull a .schema.json, keep doing that: the snapshot carries richer metadata (see Limitations). Setup Put your schema DDL under a db/ddl/ folder, ordered by filename: db/ ddl/ 001_schema.sql 002_more_tables.sql -- db/ddl/001_schema.sql CREATE TABLE Widgets ( WidgetId INTEGER PRIMARY KEY, Name TEXT NOT NULL, Quantity INTEGER NOT NULL ); Declare the dialect in the .csproj (DDL mode has no snapshot to infer it from): <PropertyGroup> <JauntyQDialect>sqlite<\/JauntyQDialect> <\/PropertyGroup> <ItemGroup> <AdditionalFiles Include=\"db\\**\\*.sql\" /> <\/ItemGroup> Valid values: sqlserver, postgres, mysql, sqlite. The standard db\\**\\*.sql glob already picks up db/ddl/*.sql; you do not need a separate entry. When you consume JauntyQ as a package, <JauntyQDialect> reaches the generator automatically (the package ships the property registration); no extra <CompilerVisibleProperty> is required. Do not commit a db/schema/*.schema.json snapshot for this project. See precedence below. That is all. Build the project and the full typed surface (auto-CRUD, row POCOs, value-safety guards) is generated from the DDL exactly as it would be from a pulled snapshot. Precedence: JSON snapshot always wins DDL mode is only active when no .schema.json snapshot is present. If a snapshot is present, it is authoritative and any db/ddl/*.sql files are silently ignored (no merge, no error). This keeps a pulled snapshot the single source of truth whenever you have one. Migrations still apply on top db/migrations/*.sql migrations are applied on top of the DDL-built base schema exactly as they apply on top of a pulled snapshot. The base schema (from DDL) is built first, then migrations simulate on top of it. Supported DDL The same subset the migration simulator understands: CREATE TABLE, column definitions, type facets, NOT NULL, PRIMARY KEY (inline or table-level), IDENTITY. DROP TABLE [IF EXISTS]. ALTER TABLE ADD / DROP COLUMN / ALTER COLUMN (plus MySQL MODIFY and PostgreSQL TYPE). Statements split on ; and GO. Index, transaction, and permission statements are ignored (they cannot change the model JauntyQ needs). Anything else, renames in particular, is JNT9001 (a warning that the effective schema may be incomplete). DDL that is invalid against the schema being built (creating a table that exists, dropping one that does not) is JNT9002 (an error). Diagnostics Code When JNT9003 db/ddl/*.sql files are present with no snapshot, but <JauntyQDialect> is missing or not one of sqlserver/postgres/mysql/sqlite. JNT9002 A DDL statement targets a table/column that does not exist (or already exists). JNT9001 A DDL statement is not simulated (e.g. a rename); the effective schema may be incomplete. Limitations The migration parser and schema simulator do not carry secondary UNIQUE-index metadata. A DDL-sourced table exposes its primary key, identity, and column types, but no secondary unique indexes. Two consequences, both matching every migration-based schema (not specific to DDL mode): The JNT8004 composite-index performance check and Upsert alternate-key resolution only ever see the primary key for a DDL-sourced table. If you rely on unique-index-aware performance analysis or alternate-key upserts, pull a .schema.json snapshot instead; it captures index metadata."},{"u":"03-guides-explain-analysis.html","t":"Live EXPLAIN plan analysis","s":"Guides","h":[{"i":"the-contract-opt-in-estimates-only-never-breaks-a-build","t":"The contract: opt-in, estimates only, never breaks a build"},{"i":"parameters","t":"Parameters"},{"i":"severity-heuristics","t":"Severity heuristics"},{"i":"failing-a-pipeline-on-purpose---fail-on","t":"Failing a pipeline on purpose: --fail-on"},{"i":"optional-msbuild-wiring-off-by-default","t":"Optional MSBuild wiring (off by default)"},{"i":"what-it-will-not-do","t":"What it will not do"}],"b":"Live EXPLAIN plan analysis JauntyQ's static performance diagnostics (JNT8001–JNT8011) reason about queries against the committed snapshot, entirely offline. They cannot see what the database's actual planner will do: row-count estimates, chosen access paths, sequential scans on large tables, or expensive sorts. The database already knows all of this via EXPLAIN. jauntyq explain closes that gap: point it at a representative database and it runs each .sql query in your corpus through the dialect's estimate-only EXPLAIN, parses the plan, and reports plan-level problems by severity, attributed to the query file that produced them. export DEV_DB='Host=localhost;Database=shop;Username=dev;Password=...' jauntyq explain --provider postgres --connection-env DEV_DB --queries db/tables jauntyq explain, postgres (estimated plans; costs are planner estimates from the connected database at run time) Orders/GetByCustomer.sql: Seq Scan on orders [est. cost 23345] [high] sequential scan on 'orders' (est. 1,200,000 rows), consider an index covering the filtered column(s) Articles/GetBySlug.sql: Index Scan on articles [est. cost 8.3] 1 analyzed, 1 cached, 0 skipped, 0 not analyzed (cap 10); 1 high / 0 informational problem(s). This is a premium feature (entitlement live-explain); without an entitling license the verb exits 3 and does nothing. The contract: opt-in, estimates only, never breaks a build Never in the generator. The Roslyn source generator stays fully offline; it never opens a database connection and a normal dotnet build is completely unaffected by this feature. jauntyq explain is a separate, on-demand CLI tool. Estimate-only. Every dialect uses the plan-without-executing variant: Postgres EXPLAIN (FORMAT JSON) (never ANALYZE), SQLite EXPLAIN QUERY PLAN, MySQL EXPLAIN FORMAT=JSON, SQL Server estimated SET SHOWPLAN_XML ON. No query runs; no data is read or mutated. Write statements (INSERT/UPDATE/DELETE/DDL, including data-modifying CTEs) are skipped entirely, safety over coverage. Never breaks. Report-only by default (exit 0). An unreachable database, auth failure, or unexplainable query is a per-query skip with a note, never an error. With no connection string configured the run is a clean no-op, which is what makes the optional CI wiring below safe. Capped. At most --max (default 10) live EXPLAIN calls per run; any remainder is listed explicitly as not analyzed (cap reached), no silent truncation. Cached. Results are memoized in .jaunty/explain-cache.json (gitignored), keyed by (dialect, normalized-SQL fingerprint, schema-snapshot fingerprint). Unchanged queries cost nothing on repeat runs, and cached queries do not consume the cap, successive runs walk further through a large corpus. Editing a query or re-pulling the snapshot invalidates exactly the affected entries. Parameters EXPLAIN needs bindable placeholders, not real values. The analyzer scans each query for @Name / :Name / $Name (and Postgres $1 positional) placeholders and binds representative typed nulls, plans are estimates, so sample data is unnecessary. Severity heuristics Finding Severity Sequential/full table scan, est. rows ≥ threshold (default 10,000) high Sequential/full table scan below the threshold informational Full scan where the dialect exposes no row estimate (SQLite) informational Sort step whose est. cost is ≥ 50% of the plan's total cost high Sort step below the dominance ratio informational SQLite USE TEMP B-TREE FOR ORDER BY/GROUP BY (always means no supporting index) high Index-backed access (seeks, index/covering-index scans) not flagged Costs and row counts are planner estimates from the connected database at run time, they vary with statistics, data volume, and engine version. Thresholds compare within a single run; do not treat costs as comparable across environments. Failing a pipeline on purpose: --fail-on By default the exit code is 0 no matter what the report says. To gate a dedicated CI stage, opt in: jauntyq explain --provider postgres --connection-env CI_EXPLAIN_DB \\ --queries db/tables --fail-on high # exit 0 = nothing at/above the threshold; exit 2 = a high-severity plan problem --fail-on informational fails on any finding; --fail-on high (recommended) fails only on high-severity ones. Optional MSBuild wiring (off by default) Exactly like the documented schema verify wiring: an explicit Exec target guarded by a connection variable. When the variable is unset, the default everywhere, including CI, the target does not run and the build is untouched. <Target Name=\"ExplainJauntyQueries\" AfterTargets=\"Build\" Condition=\"'$(JAUNTY_EXPLAIN_CONN)' != ''\"> <!-- No fail-on switch: report-only, never fails the build. --> <Exec Command=\"jauntyq explain --provider postgres --connection-env JAUNTY_EXPLAIN_CONN --queries db/tables\" /> <\/Target> Even if the variable is set and the database is down, the command exits 0 (clean skip), the build cannot break. What it will not do Run inside the Roslyn generator, ever (the generator has no connection code). Execute or mutate anything (EXPLAIN ANALYZE is deliberately unsupported). Explain write statements (skipped with a note). Prescribe DDL, it reports the problem and the hint, not a migration. See the CLI reference for the full option table and exit codes."},{"u":"03-guides-licensing-and-activation.html","t":"Licensing and activation","s":"Guides","h":[{"i":"the-code-jauntyq-generates-is-yours","t":"The code JauntyQ generates is yours"},{"i":"the-license-file","t":"The license file"},{"i":"activate","t":"Activate"},{"i":"inspect","t":"Inspect"},{"i":"using-premium-features","t":"Using premium features"},{"i":"preview-licenses-pre-10","t":"Preview licenses (pre-1.0)"},{"i":"lapse","t":"Lapse"},{"i":"ci","t":"CI"},{"i":"seats","t":"Seats"},{"i":"how-licenses-are-issued","t":"How licenses are issued"}],"b":"Licensing and activation JauntyQ's core is never gated. The source generator, every JNTxxxx build diagnostic (including the deep JNT8xxx query analysis), and the runtime always work, with or without a license, valid or lapsed. That is the perpetual-use grant in the EULA, and it is enforced structurally: no core assembly can even reference the licensing code, so generated output and diagnostics are identical regardless of license state. What is gated are the premium (\"moat\") CLI features. Today that means: Command Entitlement (feature key) jauntyq migrate impact migration-impact jauntyq schema verify contract-testing jauntyq schema verify --service <id> per-service-contracts (in addition to contract-testing) jauntyq schema verify --usage <path> rides the contract-testing gate (no extra key) jauntyq usage export contract-testing (gated 2026-08-01, it feeds schema verify --usage) jauntyq registry resolve\\|dependents\\|validate\\|list contract-testing (gated 2026-08-01, same family) jauntyq explain live-explain That is five commands over four feature keys, enforced inside Extrode.JauntyQ.Cli.Premium; schema verify --service is a second check on top of schema verify. Nothing outside Extrode.JauntyQ.Cli.Premium consults the gate. The free/paid split across the whole product is described on the pricing page. deep-query-analysis is the one remaining reserved key, defined for a future, not-yet-built feature. It does not gate the JNT8006/JNT8007 deeper-query-analysis diagnostics already shipped in the generator (spec 004), those are core, build-time diagnostics like every other JNTxxxx check, and the core generator is never gated (see below). The embeddable Extrode.JauntyQ.Schema.Contract assertion library (used from your own test suite) is not gated, only the schema verify CLI entry point is. This is entitlement gating for a source-viewable product, not DRM. A license is a signed, tamper-evident record of what you are entitled to, verified offline. It optimizes for a clean legitimate path (activate once, works offline forever within your entitlement), not for defeating a determined bypasser. The code JauntyQ generates is yours The question this page gets asked first, so it is answered before the mechanics. Both licenses define a Derivative Work as anything that incorporates the Software, which, read without qualification, would make your application a forbidden derivative the moment you compiled generated code into it. The Output Exception exists to stop that reading, and has been in force since 2026-08-17. Under it you may modify, compile, distribute and sell generated code, schema snapshot files and reports as part of your own applications, with no obligation to disclose source and no obligation to reproduce the license text; you may also redistribute Extrode.JauntyQ.Runtime in unmodified object form. Your end users do not become licensees of JauntyQ. Two limits are deliberate. The ethical use restrictions (Sections 4 and 5) continue to bind what you build with the output, this is not an unrestricted compiler exception, and that is the point of it. And the Exception grants nothing over the Software itself: the generator and CLI stay unmodifiable and non-redistributable. The Exception ships inside every package alongside the license, and applies under both ISL-R and the ISL-EULA, so it does not matter which half emitted the artifact. The license file A license is a signed jaunty.license.json issued to you out of band. It carries a payload (licensee, tier, entitlements, seats, issue/term dates, product id, format version) and a detached RSA-2048/PSS signature over the payload's canonical bytes. The tool verifies it offline against a public key embedded in the build; no network calls are ever made for licensing. Activate jauntyq activate --license path/to/jaunty.license.json The tool verifies the signature against the embedded public key, checks the product and format version, and installs the license to a per-user location: OS Path Windows %APPDATA%\\JauntyQ\\jaunty.license.json Linux ~/.config/JauntyQ/jaunty.license.json macOS ~/Library/Application Support/JauntyQ/jaunty.license.json If verification fails, nothing is installed and the error tells you why: a bad signature, malformed JSON, a license for a different product, or a license format newer than your JauntyQ build understands (upgrade JauntyQ). Activation never checks the term/expiry, so a wrong system clock can never brick activation, expiry only ever matters at use time (see Lapse). Inspect jauntyq license status Prints the tier, licensee, seat count, term dates, entitlements, and whether the license has lapsed, fully offline. A preview license reports its release scope in place of the term. jauntyq license deactivate removes the installed license (e.g. when moving to another machine). Using premium features With an entitling license installed, the five commands in the table above run normally. Without one (or with a license that does not grant that feature), they print an entitlement-required message and exit with the reserved code 3, distinct from 1 (usage/error) and 2 (drift/findings) so CI can tell \"not licensed\" apart from \"found problems.\" They do no premium work when not entitled. Preview licenses (pre-1.0) While JauntyQ is pre-1.0 the premium commands are free, and that is implemented by minting free licenses rather than by ungating anything: a preview license is an ordinary signed license with tier: \"Preview\", minted perpetual, granting the paid entitlements. The gate is unchanged, so an unlicensed caller still exits 3. A preview license is scoped to the releases the preview covers. Every 0.x build honors it forever; a 1.0 or later build classifies it NotEntitled with its own message and an upgrade path, not the generic entitlement-required wording: your free preview license covers pre-1.0 releases only This is a tier check, not an expiry check, and it is ordered ahead of the expiry test. Expiry could not do the job: a lapse runs with a warning by design and never becomes NotEntitled, so a short-dated preview license would keep working on every release JauntyQ ever ships. jauntyq license status reports the scope rather than printing \"perpetual (no expiry)\", which would be true of the date and misleading about what it covers. Nothing is revoked and no existing build changes behavior, a 0.x binary keeps accepting the license after 1.0 ships. Lapse A term can end (the payload may carry an expiresUtc; a perpetual license has none). After expiry, features you were already entitled to keep working and print a non-blocking warning: warning: your license has lapsed; migration impact analysis still works, but renew to keep receiving updates. A lapse never turns a granted feature into \"not entitled\" and never produces exit 3. This matches the pricing term that a lapsed customer keeps perpetual use of the versions they already have. CI CI rarely runs an interactive activate. Point JAUNTYQ_LICENSE at a license file instead, the same offline verification applies: export JAUNTYQ_LICENSE=\"$CI_SECRETS/jaunty.license.json\" jauntyq schema verify --provider sqlserver --connection-env CI_DB_CONN \\ --output db/schema/jaunty.schema.json Resolution order for reads is JAUNTYQ_LICENSE → the per-user install → none. Seats Seat count is recorded as informational metadata. JauntyQ does not attempt machine-count enforcement, that is unverifiable offline without a phone-home, which is deliberately out of scope. How licenses are issued The private signing key is never in this repository or in any shipped binary; only the public key is embedded, for verification. Extrode mints licenses out of band with the production key and sends you the signed file. No build phones home to check it."},{"u":"03-guides-migrating-from-dapper-and-ef.html","t":"Migrating from Dapper or EF Core","s":"Guides","h":[{"i":"mental-model-shift","t":"Mental model shift"},{"i":"from-dapper","t":"From Dapper"},{"i":"from-ef-core","t":"From EF Core"},{"i":"suggested-order-of-migration","t":"Suggested order of migration"},{"i":"what-does-not-come-along","t":"What does not come along"}],"b":"Migrating from Dapper or EF Core JauntyQ occupies the space between Dapper (hand-written SQL, hand-written mapping) and EF Core (LINQ, change tracking, runtime SQL generation). It keeps the SQL-first control of Dapper but generates the mapping, and validates the SQL, at compile time. This guide covers what changes when you move an existing data layer over. Mental model shift Dapper EF Core JauntyQ Where SQL lives inline strings generated from LINQ .sql files Mapping reflection at runtime reflection/expression trees generated at build time Schema errors surface at runtime at runtime (mostly) at build time Change tracking none yes none Runtime cost reflection per query high (tracking, translation) none (typed ordinal reads) The core move is the same in both cases: SQL (or LINQ) that lived in C# moves into .sql files that the generator turns into typed methods. From Dapper Dapper is the closer fit, you already write SQL and think in terms of parameters and result shapes. Before (Dapper): var product = conn.QuerySingleOrDefault<Product>( \"SELECT ProductId, ProductName, UnitPrice FROM Products WHERE ProductId = @id\", new { id = 42 }); After (JauntyQ), move the SQL into db/tables/Products/GetById.sql: -- @first SELECT ProductId, ProductName, UnitPrice FROM Products WHERE ProductId = @ProductId var db = new JauntyDb(conn); Product? product = db.Products.GetById(42); Mapping to Product, parameter typing, and the null-when-missing shape all come from the generator. The SQL is validated against the snapshot at build time, so a typo in a column name breaks the build instead of throwing at runtime. Translation notes: QuerySingleOrDefault / QueryFirstOrDefault → -- @first (returns Row?). Query (many rows) → default (returns List<Row>); use -- @stream for large sets. Execute (no result) → -- @result void (returns row count). IN (@ids) list expansion → -- @each. Anonymous-parameter objects → named parameters (@ProductId) become typed method arguments; declare any the parser cannot infer with -- @params. Custom projections (joins, partial selects) → a -- @result (...) inline shape or a named DTO. Simple CRUD you were writing by hand → delete it; auto-CRUD synthesises GetAll/GetById/Insert/Update/Delete/Upsert/BulkInsert. From EF Core The move is larger because you give up LINQ-to-SQL translation and change tracking, and gain compile-time validation and zero runtime overhead. LINQ queries → .sql files. Each context.Products.Where(...) becomes a .sql file. This is the bulk of the work, but it is mechanical, and the result is SQL you can read and tune. DbContext → JauntyDb. JauntyDb is a stateless facade over a DbConnection, no OnModelCreating, no migrations engine, no tracking. new one per request. Change tracking → explicit writes. There is no SaveChanges(). Update(poco) is a full-row update (last-writer-wins); for concurrency use a SQL Server rowversion column, which auto-CRUD turns into an optimistic token. See the API overview. EF migrations → snapshot + db/migrations/. JauntyQ does not run migrations against the database; it simulates pending DDL in db/migrations/ on top of the snapshot so the build validates against the post-migration schema. Applying migrations to the database stays in your deploy pipeline. Navigation properties / lazy loading → explicit FK loaders. A foreign key in the snapshot generates db.Orders.GetByCustomerId(...); you load related data with an explicit call, not a traversal. Entity graphs / includes → joins in SQL. Write the join yourself and shape the result with -- @result. Suggested order of migration Pull a snapshot with jauntyq schema pull and commit it. Wire the generator into the project (see getting started). Delete hand-written CRUD you can get from auto-CRUD; keep only custom queries. Port custom queries into .sql files one entity folder at a time; let the build tell you (via JNTxxxx) where SQL and schema disagree. Replace DbContext/Dapper call sites with JauntyDb calls. Add jauntyq schema verify to CI to catch future drift. What does not come along JauntyQ deliberately omits change tracking, lazy loading, a LINQ provider, and runtime model configuration. If you depend heavily on those, read Why JauntyQ (and why not) before committing to the move."},{"u":"03-guides-schema-registry.html","t":"Central schema-dependency registry","s":"Guides","h":[{"i":"the-registry-file","t":"The registry file"},{"i":"schemas-the-named-snapshots","t":"schemas, the named snapshots"},{"i":"services-the-dependents","t":"services, the dependents"},{"i":"cli-verbs","t":"CLI verbs"},{"i":"workflow-owner-declares-consumers-declare-ci-validates","t":"Workflow: owner declares, consumers declare, CI validates"},{"i":"programmatic-use","t":"Programmatic use"},{"i":"non-goals-current","t":"Non-goals (current)"}],"b":"Central schema-dependency registry Real systems are not one-service-one-schema: several services read the same database, one team owns the schema, and others consume slices of it. The central schema-dependency registry, a checked-in jaunty.registry.json, names the canonical schema snapshots and records which services depend on which schema (and, optionally, which tables). With it, tooling can resolve a snapshot by id, enumerate a schema's or a table's dependents, and keep the declared graph honest in CI. The registry is tooling metadata, opt-in, and additive: a project without one behaves exactly as today, the generator is unchanged, and nothing about it exists at runtime. The registry file jaunty.registry.json lives at the root the platform team chooses (typically the repo root of the schema-owning project, or a shared infra repo). It has two lists: { \"schemas\": [ { \"id\": \"sales\", \"snapshot\": \"schemas/sales.schema.json\", \"dialect\": \"postgres\", \"owner\": \"platform-team\" } ], \"services\": [ { \"id\": \"billing\", \"dependsOn\": [\"sales\"], \"consumes\": [\"sales.orders\", \"sales.invoices\"], \"owner\": \"billing-team\" }, { \"id\": \"reporting\", \"dependsOn\": [\"sales\"] } ] } schemas, the named snapshots Field Required Meaning id Yes Unique schema id, referenced by services' dependsOn. snapshot Yes Snapshot path, relative to the registry file. Rooted paths and .. escapes are rejected (same rules as the CLI --output). dialect Yes postgres, sqlserver, mysql, or sqlite. owner No Owning team/contact. services, the dependents Field Required Meaning id Yes Unique service id. dependsOn Yes Ids of the schemas the service depends on. consumes No Declared table usage: \"schemaId.table\", or a bare \"table\" when the service depends on exactly one schema. When omitted, dependents-by-table cannot narrow for this service, it appears under schema-level dependents only. owner, description No Team/contact and free-form notes. CLI verbs All verbs are offline and read-only. The registry defaults to ./jaunty.registry.json; --registry <path> overrides. All verbs accept --format text|json (default text). # Resolve a snapshot by name instead of copying the file: jauntyq registry resolve --schema sales # schema: sales # snapshot: /abs/path/to/schemas/sales.schema.json # dialect: postgres # owner: platform-team # Who depends on the schema at all? jauntyq registry dependents --schema sales # Who consumes a specific table? (the blast-radius question) jauntyq registry dependents --table sales.orders # Keep the registry honest in CI: jauntyq registry validate # Summarize everything: jauntyq registry list Exit codes: 0 ok; 1 usage or error (missing registry, unknown schema id, bad --table reference); 2, validate found problems (each problem is individually named, e.g. DanglingDependsOn: service 'billing' depends on undeclared schema 'inventory'.). validate checks: unique schema/service ids; every snapshot path is relative-within-tree and the file exists and parses; every dependsOn points at a declared schema; every consumes entry resolves to a schema the service depends on and to a table that exists in that schema's snapshot. Workflow: owner declares, consumers declare, CI validates The owning team pulls the canonical snapshot (jauntyq schema pull), commits it, and declares it in the registry under a stable id. Each consuming team adds its service with dependsOn (and ideally consumes, so table-level blast radius works for it). CI runs jauntyq registry validate on every change to the registry or a snapshot, so a dangling reference or a consumed table that no longer exists fails the build. Before a schema change, the owner runs jauntyq registry dependents --table <schemaId.table> to see exactly who is affected, and --schema <id> for the full dependent list. A build or script can consume resolve instead of copying snapshots around: SNAPSHOT=$(jauntyq registry resolve --schema sales --format json | jq -r .snapshot) Programmatic use The registry lives in the non-core Extrode.JauntyQ.Registry library (references only Extrode.JauntyQ.Schema): RegistryLoader.LoadFile(path) returns a SchemaRegistry with ResolveSchema(id), DependentsOfSchema(id), and DependentsOfTable(schemaId, table); RegistryValidator.Validate(registry) returns every problem, named. Per-service contracts (feature 010) build on this surface. Non-goals (current) Multi-schema code generation, the registry only locates a snapshot; codegen stays single-schema. The <JauntyQSchemaId>/<JauntyQRegistry> build-property hook is deferred with it. A hosted/served registry, it is a checked-in JSON file, not a service. Runtime use, build/tooling metadata only; nothing ships in your app. Auto-derived consumes, declare table usage explicitly for now; deriving it from a service's query corpus is a planned follow-up. Cross-service migrate impact --registry, a follow-up built on dependents enumeration. Federation, one registry file per invocation."},{"u":"03-guides-sequences.html","t":"Sequences","s":"Guides","h":[{"i":"how-it-works","t":"How it works"},{"i":"transactions-and-connection-lifecycle","t":"Transactions and connection lifecycle"},{"i":"when-it-appears","t":"When it appears"}],"b":"Sequences For databases with true sequence objects, JauntyQ generates typed accessors that advance a sequence and return the next value. Supported on SQL Server, PostgreSQL, and MariaDB (via its CREATE SEQUENCE support since 10.3). Real/Oracle MySQL and SQLite have no sequence object, so their snapshots carry no sequences and no accessors are generated. MariaDB shares the mysql dialect string with real MySQL, but is detected separately by the extractor and generator so its sequences are still picked up. How it works jauntyq schema pull captures every sequence object into the schema snapshot (SQL Server via sys.sequences, PostgreSQL via information_schema.sequences joined to pg_sequences, MariaDB via its information_schema.sequences-style catalog), recording its start value, increment, min/max, and current value. When the snapshot has sequences, the generator emits a lazily-constructed db.Sequences accessor with one method per sequence, named Next{Name}() (plus an async twin). The name is PascalCased from the sequence name. var db = new JauntyDb(conn); long id = db.Sequences.NextOrderNumber(); long id2 = await db.Sequences.NextOrderNumberAsync(cancellationToken); Each method returns long (the newly advanced value) and runs the dialect-native statement: Dialect Emitted SQL SQL Server SELECT NEXT VALUE FOR {name} PostgreSQL SELECT nextval('{name}') MariaDB SELECT NEXTVAL({name}) Transactions and connection lifecycle db.Sequences.Next* follows the same rules as every other db.* call: It enlists in the ambient JauntyDb transaction if one is active (via BeginTransaction()), so a sequence value drawn inside a transaction is covered by it. It opens the connection if closed and closes it again afterward; if the connection is already open, it is left open. Note that in most databases sequence advancement is not rolled back with a transaction: rolling back after drawing a value typically leaves a gap in the sequence. That is standard sequence behavior, not a JauntyQ choice. When it appears The db.Sequences accessor is only generated when the snapshot actually contains sequences. Add a sequence to the database, re-run jauntyq schema pull, and rebuild to pick it up."},{"u":"03-guides-startup-verification.html","t":"Runtime startup schema verification","s":"Guides","h":[{"i":"basic-usage","t":"Basic usage"},{"i":"modes","t":"Modes"},{"i":"the-warn-sink","t":"The Warn sink"},{"i":"locating-the-compiled-snapshot","t":"Locating the compiled snapshot"},{"i":"embedding-the-snapshot-at-build-time","t":"Embedding the snapshot at build time"},{"i":"failures-are-honest-and-distinct","t":"Failures are honest and distinct"},{"i":"see-also","t":"See also"}],"b":"Runtime startup schema verification Contract testing catches drift in CI, but CI verifies the database CI points at, not necessarily the one a given deployment connects to at runtime. A service can pass CI and still boot against a database that drifted: a hand-run migration, a wrong connection string, a replica lagging a DDL change. Startup verification closes that last gap. At boot, after the app knows its connection string and before it serves traffic, StartupSchemaGuard reflects the live database and compares it to the compiled snapshot with the same classifier as contract testing. On breaking drift it fails fast (or warns, per policy), in the deploy that has the problem, instead of on the first unlucky query. The guard lives in Extrode.JauntyQ.Schema.Contract (the non-core package that already carries the ADO.NET providers and the comparer). The zero-dependency Extrode.JauntyQ.Runtime is untouched: apps that do not opt in keep a pristine core. Basic usage Call the guard once, from the startup path (Program.cs / Startup): using Extrode.JauntyQ.Schema.Contract; // Program.cs, after configuration, before serving traffic. await StartupSchemaGuard.VerifyAsync( connectionString, ContractDialect.SqlServer, SnapshotSource.FromFile(\"db/schema/jaunty.schema.json\"), StartupVerificationMode.Throw); If the live database has breaking drift from the snapshot, this throws a StartupSchemaException naming every breaking object, and the app fails to start. With no breaking drift it returns the comparison report and startup proceeds. A synchronous StartupSchemaGuard.Verify(...) exists for plain Main paths. Run once, at boot. Every call reflects the live schema over the network. Do not call the guard per request, per connection, or on a hot path. Modes The mode is a StartupVerificationMode, chosen by the host, typically from configuration or an environment variable, never hard-coded on: Mode Behavior Use for Throw Breaking drift throws StartupSchemaException; app fails to start Canaries, staging, fail-fast production Warn Drift is written to the sink; startup proceeds Observability without blocking Off Returns immediately, no snapshot load, no database I/O Environments that must not reflect at boot var mode = Environment.GetEnvironmentVariable(\"SCHEMA_GUARD\") switch { \"warn\" => StartupVerificationMode.Warn, \"off\" => StartupVerificationMode.Off, _ => StartupVerificationMode.Throw, }; In Throw mode, compatible-only drift (an added table, a widened column) never fails startup. Pass strict: true to reject any drift at all, the boot-time mirror of AssertNoDrift(). The Warn sink Warn takes a plain Action<string>, deliberately not a logging framework, so this package adds no logging dependency. Adapt it to whatever you use: await StartupSchemaGuard.VerifyAsync( connectionString, ContractDialect.Postgres, SnapshotSource.FromFile(\"db/schema/jaunty.schema.json\"), StartupVerificationMode.Warn, warnSink: msg => logger.LogWarning(\"{SchemaDrift}\", msg)); When no sink is supplied, Warn writes to stderr, it is never silent. Locating the compiled snapshot The guard is only meaningful against the exact snapshot the app was compiled with. SnapshotSource offers three ways to supply it: SnapshotSource.FromFile(\"db/schema/jaunty.schema.json\"); // shipped alongside the app SnapshotSource.FromEmbeddedResource(typeof(Program).Assembly, \"jaunty.schema.json\"); // single-file / AOT friendly SnapshotSource.FromSchema(databaseSchema); // an object you already hold FromEmbeddedResource accepts the full manifest name or a unique suffix (resource names are prefixed with the root namespace and folder path). Embedding the snapshot at build time To make a self-contained binary carry its own contract, opt in from the project that references Extrode.JauntyQ.Schema.Contract: <PropertyGroup> <JauntyQEmbedSnapshot>true<\/JauntyQEmbedSnapshot> <\/PropertyGroup> The package's build targets then embed the snapshot from the conventional locations (schema/*.schema.json or db/schema/*.schema.json), or from an explicit <JauntyQSnapshotPath>path/to/jaunty.schema.json<\/JauntyQSnapshotPath>, as an assembly resource. Projects that do not set the property are completely unaffected. Failures are honest and distinct A verification that could not run is never reported as passing: Failure Exception Snapshot missing / unreadable / malformed SnapshotSourceException Database unreachable / reflection failed SchemaContractConnectionException Drift rejected by policy (Throw mode) StartupSchemaException (carries the full Report) Off mode is the only path that skips verification, explicitly, by policy, before any I/O. See also Database contract testing, the same classifier at test time, gating CI on drift. CLI reference, schema verify, the classifier for pipelines without a test host. Schema snapshot format, the contract file itself."},{"u":"06-reference-readme.html","t":"Reference","s":"Reference","h":[],"b":"Reference Precise, lookup-oriented documentation for JauntyQ. CLI reference, the jauntyq schema pull / verify commands, options, providers, and exit codes. Schema snapshot format, the on-disk .schema.json structure. Configuration reference, MSBuild properties, AdditionalFiles globs, and folder conventions. Directives reference, every -- @ directive with syntax and constraints. Dialect differences, per-dialect emitted SQL for identity, upsert, sequences, and bulk insert. Cross-dialect compatibility, where the same query means the same thing on all five engines, and the measured exceptions. Supported SQL surface, which query shapes the parser accepts, which it refuses, and the diagnostic for each refusal. Diagnostics, every JNTxxxx code. Troubleshooting & FAQ, symptoms mapped to causes. Versioning & support policy, version scheme, snapshot compatibility, framework support, and support channels. API overview, the shape of the generated surface."},{"u":"06-reference-api-overview.html","t":"API reference overview","s":"Reference","h":[{"i":"shape-of-the-generated-surface","t":"Shape of the generated surface"},{"i":"where-to-look-for-exact-signatures","t":"Where to look for exact signatures"}],"b":"API reference overview JauntyQ generates code at compile time; there is no runtime API surface to browse beyond what your own schema produces. This page orients you to the shape of that generated surface. Every generated member carries an XML doc comment (see CodeEmitter.cs and its Part*.cs siblings in src/Extrode.JauntyQ.Generator/ for the exact templates), those doc comments are the authoritative, always-in-sync reference; this page is a map, not a substitute. Full XML-doc-driven API site generation (e.g. DocFX) is not set up, that is a separate, larger infrastructure task (a docs pipeline, hosting, CI wiring) outside the scope of this reference page. See docs/07-roadmap/roadmap.md. Shape of the generated surface JauntyDb, the root object, constructed from a DbConnection. Exposes one lazily-constructed property per table/view entity (db.Products, db.Orders, ...) and, when the schema has sequences, db.Sequences. BeginTransaction()/BeginTransactionAsync() return a Transaction that every subsequent call on db automatically enlists in until commit/rollback/dispose. Per-entity accessor (db.Products), one method per query: hand-written .sql files under that entity's folder, plus auto-CRUD synthetics (GetAll, GetById, GetBy<ForeignKey>, Insert, Update, Delete, Upsert, BulkInsert) unless a hand-written file of the same name already exists (hand-written always wins) or <JauntyQAutoCrud>false<\/JauntyQAutoCrud> disables synthesis project-wide. Every method has a sync and an async twin, and both an instance form (db.Products.GetById(1)) and a static form taking an explicit DbConnection for callers that don't use JauntyDb. Static forms also take an optional trailing DbTransaction? transaction so they can enlist in a caller-managed transaction (instance forms flow the ambient JauntyDb.CurrentTransaction automatically); on BulkInsert a caller-supplied transaction suppresses the method's own commit, the caller owns the unit of work. Row POCOs (Product, or <Entity>Row when singularizing the entity name collides with the accessor class name, e.g. MailQueueRow next to JauntyDb.MailQueue), plain data-carrying classes, one property per column, a required modifier on non-nullable reference-typed properties, and a static Read(DbDataReader) used by every full-row query. Insert/ Update/Delete/Upsert also get POCO-taking overloads (db.Products.Insert(productRow)) that forward to the scalar form; BulkInsert is inherently row-native (IEnumerable<Row>) and has no separate scalar form to forward to. Directives (SQL-file comment pragmas: -- @first, -- @stream, -- @identity, -- @result, -- @each, -- @params, -- @type, -- @proc, -- @call), control codegen shape per query file. See src/Extrode.JauntyQ.Generator/Directives/ for the full parser. Diagnostics, every JNTxxxx code the generator can report, with meaning and fix guidance: see docs/06-reference/diagnostics.md. Where to look for exact signatures Generated code is always visible: build once and inspect the emitted *.g.cs files (in obj/Debug/<tfm>/generated/Extrode.JauntyQ.Generator/... under your project), or read the emission logic directly in src/Extrode.JauntyQ.Generator/CodeEmitter*.cs (each Emit* method's XML doc describes exactly what it produces and why)."},{"u":"06-reference-cli.html","t":"CLI reference: jauntyq","s":"Reference","h":[{"i":"two-packages-one-command","t":"Two packages, one command"},{"i":"synopsis","t":"Synopsis"},{"i":"commands","t":"Commands"},{"i":"schema-pull","t":"schema pull"},{"i":"schema-verify","t":"schema verify"},{"i":"usage-export","t":"usage export"},{"i":"migrate-impact","t":"migrate impact"},{"i":"explain","t":"explain"},{"i":"registry-resolve--dependents--validate--list","t":"registry resolve / dependents / validate / list"},{"i":"activate","t":"activate"},{"i":"license-status--license-deactivate","t":"license status / license deactivate"},{"i":"license-and-entitlements","t":"License and entitlements"},{"i":"options","t":"Options"},{"i":"--output-constraints","t":"--output constraints"},{"i":"providers","t":"Providers"},{"i":"environment-variables","t":"Environment variables"},{"i":"exit-codes","t":"Exit codes"},{"i":"examples","t":"Examples"}],"b":"CLI reference: jauntyq The jauntyq command-line tool extracts a database schema into a committed .schema.json snapshot (schema pull), checks a snapshot against a live database (schema verify), reports the blast radius of a pending migration set offline (migrate impact), and runs the query corpus through the database's estimate-only EXPLAIN (explain). The generator itself never connects to a database; only schema pull/schema verify/explain do, migrate impact and registry are fully offline. Two packages, one command Package Feed Verbs Install Extrode.JauntyQ.Cli NuGet.org schema pull dotnet tool install --global Extrode.JauntyQ.Cli Extrode.JauntyQ.Cli.Premium a private feed for subscribers everything in Extrode.JauntyQ.Cli plus schema verify, usage export, migrate impact, explain, registry, activate, license dotnet tool install --global Extrode.JauntyQ.Cli.Premium --add-source <feed> Both install the same command, jauntyq. The premium package replaces the free one; never install both. schema pull is implemented once, in the shared Extrode.JauntyQ.Cli.Core library, so it behaves identically in either. A premium verb typed at the free tool exits 3 and prints the install line; it never reads its inputs. In this repository the free tool runs via dotnet run: dotnet run --project src/Extrode.JauntyQ.Cli -f net8.0 -- schema pull --provider sqlserver ... The premium verbs come from an installed Extrode.JauntyQ.Cli.Premium, which is built elsewhere; there is no project here to run them from. The examples below use jauntyq. Synopsis jauntyq schema pull --provider <provider> (--connection-env <VAR> | --connection <connstr>) [--output <path>] jauntyq schema verify --provider <provider> (--connection-env <VAR> | --connection <connstr>) [--output <snapshot-path>] [--format text|json] [--fail-on any|breaking] [--service <id> [--registry <path>]] [--usage <path>] jauntyq usage export [--queries <dir>] [--snapshot <path>] [--output <file>] [--no-auto-crud] jauntyq migrate impact [--snapshot <path>] [--migrations <dir>] [--queries <dir>] [--format text|json] [--fail-on breaking|risky] [--fail-on-warnings] jauntyq explain --provider <provider> [--connection-env <VAR> | --connection <connstr>] [--queries <dir>] [--max <n>] [--format text|json] [--fail-on high|informational] [--cache-dir <dir>] jauntyq registry resolve --schema <id> [--registry <path>] [--format text|json] jauntyq registry dependents (--schema <id> | --table <schemaId.table>) [--registry <path>] [--format text|json] jauntyq registry validate [--registry <path>] [--format text|json] jauntyq registry list [--registry <path>] [--format text|json] jauntyq activate --license <path> jauntyq license status jauntyq license deactivate Top-level commands are schema (subcommands pull, verify), usage (subcommand export), migrate (subcommand impact), explain, registry (subcommands resolve, dependents, validate, list), activate, and license (subcommands status, deactivate). Premium gating. migrate impact, schema verify, explain, registry (every subcommand) and usage export are premium features: they require an entitling license and otherwise exit 3 without doing the work; schema verify --service requires a further, separate entitlement on top of base schema verify (see License and entitlements). schema pull, activate, license, and the core source generator are never gated. Being offline and being ungated are independent: migrate impact, registry and usage export need no database connection and are still gated. Commands schema pull Connects to the database, extracts tables, columns, foreign keys, stored procedures, and sequences, and writes the snapshot to --output. Creates the output directory if it does not exist. On success prints the table and foreign-key counts and Schema written to <output>. schema verify Loads the snapshot at --output, re-extracts the live schema, and compares them. Use it in CI or locally to catch drift between the committed snapshot and the real database. Each difference is classified breaking or compatible by a fixed structural rule set (independent of which queries reference the object): Change Severity Table or column missing from the live database breaking Column reorder / rename at a position (readers bind by ordinal) breaking Type change, length narrowing, precision/scale change breaking null → not null, primary-key / identity / row-version / unicode change breaking Column added, length widening, not null → null compatible Table present live but absent from the snapshot informational (never fails) Options Option Default Meaning --format text\\|json text Human-readable report or the machine-readable JSON report ({ \"drifts\": [ { \"kind\", \"objectPath\", \"severity\", \"message\", ... } ], \"hasBreaking\" }), with severities as strings. A scoped run (--service) adds a \"scope\" block: { \"serviceId\", \"isEmpty\", \"comparedTables\": [ { \"name\", \"ownership\" } ] }. --fail-on any\\|breaking any any (default) exits 2 on any difference, preserving the prior behavior, while a brand-new table stays informational. breaking exits 2 only when breaking drift is present, letting compatible drift pass. --service <id> , Per-service scoping. Verify only the slice the named service declares in the registry (its consumes tables): drift inside the slice is classified by the same rules; changes outside it produce no drift of any severity and cannot fail this service's gate. The report names the service and the compared tables (no silent scope). Requires the separate per-service-contracts entitlement in addition to base contract-testing (exit 3 if missing, before any comparison runs). --registry <path> ./jaunty.registry.json The registry to derive the --service scope from. Only meaningful with --service. --usage <path> , Usage-aware severity (opt-in, downgrade-only). Reclassifies the structural report against the referenced-objects manifest written by usage export: breaking drift on an object no generated query references becomes informational, still listed, annotated with the reason and its original structural severity, but never gate-failing (neither under breaking nor under the strict any default, like a brand-new table). Referenced objects always keep their structural severity; any uncertainty (unqualified references, unresolved queries, a referenced column on a dropped/reordered table) keeps the structural verdict. Composes with --service: scope filters what is compared, usage adjusts the severity of the remainder. The JSON report adds a \"usage\" block ({ \"manifestSnapshotHash\", \"verifiedSnapshotHash\", \"stale\", \"downgradedCount\", \"unresolvedQueryCount\" }) and per-drift \"downgraded\"/\"downgradeReason\"/\"structuralSeverity\" fields. If the manifest was built against a different snapshot, a staleness warning is printed on stderr (exit code unaffected, downgrade-only semantics cannot become unsafe). A missing/unreadable manifest exits 1, never a silent structural fallback. Exit 0, snapshot matches, or only differences below the --fail-on threshold (e.g. compatible drift under --fail-on breaking). With --service, this includes any change outside the service's slice, and the vacuous case where the service declares no consumes (printed as an explicit \"no scope declared\" note, never as a verified pass). Exit 2, a difference at or above the threshold; each is listed on stderr with its severity. With --service, only in-slice differences count. Exit 1, with --service: a scope/registry error (registry unreadable, unknown service id, or a scope table the snapshot does not declare), deliberately distinct from both a pass and drift. For a test-embeddable equivalent, asserting a live database matches the snapshot from inside your own xUnit/NUnit/MSTest suite, use the Extrode.JauntyQ.Schema.Contract package, which shares the exact same classifier. The same package also offers boot-time verification (StartupSchemaGuard) so an app can fail fast at startup when the database it actually connects to has drifted. usage export Writes the usage manifest (jaunty.usage.json) consumed by schema verify --usage and SchemaContract.WithUsage(...): the union of every table and table.column the project's generated queries reference, hand-written .sql files plus the auto-CRUD synthetics the generator emits, resolved by the same offline analysis that powers migrate impact. No database connection. Gated on contract-testing, the same entitlement as the verb it feeds, schema verify --usage, since a manifest is useless without it. The manifest also records the hash of the snapshot it was built against, so a verification against a different snapshot can warn that usage may be stale. Commit the manifest and regenerate it in CI whenever queries or the snapshot change. Uncertainty is encoded conservatively (downgrades must never be unsafe): an unqualified column reference in a multi-table query marks every in-scope table's columns potentially referenced (table.*); a query that is invalid against the snapshot or uses unsupported constructs marks all its tables table.*; a query that cannot be parsed/attributed at all is recorded under unresolvedQueries, which disables all downgrades until it is fixed (the export prints a warning listing each one). Options Option Default Meaning --queries <dir> , Directory of hand-written .sql query files (searched recursively). Omit to cover only auto-CRUD synthetics. --snapshot <path> schema/jaunty.schema.json Committed snapshot: seeds the auto-CRUD synthetics and the staleness hash. --output <file> jaunty.usage.json Where to write the manifest. --no-auto-crud , Exclude the auto-CRUD synthetics, only for projects that set <JauntyQAutoCrud>false<\/JauntyQAutoCrud> (otherwise the generated CRUD really does reference every table). Exit 0, manifest written. Exit 1, argument or I/O error (e.g. snapshot not found). Exit 3, not entitled (contract-testing), checked before any work. Manifest shape: { \"snapshotHash\": \"sha256:9f2c…\", \"tables\": [\"customers\", \"orders\"], \"columns\": [\"customers.*\", \"orders.order_id\", \"orders.total\"], \"unresolvedQueries\": [] } migrate impact Reports how a pending db/migrations/*.sql set affects your queries, fully offline (snapshot + migration files + query files; no database connection). Each hand-written query and each auto-CRUD synthetic is classified as exactly one of: SAFE, references no object the migration changed. RISKY, still compiles, but a referenced column changed type, nullability, length, or precision (or a referenced table was changed by a statement the simulator could not model). Reported at build time as the JNT9004 warning. BREAKING, the migration removes a referenced table or column, so the query no longer compiles. These already fail the build through the existing JNT2xxx validation errors; migrate impact names and groups them. A query already invalid against the baseline snapshot is not attributed to the migration (reported SAFE, no reasons). Options Option Default Meaning --snapshot <path> schema/jaunty.schema.json Committed baseline snapshot. --migrations <dir> db/migrations Directory of pending *.sql migrations (applied in filename order). --queries <dir> , Directory of hand-written .sql query files (searched recursively). Omit to classify only auto-CRUD synthetics. --format text\\|json text Human-readable report or the machine-readable JSON shape below. --fail-on breaking\\|risky , Exit non-zero when an entry at or above the threshold exists. Omit for report-only (always exit 0). --fail-on-warnings off Also exit 2 when the run produced any warning. A warning always means the analyzed corpus was narrower than the invocation asked for, a --migrations or --queries directory that does not exist, or a migration statement the simulator could not apply, so the report is a verdict over less than it appears to cover. Independent of --fail-on: the two gates are ORed, and classification is unaffected. Exit 0, report-only, or nothing reached the --fail-on threshold. Exit 2, an entry at or above --fail-on exists, or --fail-on-warnings is set and the run warned. Exit 1, usage or I/O error (e.g. snapshot not found). JSON shape (--format json): { \"baselineId\": \"schema/jaunty.schema.json\", \"migrationSet\": [\"0007_drop_legacy_col.sql\"], \"entries\": [ { \"queryFile\": \"Queries/GetUser.sql\", \"entityMethod\": \"User.GetById\", \"classification\": \"BREAKING\", \"reasons\": [ { \"schemaObject\": \"users.legacy_flag\", \"changeKind\": \"removed\", \"effect\": \"column referenced by the query no longer exists\" } ] } ] } MVP limitations JauntyQ has no rename tracking: a rename is a drop + an add, so queries on the old name are BREAKING and the new column is simply available. A committed JSON snapshot baseline is the supported path; DDL-as-schema-source projects are analyzed against the DDL-built base. Attribution is per-migration-set (cumulative effective schema), not per file. Index DDL (CREATE/DROP INDEX) is not modeled at all: MigrationParser classifies it Ignored, so it produces no diagnostic and no RISKY classification. An index drop that breaks a query's performance assumptions is currently invisible to both migrate impact and the build. Entity naming uses the query file's immediate folder (or Queries at the root); structural tables//views/ prefixes are not special-cased. explain Runs each .sql file under --queries through the target database's estimate-only EXPLAIN, parses the plan, and reports access paths, planner estimates, and severity-tagged plan problems per query file. Opt-in and on-demand only, the source generator never runs this and a normal build is unaffected. See the live EXPLAIN guide for the guardrails, heuristics, and optional MSBuild wiring. Estimate-only, never executes: Postgres EXPLAIN (FORMAT JSON), SQLite EXPLAIN QUERY PLAN, MySQL EXPLAIN FORMAT=JSON, SQL Server estimated SHOWPLAN_XML. Write statements are skipped with a note. Capped: at most --max live EXPLAIN calls per run; the remainder is listed as not analyzed (cap reached). Cached: .jaunty/explain-cache.json, keyed by dialect + normalized-SQL fingerprint + schema-snapshot fingerprint; cached queries do not consume the cap. Never breaks: unreachable database, auth failure, or an unexplainable query → per-query skip, exit 0. No connection string configured → clean no-op, exit 0. Options Option Default Meaning --provider <provider> required Database provider / dialect (same values as schema pull). --connection-env <VAR> / --connection , Connection string source (same resolution as schema pull). Unlike schema pull, no connection is not an error: the run is a clean exit-0 no-op. --queries <dir> db/tables Directory of .sql query files (searched recursively). --max <n> 10 Cap on live EXPLAIN calls per run. --format text\\|json text Human-readable report or the machine-readable JSON report ({ \"dialect\", \"cap\", \"results\": [ { \"queryFile\", \"status\", \"planSummary\", \"problems\": [...] } ], \"notes\" }). --fail-on high\\|informational , Exit 2 when a problem at or above the severity exists. info is accepted as an alias for informational. Omit for report-only (always exit 0). --cache-dir <dir> .jaunty Where explain-cache.json lives. Exit 0, report-only, nothing at/above --fail-on, or nothing analyzed (no/unreachable database). Exit 2, a problem at or above --fail-on exists. Exit 1, usage error (bad flag values, missing --provider). Exit 3, not entitled (live-explain). Severity heuristics (documented in the guide): sequential/full scan with est. rows ≥ 10,000 → high; cost-dominant sort (≥ 50% of total plan cost) → high; SQLite USE TEMP B-TREE → high; smaller scans/sorts and estimate-less full scans → informational. Costs are planner estimates from the connected database at run time; thresholds compare within a run only. registry resolve / dependents / validate / list Offline queries against a checked-in jaunty.registry.json, the central schema-dependency registry naming schema snapshots and the services that depend on them. Additive and opt-in: a project without a registry is completely unaffected. Fully offline, but gated on contract-testing, the registry is part of the contract-testing family, not a free verb. See the schema registry guide for the file format and workflow. Subcommand Meaning resolve --schema <id> Prints the schema's snapshot path (resolved relative to the registry file), dialect, and owner, resolve-by-name for builds/scripts. dependents --schema <id> Lists every service whose dependsOn names the schema. dependents --table <schemaId.table> Narrows to services whose consumes declares that table. A service that depends on the schema but declares no consumes list cannot be confirmed or ruled out at table granularity, so it is not counted as a dependent; it is listed separately under \"N further service(s) … declare no consumes list, so their use of this table is unknown\" (JSON: an unknownUsage array beside dependents). Both are omitted when there are none. validate Checks the whole graph: unique ids, snapshot exists within the registry tree, dependsOn resolvable, consumed tables present in the snapshot. Every problem is individually named. list Summarizes the registry's schemas and services. Options (all subcommands) Option Default Meaning --registry <path> ./jaunty.registry.json The registry file to query. --format text\\|json text Human-readable or machine-readable output. Exit 0, success (validate: no problems). Exit 1, usage or error: missing registry file, unknown schema id, or a malformed --table reference. Exit 2, validate found problems (each listed and named on stderr, e.g. DanglingDependsOn: service 'billing' depends on undeclared schema 'inventory'.). Exit 3, not entitled (contract-testing), checked before the registry is even read, so an unlicensed run cannot be told apart from a missing registry by anything but the exit code. Snapshot paths declared in the registry must be relative to the registry file and stay inside its directory tree, rooted paths and .. escapes are rejected at load, mirroring the --output constraints. activate Verifies a signed jaunty.license.json offline against the tool's embedded public key and installs it to the per-user location. Nothing is installed on failure; the error distinguishes a bad signature, malformed JSON, a wrong product, and an unrecognized license version. jauntyq activate --license path/to/jaunty.license.json Exit 0, verified and installed (prints tier, licensee, entitlements). Exit 1, missing/unreadable file, malformed JSON, bad signature, wrong product, or unknown license version. license status / license deactivate license status prints the active tier, licensee, seats, term dates, entitlements, and whether the license has lapsed, fully offline, no network. license deactivate removes the installed license. Both exit 0. License and entitlements migrate impact (entitlement migration-impact), schema verify (entitlement contract-testing; --service additionally requires per-service-contracts), usage export and every registry subcommand (both contract-testing, gated 2026-08-01 with the commercialization model, they predate it and were ungated by accident of shipping order), and explain (entitlement live-explain) each consult a single offline entitlement gate before doing premium work: Entitled, runs normally. Lapsed (term ended), still runs and prints a non-blocking warning; the perpetual-use grant means a lapse never blocks a feature you were entitled to. Not entitled (no license, or the license does not grant the feature), prints the entitlement-required message and exits 3, doing no premium work. The core source generator, JNT1xxx–JNT7xxx validation, and the runtime are never gated: generated output and diagnostics are identical with or without a license. This is entitlement gating for a source-viewable product, not DRM. In CI, set JAUNTYQ_LICENSE to a license file path instead of running activate. Feature keys: migration-impact, contract-testing, per-service-contracts, live-explain, and the reserved deep-query-analysis (defined for a future, not-yet-built feature, it does not gate the JNT8006/JNT8007 diagnostics already shipped in the generator, which are core and never gated). See the licensing and activation guide. Options These apply to schema pull and schema verify. Option Required Default Meaning --provider <provider> Yes , Database provider / dialect. See values below. --connection-env <VAR> See note , Name of an environment variable holding the connection string. Preferred. --connection <connstr> See note , Inline connection string. Avoid: visible in process lists, shell history, and CI logs. --output <path> No schema/jaunty.schema.json Snapshot path, relative to the current directory. For verify, the snapshot to compare against. Connection note: a connection string must come from exactly one source. Resolution order, most secure first: --connection-env <VAR>, read from the named environment variable. JAUNTYQ_CONNECTION, used when neither flag is given. --connection <connstr>, inline, least secure. If none is present the command errors and prints usage. --output constraints The path must be relative and must stay inside the project directory. A rooted path (C:\\..., /etc/...) or one that escapes the tree with .. is rejected. The convention is db/schema/jaunty.schema.json; the built-in default (schema/jaunty.schema.json) is used only when --output is omitted. Providers --provider value Aliases Driver sqlserver mssql Microsoft.Data.SqlClient postgres postgresql Npgsql mysql mariadb MySqlConnector sqlite , Microsoft.Data.Sqlite Provider matching is case-insensitive. An unrecognised value prints Error: unknown provider '<value>'. Supported: postgres, sqlserver, mysql, mariadb, sqlite. and exits 1. MariaDB is wire/SQL-compatible with MySQL for everything JauntyQ emits, so --provider mariadb maps to the mysql dialect in the snapshot. Environment variables Variable Effect JAUNTYQ_CONNECTION Default connection string when neither --connection-env nor --connection is given. JAUNTYQ_VERBOSE Set to 1 to print full exception detail on failure. Otherwise errors are anonymized to avoid leaking connection-string secrets into logs. JAUNTYQ_LICENSE Path to a jaunty.license.json used for premium-feature entitlement checks. Overrides the per-user install; intended for CI so it need not run activate interactively. Exit codes Code Meaning 0 Success. pull: snapshot written. verify: snapshot matches (with --usage, this includes runs whose only drift was downgraded to informational). usage export: manifest written. activate/license: completed. 1 Usage or runtime error, bad/missing arguments, unknown provider, no connection string, invalid --output path, an extraction/connection failure, an activation failure (bad signature, malformed license, wrong product, unknown version), or a registry error (missing registry file, unknown schema id). 2 verify: schema drift detected (differences listed on stderr). migrate impact: an entry at or above --fail-on exists, or --fail-on-warnings is set and the run warned. explain: a plan problem at or above --fail-on exists. registry validate: the registry has problems (each named on stderr). 3 Reserved: a premium command (migrate impact, schema verify, explain, registry, usage export) was run without an entitling license, or any premium verb was run at the free Extrode.JauntyQ.Cli tool, which prints the Extrode.JauntyQ.Cli.Premium install line instead. Distinct from 1/2 so scripts can tell \"not licensed\" from \"found problems.\" A lapsed license does not produce this code, it runs with a warning. Examples Pull with the connection string from an environment variable (recommended): export MYDB_CONN='Host=localhost;Database=app;Username=app;Password=...' jauntyq schema pull --provider postgres --connection-env MYDB_CONN \\ --output db/schema/jaunty.schema.json Verify in CI, failing the job on drift: jauntyq schema verify --provider sqlserver --connection-env CI_DB_CONN \\ --output db/schema/jaunty.schema.json # exit 0 = match, exit 2 = drift Optional MSBuild wiring that verifies before every build when a connection is available (CI often has none, which is why the snapshot is committed): <Target Name=\"VerifyJauntySchema\" BeforeTargets=\"Build\" Condition=\"'$(JAUNTY_VERIFY_CONN)' != ''\"> <Exec Command=\"jauntyq schema verify --provider sqlserver --connection-env JAUNTY_VERIFY_CONN --output db/schema/jaunty.schema.json\" /> <\/Target> See the schema snapshot format for the structure of the file this command writes."},{"u":"06-reference-configuration.html","t":"Configuration reference (MSBuild & file conventions)","s":"Reference","h":[{"i":"msbuild-properties","t":"MSBuild properties"},{"i":"additionalfiles","t":"AdditionalFiles"},{"i":"how-files-are-classified","t":"How files are classified"},{"i":"schema-source-precedence","t":"Schema-source precedence"},{"i":"the-acceptance-sidecar-acceptjson","t":"The acceptance sidecar (*.accept.json)"},{"i":"entity-and-method-naming","t":"Entity and method naming"},{"i":"consumer-project-template","t":"Consumer project template"}],"b":"Configuration reference (MSBuild & file conventions) Everything the generator reads is either an MSBuild property or an AdditionalFiles item classified by its path. There are exactly two properties and one set of folder conventions. MSBuild properties Property Values Default Effect JauntyQDialect sqlserver, postgres, mysql, sqlite (case-insensitive) unset Declares the target dialect for DDL-as-schema-source mode (no .schema.json present). Ignored when a snapshot exists, because the snapshot already carries its dialect. Missing or invalid while db/ddl/*.sql files are present raises JNT9003. JauntyQAutoCrud true / false true Whether the generator synthesises CRUD methods (GetAll/GetById/Insert/Update/Delete/Upsert/BulkInsert) for every table. Set false to emit nothing but your hand-written queries. <PropertyGroup> <JauntyQDialect>sqlite<\/JauntyQDialect> <!-- only for DDL mode --> <JauntyQAutoCrud>false<\/JauntyQAutoCrud> <!-- optional --> <\/PropertyGroup> Both are surfaced to the analyzer through CompilerVisibleProperty registrations. When you consume JauntyQ as a package, the shipped MSBuild props (build/ and buildTransitive/) register them automatically, you do not add <CompilerVisibleProperty> yourself. In-repo builds get the same registration from Directory.Build.props. No other analyzer-config keys (build_metadata.*, .editorconfig values) are read by the generator, apart from the standard dotnet_diagnostic.<code>.severity mechanism you can use to tune or suppress individual JNTxxxx diagnostics. AdditionalFiles The generator consumes these AdditionalFiles globs: <ItemGroup> <AdditionalFiles Include=\"db\\**\\*.sql\" /> <AdditionalFiles Include=\"db\\schema\\*.schema.json\" /> <AdditionalFiles Include=\"db\\schema\\*.accept.json\" /> <\/ItemGroup> The single db\\**\\*.sql glob covers queries, migrations, and DDL, they are distinguished by folder, not by separate globs. The third glob is optional and only needed if you use the acceptance sidecar. How files are classified Classification is by path segment (case-insensitive, / and \\ both match): Kind Recognized by Role Schema snapshot file ends in .schema.json (convention db/schema/*.schema.json) Authoritative schema when present. Acceptance sidecar file ends in .accept.json (convention db/schema/jaunty.accept.json) Accepts unindexed scans in generated queries. Optional. DDL a ddl path segment (convention db/ddl/*.sql) Builds the base schema only when no snapshot exists. Requires JauntyQDialect. Migration a migrations path segment (convention db/migrations/*.sql) Applied on top of the base schema, in filename order. Query any other .sql under db/ (convention db/tables/<Entity>/<Method>.sql) Parsed, validated, and emitted as a typed method. Any path component literally named ddl or migrations triggers that classification, regardless of depth. Schema-source precedence If a .schema.json snapshot is present, it is authoritative and any db/ddl/*.sql files are ignored (no merge, no error). Otherwise, if db/ddl/*.sql files are present, they build the base schema (requires JauntyQDialect). Migrations, if any, are always simulated on top of the result of step 1 or 2, in filename order. See DDL as schema source for the DDL path and dialect differences for what each dialect emits. The acceptance sidecar (*.accept.json) Auto-CRUD synthesizes a GetBy<Fk> loader for every foreign-key column, and on engines that do not index foreign keys automatically (SQLite, unlike InnoDB) those loaders scan, so they raise JNT8004. A synthesized query has no file, so it cannot carry -- @allow-unindexed. The sidecar is where a generated scan is accepted instead: { \"allowUnindexed\": [ { \"table\": \"address\", \"column\": \"city_id\", \"reason\": \"upstream schema, the index is not ours to add\" } ] } Rule Detail Per column table and column name one column. There is no wildcard and no table-level form; each accepted scan is a decision someone made. Reason mandatory Same contract as the directive. A blank reason is JNT6003 and the entry accepts nothing. Generated queries only A hand-written query filtering an accepted column still raises JNT8004. It has a file; the file carries the directive. One file More than one *.accept.json is JNT6003; the ordinal-lowest path wins and the others are ignored entirely, they are not merged. Structural problems are JNT6003 Unparseable JSON, a missing table/column/reason, a table or column the snapshot does not have, or the same column accepted twice (the first entry stays in force). Every one is a warning, and the entry is dropped. Dead entries are JNT8012 An entry that suppressed nothing on a build that ran synthesis. Either an index now covers the column, delete the entry, or a hand-written .sql claimed that loader's slot, in which case the acceptance belongs in that file as -- @allow-unindexed <reason>. Off with auto-CRUD Under <JauntyQAutoCrud>false<\/JauntyQAutoCrud> no entry can be used, so the dead-entry sweep does not run. Structural problems are still reported. It suppresses JNT8004 and nothing else. Table and column are matched case-insensitively, as everywhere else an identifier is resolved against the snapshot. Why a separate file rather than a field on the snapshot. jauntyq schema pull rewrites the snapshot wholesale, so an acceptance recorded there would be erased by the next pull. The sidecar is hand-maintained and nothing generates over it. samples/Extrode.JauntyQ.Sakila.Sqlite.Tests/db/schema/jaunty.accept.json is a worked example: the SQLite port of Pagila declares its foreign keys without secondary indexes, and its 17 entries take the resulting JNT8004 count to zero. Entity and method naming Entity name = the folder under db/tables/ (e.g. Products → db.Products, generated class Products). Method name = the .sql file name without extension (e.g. GetByCategory.sql → GetByCategory). A hand-written file whose name matches an auto-CRUD method overrides the synthetic one; hand-written always wins. Consumer project template Package consumers: <Project Sdk=\"Microsoft.NET.Sdk\"> <PropertyGroup> <TargetFramework>net8.0<\/TargetFramework> <\/PropertyGroup> <ItemGroup> <PackageReference Include=\"Extrode.JauntyQ.Generator\" Version=\"0.1.0\" PrivateAssets=\"all\" /> <PackageReference Include=\"Extrode.JauntyQ.Runtime\" Version=\"0.1.0\" /> <\/ItemGroup> <ItemGroup> <AdditionalFiles Include=\"db\\**\\*.sql\" /> <AdditionalFiles Include=\"db\\schema\\*.schema.json\" /> <\/ItemGroup> <\/Project> In-repo (project reference) consumers use the analyzer wiring instead: <ItemGroup> <ProjectReference Include=\"..\\..\\src\\Extrode.JauntyQ.Generator\\Extrode.JauntyQ.Generator.csproj\" OutputItemType=\"Analyzer\" ReferenceOutputAssembly=\"false\" /> <ProjectReference Include=\"..\\..\\src\\Extrode.JauntyQ.Runtime\\Extrode.JauntyQ.Runtime.csproj\" /> <\/ItemGroup> For DDL-as-schema-source projects, add <JauntyQDialect> and omit the .schema.json glob."},{"u":"06-reference-cross-dialect-compatibility.html","t":"Cross-dialect compatibility","s":"Reference","h":[{"i":"what-the-suite-compares","t":"What the suite compares"},{"i":"differences-the-suite-erases","t":"Differences the suite erases"},{"i":"known-differences","t":"Known differences"},{"i":"where-the-engines-differ-by-design","t":"Where the engines differ by design"},{"i":"ordering-and-comparison","t":"Ordering and comparison"},{"i":"arithmetic-and-typing","t":"Arithmetic and typing"},{"i":"constructs-with-no-portable-spelling","t":"Constructs with no portable spelling"},{"i":"probed-and-found-identical","t":"Probed and found identical"},{"i":"reading-this-table","t":"Reading this table"},{"i":"running-it","t":"Running it"}],"b":"Cross-dialect compatibility JauntyQ generates for one dialect at a time, and the same .sql file is meant to mean the same thing on every engine it supports. This page states where that holds and where it does not. It is not a summary written by hand. Every entry below is produced by samples/Extrode.JauntyQ.Conduit.Differential.Tests, which runs one query corpus against SQLite, PostgreSQL, MySQL, MariaDB and SQL Server and compares each engine's rows to the others'. Any difference the suite finds that is not listed here fails the build. What the suite compares The Conduit corpus: 28 hand-written queries plus the auto-CRUD surface generated over the same seven tables, reads, an ordered script of inserts, updates and deletes, then every read again against the changed data. Results are compared engine-to-engine, never against a hand-written expected value, so no single engine's behavior is treated as the reference. Differences the suite erases These are compared as equal. Each is a claim that the difference is representational and carries no meaning; the suite's own tests pin that it does not erase anything beyond them. Erased Why it carries no meaning Integer width and signedness COUNT(*) returns ulong on MySQL, long on PostgreSQL and SQLite. The number is the result; its CLR width is a provider choice. Trailing zeros on a decimal 1.10 from decimal(10,2) and 1.1 from decimal(10,1) are the same number. 1.10 and 1.2 stay different. Boolean representation SQLite and SQL Server return 0/1 where PostgreSQL returns bool. DateTime.Kind Npgsql returns Utc, SqlClient Unspecified, for the same instant. Sub-second precision Compared to whole seconds, the resolution the corpus schema stores. Trailing spaces on a string CHAR(n) pads to width on SQL Server and MySQL; VARCHAR does not. Leading spaces are data and are kept. Row order without ORDER BY No engine promises an order for an unordered SELECT, so those results compare as multisets. A query that does order compares in order. Anything else, a differing row count, a differing value, a null where another engine returned a value, a differing column set, or an error one engine raises and another does not, is a difference, and either appears in the table below or fails the suite. Known differences Query Engines Difference Reason Articles/GetFiltered SQL Server vs SQLite, PostgreSQL, MySQL, MariaDB A page size of zero raises an error instead of returning no rows SQLite, PostgreSQL, MySQL and MariaDB accept LIMIT 0. SQL Server's OFFSET/FETCH grammar requires FETCH NEXT to be greater than zero and raises \"The number of rows provided for a FETCH clause must be greater then zero\". JauntyQ emits the paging clause each dialect requires and cannot reconcile the two without injecting a runtime guard into every paged query. Callers targeting SQL Server must not pass a page size of 0. Paging past the end of the result set returns no rows on all five. Where the engines differ by design The table above answers \"does JauntyQ generate code that behaves the same everywhere\". This section answers the other half: which SQL constructs mean different things on different engines, whoever writes them. These are facts about the engines, measured on the versions the suite runs, SQLite in-process, postgres:16-alpine, MySQL, MariaDB and SQL Server, not statements about generated code. Each row was produced by running the construct on all five engines and recording which of them returned the same result. A row where every engine agreed is not listed, because it is not a difference; two categories were probed and dropped for that reason, noted at the end. Ordering and comparison Construct Agree Differ What happens Write instead ORDER BY v ASC with NULLs SQLite, MySQL, MariaDB, SQL Server PostgreSQL PostgreSQL sorts NULL as the largest value, so it comes last ascending. The other four put it first. NULLS FIRST / NULLS LAST where available, else ORDER BY CASE WHEN v IS NULL THEN 1 ELSE 0 END, v. ORDER BY v DESC with NULLs SQLite, MySQL, MariaDB, SQL Server PostgreSQL The same rule from the other end: NULL first on PostgreSQL, last elsewhere. As above. A descending paged query returns different first-page rows across this split. 'ABC' = 'abc' MySQL, MariaDB, SQL Server PostgreSQL, SQLite The first three use a case-insensitive collation by default and call these equal. LOWER(a) = LOWER(b), or declare the collation. SQL Server's default is an install-time choice, so two deployments can differ. 'a' = 'a ' MariaDB, SQL Server SQLite, PostgreSQL, MySQL PAD SPACE collations ignore trailing spaces. MariaDB's default is PAD SPACE; MySQL 8's is not, the one measured case where the two split. TRIM(a) = TRIM(b), or trim on write. A text uniqueness constraint means different things across this split. ORDER BY on mixed case MySQL, MariaDB, SQL Server PostgreSQL, SQLite Case-insensitive collations sort a before B; code-point ordering puts B first. PostgreSQL's side follows the database's LC_COLLATE, measured here against the image's C locale. ORDER BY LOWER(v). Arithmetic and typing Construct Agree Differ What happens Write instead 5 / 2 SQLite, PostgreSQL, SQL Server MySQL, MariaDB MySQL and MariaDB always divide as decimals and return 2.5; the rest return the integer 2. The type differs as well as the value. DIV on MySQL/MariaDB for integer division; * 1.0 on the others for a fractional one. -5 / 2 SQLite, PostgreSQL, SQL Server MySQL, MariaDB The same split. Every engine that does divide as integers truncates toward zero, so rounding direction is not a difference. As above. 1 / 0 SQLite, MySQL, MariaDB PostgreSQL, SQL Server PostgreSQL raises SQLSTATE 22012 and SQL Server raises its divide-by-zero error; the other three return NULL, turning a broken calculation into a missing value. NULLIF(d, 0), NULL on all five, and says so on purpose. AVG(v) over integers SQLite, PostgreSQL, MySQL, MariaDB SQL Server SQL Server's AVG returns the argument's type, so an integer average truncates: 1.5 comes back as 1. AVG(CAST(v AS float)) or AVG(v * 1.0). Constructs with no portable spelling These have no single form that runs everywhere, so each engine was given its own. The difference is in what the engines then do. Construct Agree Differ What happens Write instead Add one month to 31 January PostgreSQL + SQL Server; MySQL + MariaDB SQLite Four engines clamp to 28 February. SQLite's date() normalizes the overflow and returns 3 March, a different month, with no error. (The two pairs differ only in returning a temporal value against text, which follows from each dialect's spelling.) Do not add months in SQL if the result must match. Compute the clamped date in the application. Join text to a number SQLite, PostgreSQL, MySQL, MariaDB SQL Server SQL Server's + is also addition, so it tries to convert and fails. Loud, and therefore the safest case here. 'a' + CAST(1 AS varchar(11)) on SQL Server, or CONCAT. SELECT TRUE SQLite, MySQL, MariaDB PostgreSQL; SQL Server PostgreSQL returns a real bool; three return the integer 1; SQL Server has no boolean literal and reads TRUE as a column name, then fails. Write 1 and 0. SELECT (1 = 1) SQLite, MySQL, MariaDB PostgreSQL; SQL Server The same three-way split, SQL Server has no boolean value type, so a comparison cannot appear in a select list. CASE WHEN 1 = 1 THEN 1 ELSE 0 END. Probed and found identical Recorded because a measured agreement is worth as much as a measured difference, and because the suite does not keep passing no-op cases: COUNT(*) and MAX(v) over an empty set, all five return 0 and NULL. SUM(v) over an empty set, all five return NULL, not 0. Concatenating NULL, all five return NULL (using each dialect's own concatenation form). Note the second: SUM over no rows is NULL everywhere, so COALESCE(SUM(v), 0) is needed on every engine, not just some. Reading this table An entry is a permanent statement about the engines, not a known bug. Where JauntyQ could make the engines agree, it does, and no entry appears. An entry means the difference is in the engine and reconciling it would cost more than it is worth, the reason column says what that cost is. Entries do not outlive the behavior they describe: if a divergence stops occurring on a run where its query and engines were both exercised, the suite fails on the stale entry. Running it dotnet test samples/Extrode.JauntyQ.Conduit.Differential.Tests With Docker absent the cross-engine tests skip and say which engines were missing; the harness's own unit tests and the SQLite-only corpus execution still run. Fewer than two engines is a skip, not a pass, one engine cannot disagree with itself. The suite runs in the nightly full-suite job, which has all five."},{"u":"06-reference-diagnostics.html","t":"JNT diagnostics reference","s":"Reference","h":[{"i":"0xxx-generator-internal","t":"0xxx, Generator Internal"},{"i":"1xxx-sql-parsing","t":"1xxx, SQL Parsing"},{"i":"2xxx-schema-validation","t":"2xxx, Schema Validation"},{"i":"3xxx-query-shape--projection","t":"3xxx, Query Shape / Projection"},{"i":"4xxx-parameter-binding","t":"4xxx, Parameter Binding"},{"i":"5xxx-value-safety","t":"5xxx, Value Safety"},{"i":"6xxx-configuration","t":"6xxx, Configuration"},{"i":"7xxx-dialect","t":"7xxx, Dialect"},{"i":"8xxx-performance","t":"8xxx, Performance"},{"i":"9xxx-migrations","t":"9xxx, Migrations"}],"b":"JNT diagnostics reference Every diagnostic JauntyQ can report, generated from the registry in src/Extrode.JauntyQ.Generator/JauntyDiagnostics.cs. Format: JNTxxxx where the first digit is the category. An Error blocks code generation for that query/table; a Warning still emits code. 0xxx, Generator Internal Code Title Severity Meaning JNT0001 Generator Internal Error Error JauntyQ itself threw. Not a fault in your SQL, schema or configuration. It exists because Roslyn's own story for a generator that throws is CS8785, which is a warning: the build continues, every type the generator would have emitted is simply absent, and you see only the CS0246s that follow. The message names the stage that failed and the exception; the generated output may be incomplete or entirely missing depending on which stage it was. Please report it. 1xxx, SQL Parsing Code Title Severity Meaning JNT1001 Unsupported SQL Construct Warning The tokenizer/parser hit a construct it doesn't model explicitly. Usually harmless, most SQL passes through verbatim. UNION and unsupported subqueries are excluded from this generic warning; see JNT1006/JNT1007. JNT1002 Unterminated Token Error A block comment (/* ...), bracket- or quote-delimited identifier ([..., \"..., `...) or string literal ('...) ran to end-of-file with no closing delimiter. Fix the file. If the literal looks closed to you and you write MySQL, check for \\', JauntyQ implements only the ANSI '' escape, and the message says so when it finds one. JNT1003 Input Too Large Error The .sql file exceeds SqlTokenizer.MaxInputLength (1 MiB). Split the file or check for accidental content. JNT1004 Unsupported Character Error The SQL contains a character the tokenizer does not recognize (e.g. $, \\). JauntyQ refuses to parse rather than risk a silently wrong model. Remove the character or rewrite the construct. JNT1005 Nesting Too Deep Error Parentheses nest beyond SqlTokenizer.MaxNestingDepth (1000). The recursive-descent parser is O(n²) in depth, so JauntyQ refuses pathologically deep input (almost always a generated-SQL or copy-paste error) rather than let a single file hang the build. JNT1006 Unsupported UNION Error UNION/UNION ALL is not supported: the generator only models the first branch's column shape, so a second branch with different nullability could silently generate code that reads NULL as non-nullable and throws at runtime. Split into separate queries, or model the combined result as an application-level merge. JNT1007 Unsupported Subquery Error A nested SELECT that isn't a WHERE-clause [NOT] IN (SELECT ...)/[NOT] EXISTS (SELECT ...) predicate (a scalar subquery in the projection list, or a derived table in FROM) falls through to the enclosing statement's ordinary parsing instead of being lifted as its own scope, so the outer query's shape may already be misparsed. Rewrite using a JOIN, a WHERE-clause IN/EXISTS predicate, or a CTE. JNT1008 Multiple Statements In One File Error The file holds more than one top-level statement (a ; followed by more SQL). JauntyQ generates one method per file from one statement: a second SELECT's tables and columns are merged into the first statement's model, and a second statement after an INSERT/UPDATE/DELETE is dropped entirely, so the generated mapper matches neither statement. Split each statement into its own .sql file. JNT1009 Unsupported Syntax Error Syntax the parser has no grammar for, reported as such rather than as a schema problem. Currently: LATERAL joins, CROSS APPLY/OUTER APPLY, and PostgreSQL's DISTINCT ON (...). Split out of JNT2001, which was reporting both \"this relation is not in your schema\" and \"this keyword is not in my grammar\", for LEFT JOIN LATERAL the parser read the keyword as the joined table's name, so the consumer was told a table they had never written was missing from their schema. The two failures need opposite responses from the reader, so they no longer share a code. See supported-sql.md for the accepted surface. 2xxx, Schema Validation Code Title Severity Meaning JNT2001 Table Not Found Error Query references a table absent from the schema snapshot. Re-run jauntyq schema pull or fix the table name. JNT2002 Column Not Found Error Query references a column absent from the resolved table. JNT2003 Ambiguous Column Error An unqualified column name matches more than one joined table; qualify it with an alias. JNT2004 Illegal Identifier Error A table/column/alias/parameter name can't form a valid C# identifier after DialectMapper.ToPascalCase. JNT2005 Stored Procedure Not Found Error -- @call <Proc> names a procedure absent from the schema snapshot's procedures section. JNT2006 Entity/Row POCO Name Collision Error Two tables would generate identical entity or row-POCO class names. JNT2007 Unmapped Column Type Warning DialectMapper.MapDbTypeToCSharp fell through to object for this column's db type, no C# type mapping exists. JNT2008 Duplicate Generated File Error Two .sql files produce the same generated file name, compared case-insensitively as Roslyn's AddSource does (e.g. getUsers.sql and GetUsers.sql in one folder, possible on a case-sensitive filesystem). Left unchecked this crashes the whole generator (CS8785); rename one so the generated names differ by more than case. JNT2009 Duplicate Sequence Accessor Name Error Two sequences whose PascalCased names collide would generate the same accessor method; only the first is emitted. Rename one. JNT2010 Duplicate Entity Accessor Name Error Two or more tables whose PascalCased names collide all generate the same db.<Entity> accessor; only the first table's auto-CRUD is emitted and the rest are invisible. Rename the tables, or write their queries by hand. JNT2011 Duplicate Column Property Name Error Two result columns of a stored procedure map to the same generated property, which the Result DTO cannot declare twice. Alias one distinctly in the procedure's own SELECT. JNT2012 Reserved Parameter Name Prefix Error A .sql query parameter uses the __ prefix, which is reserved for JauntyQ's own bookkeeping identifiers (__conn, __cmd, __reader, __weOpened and others). Such a name can silently collide with generated code. Rename it without the leading double underscore. A single leading underscore is fine. JNT2013 Duplicate Parameter Name Error Two stored-procedure parameters map to the same generated C# parameter, DialectMapper.ToPascalCase strips separators, so order_number and OrderNumber both become orderNumber. The generated method cannot declare it twice, and no rename by the generator can resolve it. Rename one in the procedure definition. JNT2014 Table Skipped For Colliding Column Names Warning Two of a table's columns map to the same generated property, so no row type or auto-CRUD can be emitted and the table is absent from the generated API entirely. Warning rather than error so an unused table with such a pair does not fail the build. Rename one column, or alias it distinctly in a hand-written query. JNT2015 Table Skipped For Unquotable Identifier Warning The table's name, or one of its column names, cannot be written unquoted: a keyword JauntyQ's own SQL parser reserves, a word the target engine reserves in that position, an illegal character, or (PostgreSQL only) a mixed-case name an unquoted reference would fold to lower case. Auto-CRUD is skipped and the table is absent from the generated API. Warning rather than error so an unused table does not fail the build. Rename it, or write its queries by hand with the identifier quoted. The per-engine reserved-word lists behind this are the verdict of a live probe against PostgreSQL 16, MySQL 8.0, MariaDB 11.8, SQL Server 2022 and SQLite 3.45, see DialectReservedWords.cs. JNT2016 Duplicate Enum Member Name Error Two members of a captured database enum fold to the same C# identifier, 'in progress' and 'in-progress' both become InProgress, and '' and '-' both become _. The generated enum cannot declare the member twice, and renaming one in the generator would silently diverge from the value the database stores. Both offending values are named in the message. Rename one member in the database. JNT2017 Enum Type Name Collision Error A captured enum's generated type name collides with another generated type: a row type, another enum, one of the emitted {EnumName}Values companions, or the shared JauntyQEnumValueException. A table named order_status_values alongside an enum order_status is the reachable case. Renaming silently is the anti-pattern JNT2015 exists to end, so this is an error. Rename the table or the enum. JNT2018 Non-Atomic Key-Targeted MySQL Upsert Warning The table carries a UNIQUE constraint other than the key the upsert resolved, so MySQL's ON DUPLICATE KEY UPDATE, which names no conflict target and matches whichever UNIQUE the insert violates, would not agree with the key postgres/sqlite ON CONFLICT (cols) and sqlserver MERGE ... ON name explicitly. JauntyQ therefore emits a key-targeted UPDATE ... ; INSERT ... SELECT ... WHERE NOT EXISTS pair instead. Two statements are not atomic, and a transaction does not make them so, concurrent upserts of the same new key can fail with 1062 (both pass the existence check), deadlock with 1213 (one session's insert-intention lock meets the other's gap lock from the UPDATE), or silently write nothing and return 0 (another session commits the row between this call's UPDATE and its INSERT). Retry on 1062/1213 (the call is idempotent) or drop the competing constraint; a 0 return is not by itself evidence of the lost write, since an unchanged re-run also returns 0 under UseAffectedRows=true. Any UNIQUE whose column set differs from the key counts as competing, with one exception: a full-column superset of the key cannot fire independently of it (violating (id, tenant) requires duplicating id) and is not competing. An index with a MySQL prefix key part (captured from SUB_PART as IndexSchema.HasPrefixKeyPart) is always competing, whether a superset (UNIQUE (id(3), tenant)) or a restatement of the key's own columns (UNIQUE (id(3)) beside a full-column key on id), it fires on rows sharing only the prefix. A UNIQUE with an expression key part (MySQL functional index, captured as IndexSchema.HasExpressionKeyPart with only the real-column subset in Columns) always competes, it constrains rows through an expression the key's columns say nothing about, and before 2026-07-30 it was excluded at capture and therefore invisible to this check. Single-UNIQUE tables are unaffected and keep ON DUPLICATE KEY UPDATE. JNT2019 Upsert Skipped For Prefix-Only Unique Key Warning The only constraint that could serve as the table's upsert key is a UNIQUE over a column prefix (MySQL UNIQUE (email(5)), captured from SUB_PART as IndexSchema.HasPrefixKeyPart), either the sole fallback UNIQUE for an identity-only PK, or the PRIMARY index itself (PRIMARY KEY (name(10))). Such an index enforces uniqueness over the truncated prefix, so the engine matches rows sharing only that prefix, not the full value an Upsert(...) signature implies, and no emitted form, atomic or two-statement, can honor the contract. No Upsert is synthesized (the table's other CRUD is unaffected). Add a full-column UNIQUE constraint or a bindable primary key, or write the upsert by hand. A schema snapshot without index entries (e.g. migration simulation) carries no prefix evidence and is never refused. JNT2020 Upsert Skipped For Expression-Only Unique Key Warning The only constraint that could serve as the table's upsert key is a UNIQUE with an expression key part (UNIQUE (lower(email)), captured as IndexSchema.HasExpressionKeyPart with only the real-column subset of its key in Columns). The engine matches conflicts on the expression's value, which no generated method can bind a parameter to, and the snapshot does not carry the expression at all, so such an index can never be the upsert key. No Upsert is synthesized (the table's other CRUD is unaffected). Add a full-column UNIQUE constraint or a bindable primary key, or write the upsert by hand. When a prefix-only refusal (JNT2019) and this one could both apply, only JNT2019 is reported. Before 2026-08-01 this skip was silent, the reading such tables had when expression indexes were excluded at capture entirely (pre-2026-07-30). JNT2021 Write To View Error An INSERT/UPDATE/DELETE targets a relation captured as a view (isView: true). Views enter the snapshot so they can be read; they are captured read-only, so no write is generated for one. Keyed on the write's target relation only, an UPDATE against a base table whose predicate joins or subqueries a view is fine, and a SELECT from a view is the case views exist for. Error rather than a silent skip because the consumer authored the statement deliberately: skipping it would emit a method whose SQL the engine rejects at runtime, and warning would let that method be called. Write to the underlying table instead. JNT2022 Lossy Identifier Rename Warning A table or column name contains a letter or digit outside ASCII, which generated names drop: größe becomes the property GrE, café becomes Caf, 日本語 becomes _. The code compiles and nothing collides, so JNT2011/JNT2014 never see it, but the property is spelled as a different word from your column. Rename the column, or alias it to an ASCII name in a hand-written query. JNT2023 Function Name Collision Error Two captured functions fold to one generated method name, or a function's name collides with a table, enum or sequence accessor. PostgreSQL overloads are the common case: f(int) and f(text) are distinct functions whose parameter types often map to the same C# types, so C# overloading cannot carry them. Error rather than a skip because nothing can be emitted for either side, choosing one silently would make which function a call site reaches depend on emission order. Rename one function, or exclude it from capture. JNT2024 Unmappable Function Signature Warning A captured function has a parameter or return type with no C# mapping, so no method is emitted for it. The rest of the schema generates unaffected, one unmappable function does not cost you the others. Hand-write the call, or change the function's signature to a mappable type. JNT2025 Table-Valued Parameter Not Supported Warning A captured procedure takes a SQL Server table-valued parameter. No method is emitted: binding a TVP needs SqlParameter.SqlDbType = SqlDbType.Structured and SqlDataRecord, which the generated abstract DbParameter path cannot reach. The message prints the TVP type's full column list, so you have the shape needed to hand-write the call with IEnumerable<SqlDataRecord>. Previously this generated object with JNT2007, a method that compiled and could not work. TVP support is specified and deferred; if you are hitting this, that is the signal that moves it. JNT2026 Duplicate Return-Value Parameter Error A captured procedure declares more than one parameter with direction ReturnValue. A procedure has exactly one return status, so at most one of them could ever carry a value and nothing defines which. No method is emitted. Keep one and give the others direction Out. This is only reachable from a hand-authored or externally produced snapshot: jauntyq schema pull never emits the direction. 3xxx, Query Shape / Projection Code Title Severity Meaning JNT3001 Empty Query Warning The .sql file is empty or has no SELECT columns to project. JNT3002 SELECT * Forbidden Error SELECT * is rejected, list columns explicitly so the generated shape is stable. JNT3003 Invalid Directive Combination Error Two directives on the same query conflict (e.g. -- @stream with -- @first). JNT3004 Expression Projection Missing Alias Error A computed SELECT expression has no AS alias, so the generator has no property name to emit. JNT3005 Expression Type Unresolved Error A computed expression's C# type can't be inferred; declare it with -- @type <alias> <type>. JNT3006 Unknown Type Directive Alias Error A -- @type directive names an alias that doesn't match any projection item. JNT3007 IN-Subquery Column Count Error An IN (SELECT ...) subquery projects more than one column. JNT3008 Unrecognized Directive Warning A -- @word comment looks like a directive but parsed as nothing: a value-taking directive written bare (-- @each with no parameter name) or a one-edit typo of a known name (-- @frist). The line stayed a plain comment and had no effect. Ordinary @word comments (-- @author) never trigger this. JNT3009 Duplicate Result Column Error Two SELECT/RETURNING items map to the same generated property after DialectMapper.ToPascalCase (id as X, y as X, the case-only as x, as X, or the separator fold my_col/myCol). The row type can't declare the member twice (CS0102); give each item a distinct alias. JNT3010 Mirrored Query Not Comparable Warning A file declares -- @mirrors <Query> but the pairing cannot be compared, so no drift verdict is possible: the target name resolves to no query in the corpus (typo, renamed file, not in AdditionalFiles), a bare method name is ambiguous across two entities (qualify it Entity.Method), the target produced no query model (it failed to parse or validate, or it binds a stored procedure), the target is the declaring query itself, or a WHERE-clause column on either side does not resolve to a schema column (a CTE virtual column, say). Reported rather than silently skipped, and rather than falling back to raw text, a text comparison would invent drift out of a spelling difference, and one false positive kills an opt-in check. Fix the target name or qualify the column, or remove the directive. JNT3011 Duplicate Directive Warning A non-repeatable directive appears more than once in one .sql file. Every directive except -- @type and -- @each stores into a single field, so the later line overwrites the earlier one, previously without a word, which made two contradictory -- @result lines, or two -- @allow-sort reasons where only the second is in force, invisible in both the SQL and the generated code. Later still wins; the message names the value that was discarded and the one that took its place. A repeat carrying the same value (including a repeated flag such as -- @first) gets the milder wording, since nothing was lost. Bare value-taking lines are not occurrences: -- @call with no name is declined as JNT3008 and does not make a later real -- @call look like a repeat. 4xxx, Parameter Binding Code Title Severity Meaning JNT4003 Parameter Type Unresolved Error @param's C# type couldn't be inferred from a bound column; declare it via -- @params name:type. JNT4004 Duplicate Parameter Error The same @name appears twice in a -- @params directive. 5xxx, Value Safety Code Title Severity Meaning JNT5001 String Literal Exceeds Column Length Error A literal string value in the SQL is longer than the target column's MaxLength. JNT5002 Numeric Literal Out Of Range Error A literal numeric value can't fit the target column's numeric type/precision. Also covers a value the engine accepts but silently rewrites: a fractional literal beyond the column's scale, and a SQL Server bit given anything but 0 or 1 (stored as 1). JNT5003 Literal Type Mismatch Error A literal is the wrong kind of value for the column, not merely too long or too large, the server rejects the statement outright. Today: a numeric literal against a PostgreSQL bit/bit(n), which is a bit string and needs B'1010' form. 6xxx, Configuration Code Title Severity Meaning JNT6001 Schema Missing Error No schema snapshot found and no db/ddl/*.sql DDL source either. Run jauntyq schema pull, or see db/ddl/*.sql + <JauntyQDialect> (JNT9003). JNT6002 Multiple Schema Snapshots Warning More than one *.schema.json was passed as AdditionalFiles. The ordinal-lowest path wins deterministically; the message names every candidate. Remove the extras (or exclude them from <AdditionalFiles>) so the schema source is unambiguous. 7xxx, Dialect Code Title Severity Meaning JNT6003 Invalid Acceptance File Warning A structural problem in jaunty.accept.json, the sidecar that accepts an unindexed auto-CRUD scan per column (spec 016): the file is not valid JSON, more than one *.accept.json was passed as AdditionalFiles (ordinal-lowest wins, the message names every candidate, and the others are not merged), an entry is missing its table, column or reason, an entry names a table or column the snapshot does not have (a typo, or an entry that outlived a dropped column), or the same column is accepted twice (the first entry is the one in force). A rejected entry accepts nothing, so the JNT8004 it was meant to cover is still reported, a half-read entry quietening a warning would make a typo cheaper than getting it right. An entry that is well-formed but no longer needed is JNT8012, not this. Warning rather than Error: a mistake in the file written to quieten the build must not be louder than the warnings it was written to quieten. JNT7001 Identity Return Unavailable Error -- @identity was used but the table/dialect combination can't return the generated key. JNT7002 Construct Unavailable On Dialect Error A RETURNING clause or data-modifying CTE was used on sqlserver/mysql, neither of which supports it (MariaDB via the mysql dialect string does support RETURNING 10.5+/13.0+, this intentionally over-flags that case to stay safe on stock MySQL). JNT7003 Unknown Dialect Error schema.Dialect isn't one of sqlserver/postgres/sqlite/mysql, check DialectMapper.IsKnownDialect. 8xxx, Performance Code Title Severity Meaning JNT8001 Possible Cartesian Product Warning A join has no ON/equality condition connecting the two tables. JNT8002 Non-Sargable Predicate Warning A WHERE clause wraps an indexed column in a function/expression, defeating index seeks. JNT8003 Leading-Wildcard LIKE Warning LIKE '%value' can't use a standard B-tree index. JNT8004 Unindexed Filter Column Warning A filter/join column has no covering index (composite indexes are checked positionally: a non-leading column is \"covered\" only when every column ahead of it in the same index is also filtered in this query). On a hand-written query, accept it deliberately with -- @allow-unindexed <reason>. On an auto-CRUD query there is no file to annotate, so the message names the exact .sql path that would override the generated method, and, since spec 016, the jaunty.accept.json entry that accepts the scan without giving up schema tracking. Only the GetBy<Fk> loader family can raise this: GetAll has no filter, and GetById/Update/Delete filter the full primary key. JNT8005 Duplicate Query Warning Two or more generated methods compile to byte-for-byte identical SQL; consolidate them. JNT8006 Join Not A Foreign Key Warning A join's column pair matches no declared foreign key between the two tables (exact per-pair match, either direction). Usually a wrong-column join (a bug) or an unmodeled relationship. Skipped when the snapshot carries no foreignKeys, or when either side resolves to a CTE/subquery/table absent from the schema. Composite FKs are stored one row per column pair, so a pair-by-pair join matches and is not flagged. JNT8007 Unindexed Order By Warning An ORDER BY names a column no index can order (not the leading column of any index, and not a non-leading column whose more-leading index columns are all constrained by a bound parameter in this query, any comparison operator, not only equality, which makes a range on the leading column a known false negative), forcing a runtime sort. Accept it deliberately with -- @allow-sort <reason>. Only plain base-column items are checked; ordinals (ORDER BY 1), expressions (ORDER BY lower(x)), projected aliases, primary keys, and snapshots without index metadata are skipped. JNT8008 Possible N+1 Access Pattern Warning Cross-query heuristic: a SELECT is a point lookup on a child table by an equality on every column of a declared foreign key (WHERE child.parent_id = @parentId), and the corpus also contains a parent-collection query over that FK's parent table (a multi-row SELECT, not a single-row PK lookup). Calling the child lookup once per parent row is 1 + N round-trips, fetch set-based instead: join the child into the parent query, or batch with WHERE parent_id IN (...) (for the COUNT/aggregate-per-parent variant, a grouped aggregate join). Fires at most once per child lookup, naming one representative parent query. Never fires when the child is already set-based (IN on the FK column, or joins/reads the parent), when the parent query is a single-row PK lookup, or when the snapshot has no foreignKeys; composite FKs require all columns filtered (a partial match never fires) and self-referencing FKs are supported. Suppress an intentional per-row lookup the standard way: <NoWarn>$(NoWarn);JNT8008<\/NoWarn> or dotnet_diagnostic.JNT8008.severity = none in .editorconfig; or, for one query only and with the decision recorded in the file, -- @allow-n-plus-one <reason> (see JNT8013 and directives). JNT8009 Unstable Pagination Warning A query takes a page (LIMIT/OFFSET, including T-SQL's OFFSET … FETCH NEXT) but its ORDER BY covers no primary key and no complete unique index, so rows that sort equally have no defined relative order: the engine may return them differently on each execution, and a row can then appear on two pages or on none. Add a tiebreak on a unique column, usually the primary key. Checked per statement, so a page taken inside a CTE, the idiomatic paginate-then-aggregate shape, is checked against its own base columns, and a page taken by the final statement of a WITH is checked too (fixed 2026-08-03; before that the outer model never learned it paged, and both codes stayed silent on that shape). Silent when: the sort already covers a unique key; any ORDER BY item is an expression, ordinal, projected alias or unresolvable name (it may itself be unique, and the IR cannot tell); the statement joins, has more than one table, or has a GROUP BY (per-row uniqueness proves nothing about a fanned-out or collapsed result); this query's equality filters already pin one row; the statement is a predicate subquery; or the table carries no captured index metadata, in which case a non-PK unique tiebreak is unprovable rather than absent, DDL/migration-sourced tables have primary keys but no secondary unique indexes (see JauntyQGenerator.Part7.cs), so firing there would be a false positive on every such project. TOP n without OFFSET is not pagination and never fires. JNT8010 Unordered Pagination Warning A query takes a page with LIMIT/OFFSET and has no ORDER BY at all: the rows in a page are an arbitrary subset that can differ between requests. Strictly worse than JNT8009 and reported instead of it, never alongside. Needs no uniqueness proof, so unlike JNT8009 it still fires for a table with no captured index metadata. Silent under the same scope gates (join, multi-table, GROUP BY, predicate subquery) and when this query's equality filters already cover a unique key, which is what keeps the WHERE pk = @id LIMIT 1 point-lookup idiom quiet. JNT8011 Predicate Drift Warning Cross-query check, opt-in: a file declares -- @mirrors <Query> and its WHERE clause does not match that query's. The case it exists for is a paginated list and the count that sizes its pager, when one gains a filter the other lacks, the page contents stay correct while the total is wrong, and nothing else surfaces it. Compared are the WHERE clauses of the statement and of every CTE body, unioned (so the idiomatic paginate-inside-a-CTE list is comparable with a flat count), split at top-level AND into atoms and matched as a set with column references resolved against the schema first, so qualification (b.user_id vs user_id), keyword casing and atom order do not matter. ORDER BY, projection and LIMIT/OFFSET are not compared, a count query has none. Deliberately conservative: a top-level OR collapses the whole clause into one atom rather than being split, BETWEEN's own AND is not a splitter, and the match is textual after resolution, so a >= @x and @x <= a read as drift. Silent when the sets match and whenever no -- @mirrors is present; a pairing that cannot be compared is JNT3010, not silence. JNT8012 Unnecessary Unindexed Acceptance Warning Also reported for a jaunty.accept.json entry that no generated query used (spec 016), the message names the file, the table.column and the stated reason, and gives both remedies, because they differ: an index now covers the column (delete the entry), or a hand-written .sql has claimed that loader (move the acceptance into that file as -- @allow-unindexed). Not reported at all when auto-CRUD is off, since no entry could have been used. For the directive itself: a query declares -- @allow-unindexed <reason> but no filter column on it is unindexed, or -- @allow-sort <reason> but no ORDER BY column on it is unindexed, so the directive suppresses nothing. The message names the directive it is about, so a query carrying both says which one to remove. Almost always means the index it was waiting for now exists: remove the directive. Reported rather than tolerated because an exemption that outlives the condition justifying it is how an escape hatch becomes the default, left in place it stops hiding an accepted scan and starts hiding a future regression. Warning, not Error: a stale suppression is untidy, not wrong. JNT8013 Unnecessary N+1 Acceptance Warning A query declares -- @allow-n-plus-one <reason> but was never matched as an N+1 child lookup against any parent-collection query in the corpus, so the directive suppresses nothing: remove it. The message repeats the stated reason. Its own code rather than JNT8012 because the two rest on different evidence — JNT8012 (allow-unindexed) reads index metadata, this one reads the FK graph and the query corpus — and severity is configured per code, so silencing JNT8012 should not also silence this check. Usually means the parent-collection query it was written against is gone, or the child lookup has since become set-based. Silent when the snapshot declares no foreignKeys (the pass is inert, so nothing could have been suppressed). Warning, not Error, for the same reason as JNT8012. 9xxx, Migrations Code Title Severity Meaning JNT9001 Migration Statement Not Simulated Warning A migration statement MigrationParser has no explicit handling for at all (e.g. CREATE VIEW, CREATE PROCEDURE, an unmodeled RENAME, or an unrecognized constraint clause); the effective schema may be incomplete for it. Index DDL and permission statements (CREATE/DROP INDEX, GRANT/REVOKE) are explicitly recognized and silently ignored instead, they do not raise this diagnostic. JNT9002 Migration Invalid Against Schema Error A migration/DDL statement targets a table/column that doesn't exist (or already exists) in the schema being simulated. JNT9003 Missing or Unknown Dialect for DDL Schema Source Error db/ddl/*.sql files are present with no JSON snapshot, but <JauntyQDialect> isn't set to a recognized value (sqlserver/postgres/mysql/sqlite). JNT9004 Migration Risky Impact Warning A pending migration changed a column a query references (type, nullability, length, precision) without invalidating the query. Non-fatal; see jauntyq migrate impact for the full SAFE/RISKY/BREAKING report."},{"u":"06-reference-dialects.html","t":"Dialect differences","s":"Reference","h":[{"i":"identifiers-and-dialect-strings","t":"Identifiers and dialect strings"},{"i":"parameters","t":"Parameters"},{"i":"feature-matrix","t":"Feature matrix"},{"i":"identity-return","t":"Identity return"},{"i":"upsert","t":"Upsert"},{"i":"bulk-insert","t":"Bulk insert"},{"i":"type-mapping","t":"Type mapping"},{"i":"dialect-specific-notes","t":"Dialect-specific notes"}],"b":"Dialect differences JauntyQ supports four dialect families. The dialect is fixed per project, it comes from the snapshot's dialect field (or JauntyQDialect in DDL mode) and determines the SQL the generator emits for synthesised operations. Your hand-written query SQL is emitted verbatim; the differences below apply to generated CRUD, identity return, upsert, sequences, and bulk insert. Identifiers and dialect strings Dialect Snapshot dialect CLI aliases MariaDB SQL Server / Azure SQL sqlserver sqlserver, mssql , PostgreSQL postgres postgres, postgresql , MySQL mysql mysql Use mysql SQLite sqlite sqlite , MariaDB is wire- and SQL-compatible with MySQL; declare its snapshot dialect as mysql. An unknown dialect string in a snapshot is JNT7003. The CLI also accepts mariadb as a provider alias and writes mysql into the snapshot. Quoted identifiers, [brackets], \"double quotes\", and `backticks`, are tokenized and validated in hand-written queries. Auto-CRUD synthesis and bulk insert, however, emit unquoted identifiers, so tables that require quoting to resolve (e.g. a PostgreSQL table created with a quoted mixed-case name, or names with spaces) are not supported by the synthesized surface, write those queries by hand. (On MySQL, note that JauntyQ follows ANSI rules and treats a double-quoted token as an identifier, not a string literal; use single quotes for strings.) Parameters All dialects use named parameters (@Name) in the emitted SQL; the ADO.NET provider translates them to its native placeholder style. On PostgreSQL, generated code binds through NpgsqlParameter<T> (typed, no boxing on the generic path, no DbType, Npgsql infers the OID); the other dialects use the provider-agnostic CreateParameter() path with an explicit DbType and, for sized types, Size/Precision/Scale. Feature matrix Feature SQL Server PostgreSQL MySQL / MariaDB SQLite Identity return (-- @identity) OUTPUT INSERTED.<col> RETURNING <col> SELECT LAST_INSERT_ID() (narrowed) RETURNING <col> Upsert MERGE ... WITH (HOLDLOCK) ON CONFLICT (...) DO UPDATE ON DUPLICATE KEY UPDATE ON CONFLICT (...) DO UPDATE Sequences NEXT VALUE FOR <seq> nextval('<seq>') NEXTVAL(<seq>) (MariaDB only; real MySQL has no sequence object) not supported Bulk insert SqlBulkCopy binary COPY MySqlBulkCopy transactional prepared loop Identity return MySQL's LAST_INSERT_ID() returns BIGINT UNSIGNED; the generated code narrows it (checked) to the identity column's C# type. See -- @identity. Upsert Synthesised for tables that have a non-identity key to match on (primary key, else the first full-column secondary unique index). Identity-only keys with no such index skip upsert. A prefix unique (MySQL/MariaDB SUB_PART) never qualifies as the key, when it is the only candidate, synthesis is refused with JNT2019; an expression unique index is likewise never the key and always counts as a competing constraint for JNT2018. MySQL/MariaDB uses the legacy VALUES(col) form for broad compatibility, and its affected-row count is server semantics (1 insert, 2 update), do not assert == 1. SQLite uses the same ON CONFLICT form as PostgreSQL (SQLite 3.24+). See the bulk insert guide and sequences guide for the non-CRUD paths. Bulk insert Each dialect uses its provider's native bulk path where one exists; SQLite and any other provider fall back to a single-transaction prepared-command loop. MySQL's MySqlBulkCopy requires AllowLoadLocalInfile=true in the connection string and local_infile enabled server-side. Identity and rowversion columns are excluded from bulk insert on every dialect. Details in the bulk insert guide. Type mapping Type mapping is uniform across dialects, a database type name maps to the same C# type regardless of dialect, with a few dialect-specific inputs: C# type Representative DB types int / long int/int4/serial; bigint/int8/bigserial; SQLite integer string varchar, nvarchar, text, character varying, citext decimal decimal, numeric, money System.DateTime datetime, datetime2, timestamp[ without time zone] System.Guid SQL Server uniqueidentifier, PostgreSQL uuid byte[] varbinary, binary, image, bytea, blob byte[]? (concurrency token) SQL Server rowversion/timestamp T[] PostgreSQL array types (text[], integer[]), PostgreSQL only System.Net.IPAddress PostgreSQL inet, cidr string json, jsonb (PostgreSQL), the raw text, not a parsed document byte / short tinyint, dialect-dependent, see below bool / object bit, length-dependent, see below A database type with no mapping falls through to object and raises JNT2007; declare the intended type with -- @type or a -- @result shape. tinyint is dialect-aware. SQL Server's tinyint is unsigned 0–255 and the provider (Microsoft.Data.SqlClient) hands it back as System.Byte, so sqlserver maps it to byte. MySQL/MariaDB's tinyint is signed by default and round-trips through GetInt16 fine, so it keeps the historical short mapping - except tinyint(1) (including the BOOLEAN/BOOL DDL synonym, which the server desugars to tinyint(1)), which maps to bool instead, matching MySqlConnector's default \"Treat Tiny As Boolean\" behavior. bit(n) degrades to object when n > 1. SQL Server's BIT is always single-bit and maps to bool. MySQL and PostgreSQL both allow a multi-bit BIT(n), which neither MySqlConnector (returns ulong) nor Npgsql (returns BitArray) hands back as a bool, mapping it to bool would silently corrupt or throw at the generated cast, so a bit/bit[] column with a declared length greater than 1 maps to object/object[] instead. bit varying was already unmapped (no case matches the string) and stays that way. Dialect-specific notes PostgreSQL is the only dialect with array types and the typed NpgsqlParameter<T> path; it is therefore a compile-time dependency of projects using a postgres snapshot. SQL Server is the only dialect with rowversion-based optimistic concurrency and per-length parameter sizing for plan-cache stability. MySQL/MariaDB has no sequence objects and no rowversion; bulk insert needs LOCAL INFILE enabled on both ends. SQLite has no sequences and no rowversion; bulk insert uses the portable loop. INTEGER PRIMARY KEY is the identity mechanism."},{"u":"06-reference-directives.html","t":"Directives reference","s":"Reference","h":[{"i":"syntax-rules","t":"Syntax rules"},{"i":"directive-summary","t":"Directive summary"},{"i":"---first","t":"-- @first"},{"i":"---stream","t":"-- @stream"},{"i":"---identity","t":"-- @identity"},{"i":"---result","t":"-- @result"},{"i":"---params","t":"-- @params"},{"i":"---each","t":"-- @each"},{"i":"---type","t":"-- @type"},{"i":"---proc","t":"-- @proc"},{"i":"---call","t":"-- @call"},{"i":"---mirrors","t":"-- @mirrors"},{"i":"---allow-unindexed","t":"-- @allow-unindexed"},{"i":"---allow-sort","t":"-- @allow-sort"},{"i":"---allow-n-plus-one","t":"-- @allow-n-plus-one"},{"i":"related-diagnostics","t":"Related diagnostics"}],"b":"Directives reference Directives are -- comment lines placed at the top of a .sql file that control the shape of the code generated for that query. They are parsed out and stripped from the emitted CommandText; the SQL that runs never contains them. Syntax rules A directive is a line comment beginning -- @ followed by the keyword. Keywords are case-insensitive (-- @First == -- @first); type names and parameter names inside a directive are case-sensitive (they are C#). -- @type and -- @each may appear multiple times; the others appear at most once per file. Writing one of the others twice is reported as JNT3011 and is not an error: the later line wins, as it always has, and the warning names the value that was overwritten. Before JNT3011 this was silent, so a file could carry two -- @result lines, or two -- @allow-sort reasons, with nothing to show which one the build acted on. Plain -- comments without @ are left untouched. Directive summary Directive Applies to Effect -- @first SELECT Return Row? instead of List<Row>. -- @stream SELECT Return IEnumerable<Row> / IAsyncEnumerable<Row>, yielding lazily. -- @identity INSERT Return the database-assigned id, typed. -- @result any Declare the result type (void / named type / inline columns). -- @params any Declare parameter types the parser cannot infer. -- @each any Expand a parameter into a runtime IN-list. -- @type SELECT Declare the DB type of a projected expression. -- @proc SQL Server only Emit a stored-procedure definition and call it. -- @call any Bind to a procedure that already exists in the database. -- @mirrors any with a WHERE Declare that this query filters identically to another, and have the build check it. -- @allow-unindexed any with a WHERE Accept this query's unindexed filter columns deliberately. Reason mandatory. -- @allow-sort any with an ORDER BY Accept this query's runtime sort deliberately. Reason mandatory. -- @allow-n-plus-one any child point lookup by a foreign key Accept this query's N+1 shape deliberately. Reason mandatory. -- @first Marks a SELECT as returning a single row. The return type becomes Row? (async Task<Row?>) instead of List<Row>. Cannot combine with -- @stream (JNT3003). -- @first SELECT id, username, email FROM users WHERE username = @Username -- @stream Yields rows lazily straight off the reader: IEnumerable<Row> (sync) or IAsyncEnumerable<Row> (async), never buffering into a list. Keeps memory constant over large result sets. SELECT-only; cannot combine with -- @first or -- @proc (JNT3003). -- @stream SELECT ProductId, ProductName FROM Products ORDER BY ProductId -- @identity On an INSERT, returns the database-assigned identity value (typed to the column) instead of the affected-row count. The emitted SQL is dialect-native: OUTPUT INSERTED.<col> (SQL Server), RETURNING <col> (PostgreSQL / SQLite), or SELECT LAST_INSERT_ID() narrowed to the column's type (MySQL). Requires an INSERT statement, exactly one identity column on the target table, and a dialect in the snapshot; violations are JNT7001. Cannot combine with a hand-written RETURNING clause or -- @proc. Auto-CRUD Insert applies this automatically on tables with a single identity column. -- @identity INSERT INTO articles (slug, title, body, author_id) VALUES (@Slug, @Title, @Body, @AuthorId) -- @result Declares the result shape explicitly. Three forms: -- @result void, no result set; the method returns int (row count). Use for INSERT/UPDATE/DELETE. -- @result TypeName, materialize rows into an existing type instead of a generated POCO. May be fully qualified. -- @result (int Id, string Name), inline column list; generates a query-specific DTO. Names must match the SELECT aliases. -- @result void DELETE FROM products WHERE product_id = @ProductId -- @params Declares parameter types the parser cannot infer from context (for example a parameter used only inside a subquery or expression). Comma-separated Name:Type pairs; types may be nullable. An uninferrable parameter is a build error (JNT4003), not a silent object fallback, the message tells you exactly which -- @params line to add. -- @params CategoryId:int, MinPrice:decimal, Name:string? SELECT p.product_id, p.product_name FROM products p WHERE p.category_id = @CategoryId AND p.unit_price >= @MinPrice -- @each Marks a parameter for runtime IN-list expansion. The C# parameter becomes IReadOnlyList<T>, and each @Name occurrence in the SQL is expanded to @Name0, @Name1, ... at call time. Repeatable. SELECT-only, and cannot combine with -- @proc (JNT3003 otherwise). -- @each Ids SELECT ProductId, ProductName FROM Products WHERE ProductId IN (@Ids) An empty list short-circuits to an empty result before any connection is opened (IN () is invalid SQL everywhere). An oversize list fails fast with ArgumentException at the dialect's per-command parameter budget, 2,000 elements on SQL Server (server limit ~2,100 parameters per request), 32,000 on SQLite, 65,000 on PostgreSQL/MySQL, instead of an obscure provider error; batch larger workloads into chunks. Note that on SQL Server every distinct list length compiles a distinct query plan, so very high-cardinality IN-lists also pollute the plan cache, a table-valued parameter or staging table is the better tool at that scale. -- @type Declares the database type of a projected expression the generator cannot infer from the schema (aggregates, function results, computed columns). Syntax is -- @type <alias> <dbtype>, where <alias> matches the SELECT alias and <dbtype> (which may contain spaces) is resolved per dialect. Repeatable. -- @type is an assertion, not a coercion. It only tells the generator what C# type to bind the reader call to, it does not cast or convert the value the database actually returns. If the declared type doesn't match what the expression evaluates to at runtime (e.g. asserting double precision on an integer division that the database evaluates as an integer), you get an InvalidCastException at read time, not a build error. Where the mismatch is avoidable, fix the SQL itself (an explicit CAST) rather than relying on -- @type to paper over it. -- @type payment_count int -- @type total_amount numeric SELECT st.staff_id, COUNT(p.payment_id) AS payment_count, SUM(p.amount) AS total_amount FROM staff st JOIN payment p ON p.staff_id = st.staff_id GROUP BY st.staff_id -- @proc Emits a stored-procedure definition (a CREATE OR ALTER PROCEDURE constant on a nested Proc class) and generates the call with CommandType.StoredProcedure. With no argument the procedure name is inferred from entity + method; an argument overrides it and must be a valid C# identifier (JNT2004 otherwise). Cannot combine with -- @stream or -- @identity. Restricted to the sqlserver dialect (JNT7002 on any other dialect): CREATE OR ALTER PROCEDURE and T-SQL parameter types have no equivalent on Postgres/MySQL, and SQLite has no stored procedures at all. -- @proc usp_GetProducts SELECT product_id, product_name FROM products -- @call Binds the file to a stored procedure that already exists in the database. The file carries no SQL body; parameters and result columns come from the procedures section of the snapshot (captured by jauntyq schema pull). Emits a CommandType.StoredProcedure call with typed IN parameters (OUT/INOUT become C# out/ref), returning List<Result> for row-returning procedures or int otherwise. An unknown procedure name is JNT2005. PostgreSQL: the int returned for a non-row-returning procedure is always -1, unconditionally. Postgres's CALL protocol reports only a bare completion tag for a procedure invocation, never an affected-row count, regardless of how the procedure is authored, a structural PostgreSQL/Npgsql limitation with no ADO.NET-level workaround. Do not rely on this value to detect whether a Postgres procedure's side effects occurred; the generated method's own doc comment repeats this caveat at the call site. (MySQL reports a real count for the identical call shape. SQL Server usually does too, but not unconditionally, a bound procedure using SET NOCOUNT ON, a common T-SQL pattern, also reports -1; this is pre-existing general ADO.NET/T-SQL behavior, not something JauntyQ can control for a procedure it doesn't author, so it isn't called out per-call the way Postgres's unconditional case is.) -- @call GetProductsByCategory -- @mirrors Declares that this query's WHERE clause must match another query's, and makes the build check it (JNT8011). The case it exists for is a paginated list and the count that sizes its pager: when one gains a filter the other lacks, the page contents stay correct while the total is wrong, and nothing surfaces it. -- @mirrors ListPage SELECT count(*) AS total FROM bookmarks WHERE bookmarks.user_id = @userId AND bookmarks.deleted_at IS NULL The value is a method name resolved across the whole query corpus, or a qualified Entity.Method when two entities have a method of the same name. Put it on either side of the pair; only the file that carries it is checked, so one directive per pair is enough. What is compared. The WHERE clause of the statement and of every CTE body, unioned, split at top-level AND into atoms and compared as a set. Column references are resolved against the schema first, so b.user_id and a bare user_id match; keyword casing and atom order do not matter. The CTE union is what makes the idiomatic paginate-inside-a-CTE list comparable with a flat count. ORDER BY, projection and LIMIT/OFFSET are not compared, a count query has none of them. Deliberate conservatisms. A top-level OR makes the whole clause one atom rather than being split into conjuncts; BETWEEN's own AND is not a splitter; and the comparison is textual after resolution, so two predicates that mean the same thing written differently (a >= @x vs @x <= a) read as drift. Write the mirrored clause the same way on both sides, or drop the directive. It reports rather than guesses. A pairing it cannot compare, an unknown or ambiguous target, a target that failed to compile, or a WHERE column that does not resolve to a schema column (a CTE virtual column, say), is JNT3010, not silence. Falling back to raw text would invent drift out of a spelling difference, and one false positive kills an opt-in check. -- @allow-unindexed Accepts this query's unindexed filter columns deliberately, suppressing JNT8004 for this query and nothing else. A reason is required. -- @allow-unindexed status index ships in migration 0042 SELECT messages.id, messages.subject FROM messages WHERE messages.status = @status Why it exists. JNT8004 reads the schema snapshot, and the snapshot is advanced by running a migration against a live database and re-pulling. Without this directive the only way past the rule is to change the world: an ordinary WHERE status = @status stops the build until someone applies a migration, and a fresh clone cannot build a branch whose migration has not been applied anywhere. The rule is right; needing a database to satisfy it is not. The reason is mandatory. A bare -- @allow-unindexed suppresses nothing and is reported as JNT3008, like every other value-taking directive written bare. The reason is the whole point: it puts the decision in the query file, where it shows up in the diff and can be argued with at review time. A suppression whose justification is not written down is a NoWarn entry with extra steps. It suppresses only JNT8004. Every other diagnostic the query would raise still fires. This is an accepted-scan marker, not a silencer. A suppression that suppresses nothing is reported (JNT8012). Once the index lands, the directive stops hiding an accepted scan and starts hiding a future regression, so the build asks for it to be removed. Any single suppressed column keeps the directive live: a query filtering on one indexed and one unindexed column is correct use and stays quiet. Accepting a scan in an auto-CRUD query. Auto-CRUD synthesizes a GetBy<Fk> loader for every foreign-key column, and on engines that do not index foreign keys automatically (SQLite, unlike InnoDB) those loaders scan. The directive needs a file, and a synthesized query has none, so JNT8004 on a generated method names the file that would claim its slot: warning JNT8004: No index covers address.city_id used as a filter/join key: this query scans. Add an index or filter on an indexed column. 'Address.GetByCityId' is generated by auto-CRUD, so there is no file carrying it to annotate. To accept this scan deliberately, create '<your query root>/Address/GetByCityId.sql' containing '-- @allow-unindexed <reason>' followed by: SELECT address_id, ... FROM address WHERE address.city_id = @city_id -- a hand-written file always overrides the generated method of the same name. That file is then yours: it no longer tracks schema changes, ... (The real message never elides the SQL, the full column list is what makes it paste-able. It is condensed here only for the page.) Creating that file overrides the generated method: a hand-written .sql always wins the entity.method slot, and the directive then applies normally, JNT3008 and JNT8012 included. The copy stops tracking the snapshot. Once the file exists it is yours: a column added upstream will not appear in its SELECT, and no diagnostic says so, JNT8012 wakes when the scan stops being real (someone indexes the column), not when the projection drifts. For the audience this route is aimed at, an upstream-owned schema nobody in the building controls, drift is the normal condition rather than the edge case, so this is the cost of the acceptance and not a footnote to it. <JauntyQAutoCrud>false<\/JauntyQAutoCrud> is the blunt alternative and removes auto-CRUD for every table. Or accept it without taking ownership of the SQL. The override file is the right answer when you wanted to hand-write that query anyway; when you only wanted the warning to stop, paying for it with a copy that no longer tracks the snapshot is a poor trade. A *.accept.json sidecar accepts a generated scan per column and keeps the generated loader, the acceptance sidecar has the shape and the rules. The message above names it too. It covers generated queries only: a hand-written query filtering an accepted column still raises JNT8004, because that query has a file and the file can carry the directive. -- @allow-sort Accepts this query's runtime sort deliberately, suppressing JNT8007 for this query and nothing else. A reason is required. -- @allow-sort 20 rows a page, the sort never sees more than a screenful SELECT articles.id, articles.title FROM articles ORDER BY articles.created_at DESC, articles.id Why it is a separate directive. JNT8007 is the sort half of the same question -- @allow-unindexed answers for filters, and the obvious move is to let one directive cover both. It is the wrong move, and the reason is the JNT8012 remedy: remove the directive. Under a single shared directive that advice goes wrong the moment a query has both an unindexed filter and an unindexed sort and only one of them gets an index, the directive still suppresses the other, so JNT8012 never fires and the stale half lives on forever. Two directives each expire on their own evidence. Everything else matches its sibling exactly. A bare -- @allow-sort suppresses nothing and is reported as JNT3008. Every other diagnostic the query raises still fires, JNT8009 and JNT8010 included, accepting a sort is not accepting an unstable one. Once an index can serve the sort, the directive is reported as unnecessary (JNT8012), naming itself so a query carrying both directives says which one to remove. What \"an index can serve the sort\" means is the same predicate JNT8004 uses (IsColumnIndexSupported): the column is the primary key, or leads some index, or sits in a composite index every more-leading column of which this query constrains with a bound parameter. Note constrains, not filters by equality, the set that predicate consults is every bound filter/join column regardless of comparison operator, so WHERE author_id > @since ORDER BY created_at against an index on (author_id, created_at) is treated as served. A range on the leading column does not in fact deliver the trailing column in order, so that case is a known false negative rather than a guarantee. Each ORDER BY item is tested on its own. There is no multi-column prefix logic: ORDER BY a, b against an index on (a, b) clears a, and clears b only if a is also constrained by a parameter, or b is the primary key. A two-column sort whose tiebreak is the primary key, the common ORDER BY created_at DESC, id shape, therefore passes on the tiebreak's own merit, not because the index covers the pair. Sort direction is invisible to this check. IndexSchema.Columns is a list of names with no ASC/DESC, and the parser discards the direction of each ORDER BY item, so JNT8007 cannot tell (a, b) from (a DESC, b). The engine can: PostgreSQL serves ORDER BY a DESC, b from (a, b) with a backward scan plus an incremental sort for the tiebreak, and from (a DESC, b) with no sort at all. When ties on a are common that difference is real and no JauntyQ diagnostic will point at it. -- @allow-n-plus-one Accepts this query's N+1 access pattern deliberately, suppressing JNT8008 for this query and nothing else. A reason is required. -- @allow-n-plus-one one customer at a time on the service desk screen, never called per row of a list SELECT rental.rental_id, rental.rental_date FROM rental WHERE rental.customer_id = @CustomerId Why it exists. Unlike its siblings, JNT8008 already has a suppression route: it is an ordinary Roslyn diagnostic, so <NoWarn>$(NoWarn);JNT8008<\/NoWarn> or dotnet_diagnostic.JNT8008.severity = none turns it off. That route is project- or file-wide and carries no reason in the diff, which is the wrong shape for this rule: JNT8008 is a corpus-level heuristic, so the query it misjudges is usually one query among many the rule gets right, and switching the whole code off to accept that one takes the rest of the corpus down with it. This directive is the per-query alternative, with the justification recorded where the decision was made. The reason is mandatory. A bare -- @allow-n-plus-one suppresses nothing and is reported as JNT3008, like every other value-taking directive written bare. The reason is the whole point: it puts the decision in the query file, where it shows up in the diff and can be argued with at review time. A suppression whose justification is not written down is a NoWarn entry with extra steps. It suppresses only JNT8008. Every other diagnostic the query would raise still fires, JNT8004 included, accepting a per-row lookup is not accepting a scan on top of it. A suppression that suppresses nothing is reported (JNT8013, not JNT8012, which is its siblings' code). Once the pairing that justified the directive is gone, because the parent-collection query was deleted or the child lookup was made set-based, the directive stops hiding an accepted pattern and starts hiding a future one, so the build asks for it to be removed. The message repeats the stated reason. Related diagnostics JNT3003 (invalid directive combination), JNT7001 (identity unavailable), JNT7002 (construct unavailable on dialect, -- @proc outside sqlserver), JNT4003 (parameter type unresolved), JNT2004 (illegal identifier), JNT2005 (procedure not found), JNT3006 (unknown @type alias), JNT8004 (unindexed filter column), JNT8012 (unnecessary unindexed acceptance), JNT8013 (unnecessary N+1 acceptance). Full list in diagnostics."},{"u":"06-reference-mutation-coverage-report.html","t":"Mutation coverage report — Extrode.JauntyQ.Analysis / SqlParser","s":"Reference","h":[{"i":"score-history","t":"Score history"},{"i":"current-per-file-breakdown-as-of-9584-whole-project-re-run-2026-09-20-1905-1909-confirming-the-readobjectname-fix","t":"Current per-file breakdown (as of 95.84%, whole-project re-run 2026-09-20 19:05-19:09, confirming the ReadObjectName fix)"},{"i":"mutation-coverage-test-files-added-this-effort","t":"Mutation-coverage test files added this effort"},{"i":"remaining-gap-to-96","t":"Remaining gap to 96%"},{"i":"migrationparsercs--per-mutant-pass-2026-09-20-8702--8978","t":"MigrationParser.cs — per-mutant pass, 2026-09-20 (87.02% → 89.78%)"},{"i":"migrationparsercs--readobjectname-bracket-quoted-dot-correction-2026-09-20-8978-scoped--9584-whole-project","t":"MigrationParser.cs — ReadObjectName bracket-quoted-dot correction, 2026-09-20 (89.78% scoped → 95.84% whole-project)"},{"i":"post-96-survivor-pass-2026-09-20--small-files","t":"Post-96% survivor pass, 2026-09-20 — small files"},{"i":"post-96-survivor-pass-2026-09-20--migrationparsercs-full-sweep","t":"Post-96% survivor pass, 2026-09-20 — MigrationParser.cs full sweep"},{"i":"adversarial-fable-verify-pass-over-the-post-96-survivor-claims-2026-09-20","t":"Adversarial fable-verify pass over the post-96% survivor claims, 2026-09-20"},{"i":"2026-09-21-pass-genuine-fable-model-verify--malformed-sql-survivor-category-9647--9723","t":"2026-09-21 pass: genuine fable-model verify + malformed-SQL survivor category (96.47% → 97.23%)"},{"i":"5-confirmed-real-gaps-5deae22","t":"5 confirmed real gaps (5deae22)"},{"i":"malformed-sql-category-resolved-a9603e4","t":"Malformed-SQL category resolved (a9603e4)"},{"i":"whole-project-re-run-0eee118","t":"Whole-project re-run (0eee118)"},{"i":"2026-09-21-pass-stryker-comment-based-exclusions-for-confirmed-equivalent-survivors-9723--9914","t":"2026-09-21 pass: Stryker comment-based exclusions for confirmed-equivalent survivors (97.23% → 99.14%)"},{"i":"whole-project-re-run","t":"Whole-project re-run"},{"i":"extrodejauntyqsqlparser--baseline","t":"Extrode.JauntyQ.SqlParser — baseline"},{"i":"extrodejauntyqsqlparser--parser-core-pass-sqlparsercs-part467","t":"Extrode.JauntyQ.SqlParser — parser-core pass (SqlParser.cs, Part4/6/7)"},{"i":"extrodejauntyqsqlparser--ir-model-types-pass-easiest-win-done","t":"Extrode.JauntyQ.SqlParser — IR model types pass (easiest win, done)"},{"i":"2026-09-21-push-fresh-baseline--full-file-by-file-pass-toward-96-7698--8853","t":"2026-09-21 push: fresh baseline + full file-by-file pass toward 96% (76.98% → 88.53%)"}],"b":"Mutation coverage report — Extrode.JauntyQ.Analysis / SqlParser Living record of Stryker.NET mutation-testing results for the two src/ assemblies that carry a Stryker config: Extrode.JauntyQ.Analysis and Extrode.JauntyQ.SqlParser. Updated after each coverage-raising pass. Equivalent-mutant reasoning and per-mutant detail live in docs/handoffs/2026-09-19-stryker-mutation-gaps.md; this file tracks the numbers over time. Score formula: (Killed + Timeout) / (Killed + Timeout + Survived + NoCoverage). Score history Date Milestone Score Commit 2026-09-19 (earlier session) Baseline before this effort 32.52% — 2026-09-19 (earlier session) Reserved-word tests + misplaced-test-file relocations 81.51% 98af6f3 2026-09-19 7-file targeted plan complete (DialectMapper, ReferencedObjects, UpsertKeyResolver, AutoCrud, SchemaSimulator, ImpactClassifier, MigrationParser 1st pass) 91.89% 9de5f4e 2026-09-19 MigrationParser.cs 2nd pass 92.98% e567844 2026-09-19 MigrationParser.cs — SkipParenGroup/ApplyFacets follow-up 93.78% 75dfcf9 2026-09-20 6 previously out-of-scope small files raised 94.33% 951a120 2026-09-20 Round 4 (4 remaining sub-96% files) 94.50% 3535b13 2026-09-20 Near-target cleanup (UpsertKeyResolver/AutoCrud/DialectMapper/DialectReservedWords) 94.54% 1dd7886 2026-09-20 SqlParser baseline run (first ever) 59.22% 209db2d 2026-09-20 SqlParser — 6 zero-coverage IR model files closed 59.65% 6763087 2026-09-20 fable-verify pass: fixed 3 mislabeled-equivalent survivors (DialectMapper/DialectReservedWords) 94.66% 6059d24 2026-09-20 SqlParser — SqlParser.cs/Part4/6/7.cs parser-core pass (76 new tests) (SqlParser whole-project re-run pending — see note below) 91e4c04 2026-09-20 MigrationParser.cs — per-mutant pass (12 new tests, 7 real gaps) 89.78% (scoped; whole-project re-run pending) 3938a48 2026-09-20 Whole-project re-run confirming the per-mutant pass 95.55% (pre-4b5c154) 2026-09-20 MigrationParser.cs — ReadObjectName bracket-quoted-dot merge correction + boundary test 95.84% c9fa8c1 2026-09-20 Adversarial fable-verify pass over pos <= def.Count/SkipParenGroup equivalence claims + MigrationParser.cs DEFAULT-value Negate mutant fix (line 689, flagged by the pass) 95.97% ac22f74 2026-09-20 MigrationParser.cs GENERATED-column Negate mutant fix (line 704, found by manual re-check after the fable-verify pass) 96.01% — target reached (merge into dev after 34c070b) 2026-09-20 Post-96% survivor pass: 1 real gap in ReferencedColumnComparer.Equals + 6 real gaps in MigrationParser.cs (91→84 survived) 96.30% 703192c, f8e2c18 2026-09-20 Adversarial fable-verify pass over the 84 remaining survivors: 0/18 small-file claims refuted, 3/60 MigrationParser.cs claims refuted (6 mutants, 2 correcting wrong claims) 96.47% 9dba99b, cca01e6 2026-09-21 Genuine fable-model verify pass: 5 real gaps fixed in MigrationParser.cs 96.47% → higher (scoped fixes, whole-project re-run below) 5deae22 2026-09-21 Malformed-SQL survivor category resolved: 6 mutants killed with a nested-paren test, 3 confirmed genuinely equivalent (outer-loop IDENTITY-branch fallback) — a9603e4 2026-09-21 Whole-project re-run confirming both passes above 97.23% 0eee118 2026-09-21 Stryker comment-based exclusions for 47 confirmed-equivalent survivors across 7 files 99.14% bb177ce 2026-09-21 SqlParser: fresh whole-project baseline (supersedes stale 59.22%/59.65%/73.69% mixed-date figures) 76.98% (baseline run only) 2026-09-21 SqlParser: full push across all 10 files (3 tiny IR files to 100%, 7 parser-body files real-test + equivalence passes) 88.53% 0b8b78b (dev HEAD) Current per-file breakdown (as of 95.84%, whole-project re-run 2026-09-20 19:05-19:09, confirming the ReadObjectName fix) 19 source files, 2380 mutants tested (2281 killed+timeout, 95 survived, 4 no-coverage). Superseded by the 96.01% whole-project run, 2026-09-20 20:12-20:16 (2285 killed+timeout, 91 survived, 4 no-coverage, 2380 total) — the two fixes below (lines 689 and 704) moved 4 mutants from Survived to Killed; Stryker's coverage-based test-impact selection also shifted 2 mutants between the Killed and Timeout buckets run-to-run (694 killed / 33 timeout in the prior run's MigrationParser.cs row vs. this run's totals), which is normal run-to-run noise in timeout classification, not a regression. No fresh file-scoped MigrationParser.cs run has been taken at 96.01%, so the per-file breakdown below is not re-stated row-by-row; the totals above are the authoritative current numbers. Score Killed Timeout Survived NoCov Total File 90.00% 9 0 1 0 10 Impact/MigrationImpactReport.cs 90.38% 45 0 5 2 52 Impact/ReferencedObjects.cs 91.86% (→ was 89.78% scoped) 659 33 70 1 763 Migrations/MigrationParser.cs 94.17% 194 0 12 0 206 Migrations/SchemaSimulator.cs 96.25% 77 0 3 0 80 UpsertKeyResolver.cs 98.60% 141 0 1 1 143 AutoCrud.cs 99.36% 307 3 2 0 312 DialectMapper.cs 99.84% 627 4 1 0 632 DialectReservedWords.cs 100.00% 1 0 0 0 1 AnalysisDiagnostic.cs 100.00% 27 0 0 0 27 CrudColumnRules.cs 100.00% 15 0 0 0 15 Diff/SchemaDelta.cs 100.00% 44 0 0 0 44 Diff/StructuralSchemaDiff.cs 100.00% 15 0 0 0 15 EntityNameResolver.cs 100.00% 4 0 0 0 4 Impact/Classification.cs 100.00% 66 0 0 0 66 Impact/ImpactClassifier.cs 100.00% 3 0 0 0 3 Impact/ImpactEntry.cs 100.00% 4 0 0 0 4 Impact/ImpactReason.cs 100.00% 1 0 0 0 1 Impact/QueryImpactInput.cs 100.00% 2 0 0 0 2 Migrations/MigrationStatement.cs Mutation-coverage test files added this effort Test file Target file Score reached Commit AutoCrudSynthesizeTests.cs (strengthened) AutoCrud.cs 97.90% → 98.60% 7c554e0, 6e4c5d9 ImpactClassifierTests.cs (strengthened) Impact/ImpactClassifier.cs 100.00% 875ac78 SchemaSimulatorMutationCoverageTests.cs Migrations/SchemaSimulator.cs 94.17% bd11450 DialectMapperMutationCoverageTests.cs DialectMapper.cs 98.72% → 99.36% 147e904, 6059d24 ReferencedObjectsMutationCoverageTests.cs Impact/ReferencedObjects.cs 86.54% e6a75c3 UpsertKeyResolverMutationCoverageTests.cs UpsertKeyResolver.cs 96.25% bf40c03 MigrationParserMutationCoverageTests.cs (created) Migrations/MigrationParser.cs 84.0% → 87.02% 23e129c, 04f7b1f, 75dfcf9, 091f6a9 MigrationParserMutationCoverageTests.cs (12 more tests) Migrations/MigrationParser.cs 87.02% → 89.78% 3938a48 SmallModelTypesMutationCoverageTests.cs MigrationStatement.cs, ImpactReason.cs, ImpactEntry.cs, MigrationImpactReport.cs, Classification.cs, SchemaDelta.cs 90–100% 951a120 DialectReservedWordsTests.cs (earlier session, strengthened) DialectReservedWords.cs 99.68% → 99.84% 7a08a4b, 6059d24 Also relocated from Generator.Tests to Analysis.Tests (earlier session): DialectMapperTests.cs (ae9b086), MigrationParserTests.cs (afb9fad, 9f34577). Remaining gap to 96% As of 94.54%, 4 files remain below 96%: ReferencedObjects.cs (86.54%), MigrationParser.cs (87.02%), MigrationImpactReport.cs (90.00%), and SchemaSimulator.cs (94.17%). The round-4 pass (091f6a9, 555dda3, merged 3535b13) fixed 4 more MigrationParser.cs mutants and rigorously re-verified the other three files, concluding: SchemaSimulator.cs is fully closed at 94.17% — all 12 remaining survivors proven equivalent via the TryFindTable/TryFindColumnKey/ TryFindColumn \"null-iff-false\" contract (traced through every helper and call site). No further test can move this file without a source change. ReferencedObjects.cs and MigrationImpactReport.cs were re-verified with no new findings — both remain at their documented-equivalent floors. MigrationParser.cs still has ~91 survivors and is the only file with real remaining headroom, but most now fall under one of two absorption mechanisms (the \"unknown flag, skip one token\" fallback, generalized this round to cover positional-arithmetic facet-parsing mutations too, not just flag-keyword literals) — further gains need slow per-mutant manual-mutation verification, not another broad sweep. A follow-up \"near-target cleanup\" pass (6e4c5d9, merged 1dd7886) pushed the four files that were already ≥96% toward their own ceilings instead: AutoCrud.cs 97.90% → 98.60%: the one real fix was a List<T> capacity arithmetic mutant (pkCols.Count + versionCols.Count → - versionCols.Count) killed with a table carrying more RowVersion columns than PK columns (negative capacity would otherwise throw). Its remaining break;-removal survivor is equivalent — the loop's result list is discarded on the failure path regardless of whether the loop breaks early or keeps iterating. UpsertKeyResolver.cs (96.25%), DialectMapper.cs (98.72%), and DialectReservedWords.cs (99.68%) did not move — all 9 remaining survivors across the three were proven equivalent by direct code-flow tracing (documented in the handoff doc): mostly break;/continue; removals whose surrounding loop already discards its accumulated state on the same path, plus two NormalizeDbType/StripMySqlUnsignedModifier boundary conditions (index == 0) that are reachable only by strings that can never match a real dictionary key either way, and a ||→&& guard mutation in DialectReservedWords whose \"protected\" branches already fall through to false on their own. These three files are now considered closed at their current scores — no further test-writing can move them. Net: the 96% target is likely not fully reachable through more test-writing alone; the remaining gap is dominated by a confirmed equivalent-mutant floor in 6 of the 7 files below 100% (all but MigrationParser.cs), which remains the sole file where further (slow) work could still move the needle. Correction, 2026-09-20 (fable-verify pass). The \"did not move\" / \"closed at their current scores\" claim two paragraphs up turned out to be wrong for 2 of the 3 files it names. An adversarial (fable) verify pass, instructed to try to break each equivalence claim rather than confirm it, found real, killable gaps in DialectMapper.cs (2 mutants) and DialectReservedWords.cs (1 mutant) — both boundary/guard conditions whose equivalence reasoning missed a code path (see the handoff doc's fable-verify update for the full per-mutant trace). Fixed in branch test/fix-mislabeled-equivalents (6059d24): DialectMapper.cs 98.72% → 99.36%, DialectReservedWords.cs 99.68% → 99.84%. UpsertKeyResolver.cs's equivalence claim held under the same adversarial pass. The \"6 of 7 files\" count in the Net paragraph above is accordingly one file too many as originally stated — the corrected, per-mutant-traced state for all 7 sub-100% files is in the current per-file breakdown table above this section, not this historical paragraph. Final update: the 96% target was reached, 2026-09-20 20:12-20:16 — 96.01%. Two more genuine gaps were found in MigrationParser.cs after this section was written (line 689 and line 704, both documented further down this file), closing the last 1-mutant gap. The \"likely not fully reachable through more test-writing alone\" conclusion above turned out to be wrong — the target was reached with real tests, not source changes or contrived tests. See the score-history table at the top of this document for the full path from 94.66% to 96.01%. MigrationParser.cs — per-mutant pass, 2026-09-20 (87.02% → 89.78%) Went through the ~91 survivors from the prior baseline one by one (or in small related batches): read the exact mutated line, tried to construct a concrete distinguishing SQL input, wrote and verified a test against both the real source and a hand-mutated copy where a test's result was ever in doubt. 12 new tests added to MigrationParserMutationCoverageTests.cs, all 44 tests in that file still pass. Result: Killed 632→652 (+20, since several tests each kill 2-3 related mutants sharing one root cause), Survived 91→70, NoCoverage unchanged at 8, scoped score 87.02%→89.78%. 7 real, killable gaps found and fixed (mutated line, mechanism, killed via): Line 502/745 — SkipParenGroup call removed/no-op in the AS-shorthand computed-column path (Total AS (Qty * Price)). Without the call, the parenthesized expression's tokens leak into the flags loop one at a time. Killed with a computed column whose expression contains a token that textually matches a flag keyword. Line 675 (DEFAULT keyword-literal blanking) — breaks the atomic default-expression skip; the DEFAULT keyword becomes an unrecognized token and its expression's tokens leak into the flags loop instead of being atomically skipped. Line 447 (ADD/DROP GENERATED|IDENTITY arms of an OR-chain) — only the SET arm had a test previously; added two tests covering ADD and DROP. Line 626 (bare NULL flag-check blanking) — specifically the \"flip PK-forced nullability back to true\" case, not just the ordinary nullable-flag case already covered. Line 640 (bare AUTOINCREMENT/SERIAL keyword flags) — distinct from the SERIAL DbType-sugar path; two new tests, one per keyword. Line 647 (identity-seed-paren closing-) detection) — specifically the case where a real closing paren IS present and is followed by more flags, distinguished from the already-tested genuinely-unterminated case (which produces the same observable result either way). Correction, 2026-09-20 (fable-verify pass): this test does not actually kill the <= boundary mutant it was written for — per Is/IsSymbol's own bounds-checking (lines 792-799), that mutant is genuinely equivalent. The test's real (and only) kill power is against an unrelated &&→|| Timeout mutant on the same line. The test's comment in MigrationParserMutationCoverageTests.cs was corrected to state this honestly rather than the original overclaim; this is the one item of the 7 below that is not, as originally listed, a \"real, killable gap.\" Line 815-818 (ReadObjectName's multi-level dotted-name while-loop, previously NoCoverage) — required bracket-quoted syntax ([dbo].[Gadgets]) to trigger the tokenizer-split path at all; unbracketed dotted names tokenize as one compound identifier handled entirely by BareName, never reaching this loop. Line 851 (SplitTopLevel's comma-detection, &&→|| mutation) — at paren-depth 0, the mutated condition treats any Symbol token (not just ,) as a column-def separator. A first attempt using create table t (a int default -1, b int) did NOT distinguish the mutant: the spurious split at - produces an orphaned [1] fragment whose leading token is a Number, which ParseColumnDef's def[0].Type != Identifier guard silently filters — column count/names came out identical either way. The mutant-killing input needed a flag after the negative default (a int default -1 not null, b int): under the mutation, NOT NULL ends up inside that same orphaned/filtered fragment and is lost, so column a wrongly reports IsNullable == true. This is a concrete instance of the \"always empirically verify a survived test actually distinguishes the mutant — don't stop at 'looks plausible'\" lesson from this campaign; see the coverage-misattribution caveat below for the matching Stryker-side version of the same lesson. No new equivalent-mutant classes were confirmed this round beyond what was already documented in the handoff doc — the ~70 remaining survivors were individually re-traced (not just template-matched against existing categories) and fall entirely under the previously-established mechanisms: unknown-flag/positional-arithmetic absorption in the flags loop (lines 558-751 region, including the numeric-facet block and the GENERATED ALWAYS/BY DEFAULT/AS IDENTITY vs. AS (expr) STORED/VIRTUAL block), bounds-safety via Is/IsSymbol (lines 618-748 loop-bound comparisons), dead/unreachable branches (SkipParenGroup's unreachable guard, line 798's index > 0 in a helper only ever called with index >= 0 from a caller that already special-cases the zero case upstream), discarded return values (SplitTopLevel/SplitRemaining's exact final pos, lines 845/862/886/ 898-899 — never re-read by the sole caller in either method), and algebraic equivalence (line 805's dot >= 0 ? Substring(dot+1) : name). Time did not allow writing out a fresh per-line justification for every one of the ~70 in this doc; the categories above are the same ones already itemized with specific line numbers in the handoff doc's MigrationParser section, and this pass's spot checks did not surface any case where the category assignment was wrong. Open methodological caveat — Stryker coverage misattribution. During this pass, Stryker's own scoped report listed the line-506 String mutations (\"NOT\"→\"\" and \"NULL\"→\"\") as \"Survived\" in two separate runs, but direct hand-mutation + dotnet test --filter against the real test suite proved both are already killed by the pre-existing test ComputedColumn_JunkTokenAfterAnExplicitNotNull_DoesNotFlipNullabilityBack. Attempts to fix this via --coverage-analysis all (unrecognized CLI option, silently no-ops) and via \"coverage-analysis\": \"all\" in stryker-config.json (accepted without error, but the run log still showed 'SkipUncoveredMutants'/'CoverageBasedTest' mode and produced identical counts) did not resolve it. This means Stryker's own survivor counts for this file may already include false positives beyond the two confirmed here — a caveat for whoever picks up the remaining ~70, not something resolved in this pass. Ground truth for any individual mutant should be re-verified by hand-mutation + targeted dotnet test --filter, not taken on Stryker's report alone. MigrationParser.cs — ReadObjectName bracket-quoted-dot correction, 2026-09-20 (89.78% scoped → 95.84% whole-project) Whole-project re-run after the 89.78% scoped pass landed at 95.55% (2241 killed, 33 timeout, 95 survived, 11 no-coverage) — an exact match for the pre-computed estimate above, confirming that number. Investigating the file's 8 NoCoverage mutants found the existing test CreateTable_BracketQuotedSchemaAndTableName_Tokenizer... was asserting a false premise: its comment claimed [dbo].[Gadgets] exercises ReadObjectName's dotted-name while-loop (lines 815-818), but tracing SqlTokenizer.MergeQualifiedIdentifiers proved the tokenizer actually merges [dbo].[Gadgets] into one Identifier token before the loop ever runs (it only leaves an Identifier '.' Identifier sequence unmerged when the left segment's own value already contains a literal dot and isn't a chain continuation) — the test passed, but not for the reason its comment claimed, and the loop itself stayed uncovered. Fixed in test/migrationparser-readobjectname-boundary (4b5c154, merged c9fa8c1): renamed the test to CreateTable_BracketQuotedSchemaAndTableName_TokenizerMergesToOneIdentifier with a corrected comment, and added two new tests exploiting the one input shape that actually reaches the while-loop — a bracket-quoted segment containing a literal internal dot ([db.with.dots].[Gadgets]): CreateTable_BracketQuotedNameContainingLiteralDot_TokenizerLeavesUnmergedForReadObjectNamesLoop — kills the loop's real body mutants and documents the line-815 tokens[pos + 1] → tokens[pos - 1] mutant as equivalent (at that check, pos is always one past a just-consumed Identifier, so tokens[pos - 1] is always of type Identifier too — same boolean result as the correct check, whenever the loop's other conditions hold). CreateTable_TrailingDotWithNoFollowingIdentifier_DoesNotOverrunTheTokenArray — guards the loop's pos + 1 < tokens.Count bound with a truncated input (create table [db.with.dots].) so the unmerged trailing . is the last token; without the bound, the inner tokens[pos + 1] read would index past the end of the array. Result: whole-project Killed 2241→2248 (+7), NoCoverage 11→4, Survived flat at 95 (6 of the file's 8 no-coverage mutants killed outright, the other 2 converted to 1 killed + 1 confirmed-equivalent, per the file's own Survived 70 / NoCov 1 split above). Whole-project score: 95.55% → 95.84%. Full Analysis suite: 1431/1431 on both net8.0 and net10.0. Why 96% is likely not reachable through more test-writing. 96% needs Survived + NoCoverage ≤ 95 project-wide; the count is currently 99, so 4 more mutants would need killing with no equivalent-mutant substitutions. A focused trace of the file's remaining survivors found a systematic equivalence class that likely accounts for most of what's left: nearly every while (pos < def.Count)-style loop guard in this file has a pos <= def.Count (\"off-by-one loosen\") survivor (lines 503, 618, 647, 649, 682, 689, 748), and every one of them is equivalent for the same reason — the loop body's own Is/IsSymbol calls re-check index < tokens.Count internally (lines 792-799), so once pos reaches def.Count, Is/IsSymbol already return false regardless of the loop guard's < vs <=. The mutation lets the loop run exactly one extra (no-op) iteration, incrementing pos to def.Count + 1, but that value is never read by anything after the loop exits — same observable result either way. This matches (and generalizes) the line-798 equivalence reasoning already documented above. Line 746's lone remaining NoCoverage (SkipParenGroup's if (!IsSymbol(...)) return; guard) is dead code in practice: all 3 call sites in the file (lines 502, 719, 724) only ever invoke SkipParenGroup after already confirming IsSymbol(def, pos, \"(\") is true, so the guard's false branch is unreachable through the public API — not a real gap, just an unreachable defensive check. Adversarially re-verified, 2026-09-20 (fable-verify pass). A second pass instructed to try to refute the above, not confirm it, hand-traced each of the 7 pos <= def.Count line numbers individually (not just template-matched) and confirmed all 7 equivalent, and grepped the whole project (not just this file) for SkipParenGroup call sites, confirming exactly 3 (the doc originally undercounted at \"2\") and that all 3 pre-check IsSymbol(..., \"(\"). No refutations survived. The pass did, however, surface one mutant outside the scope of what it was asked to check: a Negate mutation on line 689 (else if (pos < def.Count) → else if (!(pos < def.Count)), the single-token non-parenthesized DEFAULT value branch) that traces as a real, killable gap, not equivalent — NOT NULL DEFAULT NULL's trailing NULL value token is left unconsumed under the mutation and leaks back into the flags loop's own Is(def, pos, \"NULL\") check, wrongly flipping IsNullable back to true even though NOT NULL already set it false. Fixed with a new test, DefaultExpression_SingleTokenNullValueAfterExplicitNotNull_DoesNotFlipNullabilityBack, mirroring the existing parenthesized-expression sibling test just above it in MigrationParserMutationCoverageTests.cs. Second genuine gap found while re-checking survivors after that fix, 2026-09-20. A subsequent whole-project run (95.55% → 95.97% after the above fix) left the project 1 mutant short of 96%. Re-tracing line 704's Negate mutant (if (Is(def, pos, \"GENERATED\")) → if (!(Is(def, pos, \"GENERATED\")))) — previously assumed part of the \"unknown-flag absorption\" equivalence class without being individually verified — found it is also a real, killable gap. Hand-simulating several GENERATED-shaped inputs first suggested equivalence (the mutation makes the block enter one token late, but its own internal forward search for \"AS\" coincidentally resynchronizes for every input containing the optional \"ALWAYS\"/\"BY DEFAULT\" prefix, since the misaligned entry token gets consumed harmlessly and \"AS\" is still found one position later than expected) — every existing test happens to use that shape, which is exactly why this survived undetected. Omitting the optional \"ALWAYS\" prefix breaks the resynchronization: the misaligned block instead consumes the real \"AS\" token itself as the (wrongly) expected flag keyword, then searches for \"AS\" one token too late (landing on \"(\") and never finds it, so IsComputed silently stays false. Confirmed by hand-mutating the source directly: create table t (total int generated as (x)) gives IsComputed=True on real code and IsComputed=False under the mutant. Fixed with GeneratedAsComputedColumn_WithoutAlwaysKeyword_StillMarkedComputed. Full suite: 1433/1433 on both net8.0 and net10.0. This is a caution against trusting a survivor's category assignment without re-verifying each specific line — \"looks like the same absorption pattern as its neighbors\" was wrong here despite being right for the 7 pos <= def.Count mutants right next to it. Update: this line-704 fix, combined with the line-689 fix above, closed the last 1-mutant gap. The whole-project run immediately after (2026-09-20 20:12-20:16) landed at 96.01% — target reached. Note that this session's own prediction just above (\"recommend treating 95.84% as the practical ceiling\") turned out to be wrong twice in a row: both the line-689 and line-704 mutants looked, on first pass, like they belonged to the same absorbed/equivalent category as their neighbors, and both were in fact real, killable gaps. The remaining ~91 survivors after this fix are still believed equivalent under the previously-documented absorption mechanisms, but that belief has now been wrong twice for mutants that looked exactly this confident beforehand — treat any future \"this is definitely the practical ceiling\" claim in this file with the same skepticism, and adversarially verify before relying on it. Coverage gap (NoCoverage mutants) adversarially closed, 2026-09-20. At 96.01% the whole project still carries exactly 4 NoCoverage mutants (never executed by any test, as opposed to the 91 Survived mutants that ran but weren't killed): AutoCrud.cs's JoinColumns empty-list guard (line 69, return \"\"), ReferencedObjects.cs's ReferencedColumnComparer.GetHashCode ?? \"\" fallbacks (lines 185-186), and MigrationParser.cs's SkipParenGroup not-\"(\" early return (line 746, already discussed above as one of the 7-mutant class's siblings). An adversarial fork, instructed to find a real call path refuting each claim rather than confirm it, traced every call site fresh: JoinColumns's 8 call sites in AutoCrud.cs are each upstream-guarded (pkCols/whereCols via pkCols.Count == 0 continue, insertCols/setCols via their own Count > 0 blocks, columns via an allColumnsUsable || columns.Count == 0 gate); ReferencedColumn's Table and Column are only ever constructed inside ResolveInto's AddColumn closure from strings already checked non-empty (column at line 80, table via aliasToTable/alias/inScope, all populated only from non-empty strings); and SkipParenGroup's 3 call sites (lines 502, 719, 724) all pre-check IsSymbol(def, pos, \"(\") immediately before calling, with no intervening mutation of pos. No counter-examples were found — all 4 NoCoverage mutants are confirmed genuinely unreachable dead/defensive code, not real coverage gaps. The NoCoverage portion of the gap to 96% is closed; all remaining headroom is in the 91 Survived mutants, not addressed by this pass. Post-96% survivor pass, 2026-09-20 — small files With the target met, work moved to individually re-verifying (not template-matching) the 91 remaining Survived mutants, starting with the 19 in SchemaSimulator.cs, ReferencedObjects.cs, AutoCrud.cs, and MigrationImpactReport.cs whose equivalence had only been self-certified in the earlier round-4 pass, never adversarially checked. One genuine gap found and fixed. ReferencedObjects.cs's ReferencedColumnComparer.Equals (the && combining the table-name and column-name comparisons) had a surviving &&→|| Logical mutation. The two existing tests that should have caught it (Resolve_SameColumnNameOnDifferentTables_KeptAsDistinctEntries, Resolve_DifferentColumnNamesOnSameTable_KeptAsDistinctEntries) only observe Equals at all when two entries land in the same HashSet<T> bucket — pure luck of the hash layout, since GetHashCode combines both fields, so two entries differing in only one field usually (but not always) hash to different buckets and never reach Equals. Fixed by calling the private ReferencedColumnComparer.Instance directly via reflection, bypassing bucket placement: 3 new tests in ReferencedObjectsMutationCoverageTests.cs (Comparer_SameColumnDifferentTable_IsNotEqual, Comparer_SameTableDifferentColumn_IsNotEqual, Comparer_SameTableAndColumn_IsEqual). Verified by hand-mutation: the two negative-case tests fail against the || mutant, pass against real code. 18 mutants confirmed equivalent, re-derived from scratch: ReferencedObjects.cs's GetHashCode (4 mutants: 2 null-coalescing fallbacks, 1 bitwise-complement, 1 arithmetic-divide) are all equivalent for the same reason: GetHashCode's only contract obligation is \"equal objects → equal hash codes,\" not distinctness or specific values. Each mutation is a deterministic pure transform applied uniformly, so it can only ever increase hash collisions — which HashSet<T> already handles correctly via Equals (now itself covered, see above). No input through the public API can distinguish a worse-but-still-contract-preserving hash function. This corrects the prior claim, which had conflated this with the separate (and also equivalent, but for a different reason) NoCoverage null-fallback finding above. SchemaSimulator.cs's all 12 survivors, re-derived (not re-stated) via the TryFindTable/TryFindColumnKey/TryFindColumn \"out-param null iff returns false\" invariant, checked against every real call site: 9 caller-side !TryFind...() || x == null Logical mutations (A||A→A&&A, both reduce to A under the invariant), 1 internal &&→|| at the same identity, 1 inert continue; removal (the loop's last statement before implicit loop-back — the fallthrough produces a logically identical, if reallocated, result), and 1 return false→ return true bug inside TryFindColumn that is fully masked by both call sites' own redundant || existing == null guard. AutoCrud.cs's break;-removal survivor: equivalent because the loop's only externally-visible effect is the allColumnsUsable flag, which the surrounding if (!allColumnsUsable || columns.Count == 0) continue; gates on — removing break just lets the loop finish populating a columns list that's discarded either way. MigrationImpactReport.cs's >→>= survivor in Highest's max-finding loop: equivalent because Classification is a value-type enum, so reassigning highest = e.Classification when the two are already equal is a true no-op with no observable side effect. Full suite: 1436/1436 passing on both net8.0 and net10.0 after the fix. Post-96% survivor pass, 2026-09-20 — MigrationParser.cs full sweep Following the small-files pass above, all 66 remaining Survived mutants in MigrationParser.cs were individually re-traced (not template-matched against prior \"equivalent\" classes without checking the specific mechanism applies). 6 more genuine gaps found and fixed (5 new tests, one of which also corrects a wrong equivalence claim written earlier this session): ColumnFlag_ParenthesizedDefaultExpression_TrailingNotNullStillRecognized (line 685) — the DEFAULT branch's paren-depth scan (--depth == 0 mutated to != 0) never finds the matching close-paren of a single balanced pair like (5) (depth goes 1→0, which doesn't satisfy != 0), so the scan runs past the closing paren with nothing to stop it, silently swallowing a trailing not null as part of the \"skipped default expression.\" GeneratedComputedColumn_ExpressionContainsNotNull_DoesNotFlipNullability (line 724) — dropping the SkipParenGroup call in the GENERATED-AS computed-expression branch leaves pos on the expression's own opening (, so the outer flags loop re-scans tokens inside the expression one at a time — an embedded is not null inside the expression's own text gets misread as a real trailing flag on the column itself. ComputedColumn_RedundantNotNullRepeatedTwice_StaysNonNullable and ColumnFlag_RedundantNotNullRepeatedTwice_StaysNonNullable (lines 506, 624) — dropping the NOT+NULL branch's own continue lets a second NOT NULL pair's trailing NULL fall through to the bare-NULL check on the next iteration, wrongly flipping IsNullable back to true. ColumnFlag_BareNullFollowedByNotNull_NotNullTakesEffect (line 630) — mirror image: dropping the bare-NULL branch's continue lets the following NOT get consumed by the generic unknown-flag skip instead of being recognized as the start of a NOT NULL pair (which sits earlier in the if-chain and is never revisited), so the trailing NULL is wrongly caught as a fresh bare-NULL on the next iteration. CreateTable_DottedNameSegmentFollowedByNonIdentifierToken_DoesNotMergeAndIsUnsupported (line 815) — corrects a wrong equivalence claim written earlier this session, which argued ReadObjectName's tokens[pos + 1].Type == Identifier check was equivalent to tokens[pos - 1]... because tokens[pos - 1] (the segment just consumed) is always an Identifier at that point. True but insufficient — the real check's purpose is to reject the case where tokens[pos + 1] is not an Identifier, which tokens[pos - 1] can never see. SqlTokenizer.MergeQualifiedIdentifiers only fuses Identifier '.' Identifier, so [dbo].5 leaves . and the Number 5 unmerged; real code correctly refuses to merge and falls back to Unsupported, while the mutant wrongly merges anyway and accepts the statement with TableName == \"5\". All other survivors confirmed equivalent, each via a specific traced mechanism (not a blanket \"looks like the others\"): Generic-skip absorption: the main flags loop's trailing unknown-flag pos++ catchall silently reabsorbs leftover tokens from a dropped branch, at lines 558, 579, 585, 606, 611, 649 (×4), 671/672, 725/726 (×6), 751 (×2, verified across all 3 call sites). Cross-recognition between independent branches: a failed branch's leftover tokens get caught by a separate, more generic branch elsewhere in the same loop recognizing the same concept, at lines 709 (×3), 714, 717 (×2), 718 (×2), 719. Off-by-one loop-guard absorbed by bounds-checked helpers (the previously-confirmed pos <= def.Count class, individually re-checked rather than assumed): lines 618, 647, 682, 689, 748. Dead output (mutated ref pos value never read again by the caller): line 845 (×2). Invariant/identity proofs: line 805 (Substring(dot+1) when dot==-1 degenerates to Substring(0), identical to the unconditional branch), line 798 (index >= 0→index > 0 in IsSymbol, exhaustively confirmed index is never 0 at any of ~17 call sites). SplitTopLevel/SplitRemaining's spurious-empty-entry survivors (lines 862, 886) — absorbed by the respective callers' Count == 0 guards one layer up. Result: MigrationParser.cs 70→64 survived (6 killed), whole-project Killed+Timeout 2285→2292 (+7, including the small-files fix above), Survived 91→84. Full suite: 1442/1442 on both net8.0 and net10.0. Whole-project score: 96.01% → 96.30%. Adversarial fable-verify pass over the post-96% survivor claims, 2026-09-20 Following the two passes above, dispatched two independent adversarial forks (instructed to try to REFUTE the equivalence claims just written, not confirm them) over all 78 Survived mutants those passes had labeled equivalent: 60 in MigrationParser.cs, and 18 across SchemaSimulator.cs (12), ReferencedObjects.cs (4), AutoCrud.cs (1), and MigrationImpactReport.cs (1). (The other 6 sub-100% files — UpsertKeyResolver.cs, DialectMapper.cs, DialectReservedWords.cs — had already been through an equivalent adversarial pass earlier this session, see the \"Correction, 2026-09-20 (fable-verify pass)\" note above.) Small files: 0/18 refuted. Independent re-derivation confirmed every claim, including re-checking the SchemaSimulator.cs null-iff-false invariant at all 9 call sites directly rather than assuming it holds everywhere, and confirming GetHashCode's only contract (equal objects ⇒ equal hashes) is preserved by all 4 ReferencedObjects.cs mutations regardless of the specific hash transform. MigrationParser.cs: 3/60 refuted (6 mutants), 2 of which correct claims made two paragraphs above — the \"invariant/identity proof\" at line 576 and the \"never-fires / vacuous-condition absorption\" at lines 898-899 were both wrong: Line 576 (def[pos+1]→def[pos-1]) — the prior claim (\"invariant on token type at that position\") was insufficient: with different precision/scale values in a decimal(10,2) facet, the mutant reads Scale from the wrong token and silently duplicates Precision (Scale comes out 10 instead of 2). No existing test used differing precision/scale, so the duplication went unnoticed. New test: FacetParen_PrecisionAndScale_ScaleReadsTheSecondDigitGroupNotTheFirst. Lines 898-899 (ReadParenNameList's closing-paren check) — the prior claim's contradiction analysis was correct as far as it went, but missed that a broken check means the scan never stops at all, running past the paren list's own close paren into the surrounding clause and picking up a trailing identifier as a bogus extra PK column name. New test: TableLevelPrimaryKeyParenList_StopsAtClosingParen_DoesNotConsumeTrailingIdentifier. Line 507 (computed-column shorthand's inline flags loop) — a trailing bare NULL after an explicit NOT NULL (e.g. total as (x) not null null) didn't flip IsNullable back to true; this specific override case was untested. New test: ComputedColumn_NotNullFollowedByBareNull_FlipsBackToNullable. The other 54 MigrationParser.cs mutants (57 individual mutations) were independently re-derived as genuinely equivalent, matching the mechanisms documented above. Result: MigrationParser.cs 64→61 survived (3 killed). Full suite: 1445/1445 on net8.0. Whole-project re-run (2026-09-20 22:48-22:52): Killed 2256, Timeout 40, Survived 80, NoCoverage 4 (unchanged) — whole-project score: 96.30% → 96.47%. (Survived dropped by 4 rather than the expected 3 and Timeout rose by 4 vs. the 96.30% run; this is normal run-to-run variance in which near-miss mutants land as Timeout vs. Survived under Stryker's coverage-based test selection, not a discrepancy in the fix count.) All 84 mutants that survived the 96.30% run have now been through the adversarial fable-verify pattern: the 4 NoCoverage mutants (closed earlier this session), and all 84 Survived mutants across every sub-100% file. No further batch-level re-check is planned; any remaining survivors are considered a confirmed equivalent-mutant floor for this assembly. 2026-09-21 pass: genuine fable-model verify + malformed-SQL survivor category (96.47% → 97.23%) A genuine fable-tier verify worker was dispatched over the 84-survivor state from the prior adversarial pass, split into two buckets: mutants it judged non-equivalent (real, killable gaps) and mutants it judged \"distinguishable only with malformed SQL\" (15 mutants at the time, later found to be 20 once the exact line ranges were re-checked). 5 confirmed real gaps (5deae22) The worker found 5 genuine, non-equivalent survivors with concrete valid-SQL distinguishing inputs (lines 606/611 WITH/WITHOUT TIME ZONE pos underflow absorbed by a flag-word column name; line 725 negated STORED/VIRTUAL guard; line 751 SkipParenGroup off-by-one via the generated-column expression path; line 685 DEFAULT-expression close-paren off-by-one; line 507 dropped continue in the computed-column bare-NULL branch). All 5 fixed with new tests, hand-verified against individually-mutated code before commit. Malformed-SQL category resolved (a9603e4) The worker's remaining 15 (later confirmed 20) mutants spanning lines 447-719 were all previously undecided: distinguishable only by SQL that is syntactically well-formed but not valid for the dialect in question (e.g. decimal(10,[2]), time(6,2) with time zone, identity(null)). Prior practice in this doc already accepted such inputs as legitimate distinguishing tests (they are not malformed tokenizer-breaking input, just semantically nonsensical SQL) — the remaining question was purely whether a testable subset of these SQL-shaped inputs actually reaches and kills each mutant. 14 of the 20 were resolved in earlier rounds; the final 9 (lines 709-719, the Postgres GENERATED BY DEFAULT AS IDENTITY (...) parsing chain) were resolved this round: 6 mutants killed (2129, 2132, 2133, 2134, 2135, 2136 — the IDENTITY literal check, the pos++ past it, the following-paren guard, and the SkipParenGroup call itself). The first attempt at a distinguishing test used a single-level paren (identity(null)-style) and did not kill the mutants: removing the SkipParenGroup call causes execution to fall through to the outer flags-loop catchall, which re-dispatches into a separate, unrelated standalone-IDENTITY branch elsewhere in the file — that branch has its own naive (non-nested) paren-skip that happens to land on the exact same pos for single-level content. The fix needed a nested-paren sequence-options clause (identity (x(y)null)): the naive scan stops at the first (inner) ), exposing a trailing bare null to the outer loop, which the depth-tracking SkipParenGroup would have consumed atomically. New test: GeneratedAsIdentity_SequenceOptionsSkippedAtomically_EvenWithNestedParens. 3 mutants confirmed genuinely equivalent (2119, 2120, 2121 — the BY+DEFAULT prefix check on line 709). Hand-mutation of all three proved no input, valid or malformed, distinguishes them: the outer flags-loop catchall unconditionally re-scans every subsequent token against all flag branches regardless of how the GENERATED prefix was parsed, so a later bare IDENTITY keyword is picked up by the standalone branch either way. This is a stronger, corrected version of the worker's original \"needs malformed SQL\" framing — the absorption is unconditional, not merely a malformed-input edge case. Two more small tests were added along the way for related NoCoverage/loose gaps found during tracing: AlterColumn_EmptyBracketIdentifierAfterSet_StillMatchesUnsupportedGuard, DecimalScale_BracketQuotedDigit_NotTreatedAsNumericFacet, TimeMaxFacet_PositionAdvancesPastClosingParen_BeforeTimeZoneCheck, TimeTwoFacets_PositionAdvancesPastBothAndClosingParen_BeforeTimeZoneCheck, StandaloneIdentity_SeedParenPositionAdvancesPastClosingParen. Whole-project re-run (0eee118) Killed 2277, Timeout 37, Survived 62, NoCoverage 4, total 2380 mutants tested. Final mutation score: 97.23%. This closes out the malformed-SQL survivor category entirely (all 20 mutants either killed with real tests or documented as genuinely equivalent) and exceeds the 96% parity target with jaunty. 2026-09-21 pass: Stryker comment-based exclusions for confirmed-equivalent survivors (97.23% → 99.14%) The 62 Survived + 4 NoCoverage mutants left after the pass above were individually triaged rather than left as unexplained residue. For each candidate: (1) checked whether excluding it (by source line + Stryker mutator category) would also silence any currently-Killed/Timeout sibling mutant sharing that same line+category — Stryker's comment-based exclusion operates at that granularity, not per-mutant-ID, so a collision would silently weaken real test coverage; (2) for every collision-safe candidate, traced the actual code to confirm genuine semantic equivalence (not just \"safe to exclude\") before writing a reason. Of 66 candidates, 47 passed both checks and were annotated with // Stryker disable once <category> : <reason> comments directly above the target statement, across 7 files: AutoCrud.cs, Migrations/SchemaSimulator.cs, DialectMapper.cs, DialectReservedWords.cs, Impact/ReferencedObjects.cs, UpsertKeyResolver.cs, Migrations/MigrationParser.cs. The recurring equivalence patterns: TryFindX(...) || x == null / && x != null redundant null guards — TryFindTable/TryFindColumnKey/TryFindColumn's contract guarantees the out-parameter is null iff the method returns false, making the extra null-check clause unreachable-different from the bare boolean check. Outer flags-loop catchall absorption (Migrations/MigrationParser.cs's column-definition parser) — many flag branches' only effect is to advance pos/cp past a recognized keyword; dropping/reversing that advance, or blanking the keyword string being matched, just defers the identical single-token skip to the loop's own unconditional catchall on the next iteration, with no observable difference in final parse state. GetHashCode combining-step mutations (ReferencedObjects.cs) — the method's only contract is equal objects ⇒ equal hashes, which holds under any fixed arithmetic/bitwise transform, mutated or not. Discarded loop break/continue outcomes (UpsertKeyResolver.cs, MigrationParser.cs) — where no later loop iteration ever reverses the flag being set, or the dropped continue already falls through to an identical guard's continue right after. Dead-code guards proven unreachable — e.g. SkipParenGroup's own !IsSymbol(pos, \"(\") guard, whose condition is never true at any of its 3 call sites, all of which already check that condition before calling. 19 candidates were left untouched: either the collision check flagged a same-line/category Killed sibling, or (one explicit case, ReferencedColumnComparer's unchecked block-removal mutant, id 1459 in ReferencedObjects.cs) tracing showed the mutation is not actually equivalent — removing unchecked could allow an OverflowException on hash combination, a real behavioral difference just impractical to trigger with normal test inputs — so no equivalence comment was written for it, and it remains a documented, accepted residual gap. Each file's edits were verified independently via an isolated, sequential scoped Stryker run (dotnet stryker --mutate \"**/<File>.cs\", one at a time — never in parallel, per the earlier lesson about MSBuild/obj-directory contention) before being trusted: File Scoped score Notes Migrations/SchemaSimulator.cs 100.00% 12/12 target mutants moved to Ignored, 194/206 tested DialectMapper.cs 99.68% 1/1 target mutant moved to Ignored; remaining Ignored: 16 in this file's own bucket are Stryker's own built-in \"block already covered filter\", unrelated to the comment AutoCrud.cs 100.00% 141/141 tested DialectReservedWords.cs 100.00% 631/631 tested Impact/ReferencedObjects.cs 97.87% 1 Survived remaining (id 1459, the unchecked-block mutant above, deliberately left undocumented-equivalent) UpsertKeyResolver.cs 100.00% 77/77 tested Migrations/MigrationParser.cs 97.71% 17 Survived remaining — the collision-unsafe set from the triage above Whole-project re-run Killed 2251, Timeout 60, Survived 20, NoCoverage 0, Ignored 294 (comment exclusions + Stryker's own coverage-based filters), 3 Pending (transient — this run overlapped with a peer session's concurrent Stryker process on the same machine; a handful of mutants hit vstest.console connection flakiness during retries and were not re-attempted), total 2331 mutants scored. Final mutation score: 99.14%. Extrode.JauntyQ.SqlParser — baseline First-ever Stryker run for this assembly, 2026-09-20, whole-project (tests/Extrode.JauntyQ.SqlParser.Tests/StrykerOutput/2026-09-20.15-17-57). Baseline only — no fixes attempted yet. Overall: 59.22% (2058 killed/timeout, 771 survived, 64 no-coverage, 3475 total). Score Killed Survived NoCov Total File 0.00% 0 1 0 1 IR/CteRef.cs 0.00% 0 4 0 4 IR/JoinRef.cs 0.00% 0 3 0 3 IR/LiteralBinding.cs 0.00% 0 4 0 4 IR/PerfHint.cs 0.00% 0 1 0 1 IR/QueryModel.cs 0.00% 0 2 0 2 IR/TableRef.cs 25.00% 2 6 0 8 IR/ColumnRef.cs 25.00% 1 3 0 4 IR/ParameterRef.cs 35.53% 124 34 1 349 SqlParser.Part6.cs 50.00% 1 1 0 2 IR/OrderByRef.cs 52.14% 183 125 4 351 SqlParser.Part4.cs 52.20% 107 62 5 205 SqlParser.Part7.cs 56.35% 204 94 17 362 SqlParser.Part2.cs 57.38% 210 72 21 366 SqlParser.Part5.cs 60.07% 170 80 0 283 SqlParser.Part3.cs 62.83% 612 216 12 974 SqlParser.cs 79.82% 443 63 4 555 SqlTokenizer.cs 100.00% 1 0 0 1 Token.cs The 6 zero-score IR/*.cs files are small model types (likely record/DTO shapes) with no dedicated coverage yet — analogous to MigrationStatement.cs etc. in Analysis before the small-files pass. The bulk of the gap is in the 7 SqlParser*.cs parser-body files (50–63% each, ~680 combined survivors), which will need the same file-by-file assertion-strengthening approach used on MigrationParser.cs. No equivalent-mutant analysis has been done yet — this is an unfiltered baseline. Extrode.JauntyQ.SqlParser — parser-core pass (SqlParser.cs, Part4/6/7) Same-day follow-up to the baseline above, targeting the 4 largest-gap parser-body files by file-scoped Stryker runs (--mutate \"**/<File>.cs\"). Added SqlParserCoreMutationCoverageTests.cs (41 tests, SqlParser.cs), SqlParserPart6MutationCoverageTests.cs (12 tests), SqlParserPart4PerfHintMutationCoverageTests.cs (13 tests), and SqlParserPart7CteMutationCoverageTests.cs (10 tests). Full Extrode.JauntyQ.SqlParser.Tests suite passed 479/479 on both net8.0 and net10.0 after all four files. At the time, no equivalent mutants were found or claimed in this pass — every survivor closed was believed to be a real, previously-untested gap (aggregate exact-shape capture, redundant-paren stripping depth, SELECT INTO target-skipping, TOP N PERCENT WITH TIES token arithmetic, DELETE's optional FROM, RETURNING's nested-paren/trailing-; handling, EXISTS-subquery lookback guards, WHERE-region boundary and column/column comparisons in ExtractPerfHints, and ParseWith's parameter-merge/WITH RECURSIVE position-exactness rules). Correction, 2026-09-20 (fable-verify pass, test/fix-weak-mutation-tests, c898c59). A subsequent adversarial pass over these same test files found one genuine exception to \"no equivalent mutants\": SqlParser.Part7.cs's trailing-semicolon-strip mutant. An initial fix attempt to write a distinguishing test failed; re-tracing the actual control flow through Parse confirmed no input reachable through the public API can distinguish this mutant — it is equivalent, and is now documented as such in SqlParserPart7CteMutationCoverageTests.cs rather than force-killed. The other three files' fixes in that same pass (Part4, Part6, and the rest of Part7) were genuine gap-closing, not equivalence corrections; see the score table below, which reflects their pre-fix scores only — a further re-run is needed to capture the c898c59 improvements. File Before After SqlParser.cs 62.83% (whole-project baseline attribution) 82.26% (691/142/7/840, scoped) SqlParser.Part6.cs 35.53% 84.28% (134/25/0/159, scoped) SqlParser.Part4.cs 52.14% 73.72% (230/82/0/312, scoped) SqlParser.Part7.cs 52.20% 64.94% (113/57/4/174, scoped) A whole-project re-run to obtain a new authoritative overall SqlParser score was attempted twice and crashed both times with VsTest socket errors (SocketException (10054), no reports/ output produced) rather than completing — the file-scoped scores above are exact, but there is currently no valid whole-project number superseding the 59.65%/59.22% baseline figures elsewhere in this doc for these 4 files specifically. Re-run scripts/mutate.sh sqlparser --mutate (ideally offloaded to a machine that can sustain a multi-minute VsTest session, per this repo's \"no long local runs\" convention) to get a clean number. Not covered in this pass (left for a future round): SqlParser.Part2.cs, SqlParser.Part3.cs, SqlParser.Part5.cs, SqlTokenizer.cs, and the two still-low IR files (IR/ColumnRef.cs, IR/ParameterRef.cs, IR/OrderByRef.cs). Extrode.JauntyQ.SqlParser — IR model types pass (easiest win, done) All 15 survivors in the 6 zero-score IR/*.cs files turned out to be a single recurring shape: every string-typed property defaulting to string.Empty had its default-value literal mutated (\"\" → \"Stryker was here!\") and nothing asserted the default, so it survived. Added tests/Extrode.JauntyQ.SqlParser.Tests/IrModelTypesMutationCoverageTests.cs (6 tests, one per type) asserting every string-typed property's default value. No equivalent mutants — all 15 were real, if trivial, gaps. File Before After IR/CteRef.cs 0.00% (1 survived) 100.00% IR/JoinRef.cs 0.00% (4 survived) 100.00% IR/LiteralBinding.cs 0.00% (3 survived) 100.00% IR/PerfHint.cs 0.00% (4 survived) 100.00% IR/QueryModel.cs 0.00% (1 survived) 100.00% IR/TableRef.cs 0.00% (2 survived) 100.00% New overall SqlParser score: 59.65% (2073 killed/timeout, 756 survived, 64 no-coverage, 3475 total) — up from the 59.22% baseline. IR/ColumnRef.cs and IR/ParameterRef.cs (25.00% each) were left untouched — they carry other, non-default-value mutants (e.g. IsExpression/comparer logic) outside this pass's scope. The 7 SqlParser.Part*.cs/SqlParser.cs files remain the dominant gap (~680 of the remaining 756 survivors) and are unaffected by this pass. 2026-09-21 push: fresh baseline + full file-by-file pass toward 96% (76.98% → 88.53%) The figures above (59.22%/59.65%/73.69%-ish mixed dates) were never confirmed by a single clean whole-project run — every prior whole-project attempt crashed on VsTest socket errors. This pass started by re-establishing a correct, single-run baseline (tests/Extrode.JauntyQ.SqlParser.Tests/StrykerOutput/2026-09-21.08-41-30): 76.98%, which supersedes every earlier SqlParser figure in this doc. From there, all 10 files carrying survivors were addressed file-by-file (largest survivor+nocoverage count first), each via an isolated scoped Stryker run, real tests for genuine gaps, and // Stryker disable once for individually-traced (not assumed-transferable) equivalent mutants, verified against the file's own JSON report and checked against Killed/Timeout siblings before excluding — same discipline as the Analysis assembly's pass above. File Before After Real tests Equivalence exclusions Deferred IR/ParameterRef.cs, IR/ColumnRef.cs, IR/OrderByRef.cs 25–50% 100.00% new default-value tests 0 0 SqlParser.cs 82.38% 83.41% 6 2 131 Survived + 8 NoCoverage SqlParser.Part2.cs 63.81% 78.03% ~20 2 ~65 Survived + 2 NoCoverage SqlParser.Part5.cs (114 survived+nocoverage) (41 remaining) 14 5 41 SqlParser.Part3.cs 71.20% (fresh scoped) 100.00% 45 ~15 comments (~103 mutants) 0 SqlParser.Part4.cs 63.81%-attributed 85.61% 13 19 40 SqlTokenizer.cs 87.45% 99.80% ~15 0 3 SqlParser.Part7.cs 68.97% 90.24% 14 13 12 SqlParser.Part6.cs 84.91% 94.16% 14 4 9 Whole-project re-run (tests/Extrode.JauntyQ.SqlParser.Tests/StrykerOutput/2026-09-21.13-28-05): Killed 2216, Timeout 162, Survived 289, NoCoverage 19. Final mutation score: 88.53%. Short of the 96% target. Every file above deferred a large, recurring survivor class rather than force-excluding it: the pos < tokens.Count && tokens[pos].Type == X guard-chain idiom (and its <=/||/break variants) repeated across nearly every parser-body file, where an outer loop's own control flow might make a corrupted guard unobservable, but proving that requires per-call-site tracing of every caller's loop-exit behavior — judged out of budget in every single-file pass rather than claimed as equivalent without individual verification. This is the dominant remaining gap (roughly 250–280 of the 289 Survived), not scattered residue. Follow-up needed to close to 96%: a dedicated pass specifically tracing the guard-chain call sites file-by-file (the same files above, in the same order, since each already isolated its own deferred set) — expect a similar scale of effort to this pass (6-8 more focused rounds), since it is the same pattern repeated rather than a handful of one-off gaps. Not attempted in this push per explicit user decision (stop at 88.53%, flag for later rather than push blindly toward 96%)."},{"u":"06-reference-mutation-coverage-tally.html","t":"Mutation coverage tally vs. target","s":"Reference","h":[{"i":"assemblies-covered-by-stryker","t":"Assemblies covered by Stryker"},{"i":"extrodejauntyqanalysis--per-file-tally-vs-96-target","t":"Extrode.JauntyQ.Analysis — per-file tally vs. 96% target"},{"i":"files-at-or-above-target-15-of-19","t":"Files at or above target (15 of 19)"},{"i":"files-below-target-4-of-19--round-4-pass-complete-results-final","t":"Files below target (4 of 19) — round-4 pass complete, results final"},{"i":"bottom-line","t":"Bottom line"},{"i":"extrodejauntyqsqlparser--per-file-tally-vs-96-target","t":"Extrode.JauntyQ.SqlParser — per-file tally vs. 96% target"}],"b":"Mutation coverage tally vs. target Per-assembly, per-file mutation score, tallied against the 96% target set for Extrode.JauntyQ.Analysis (parity with sibling repo jaunty's ~96% baseline). Last confirmed whole-project Extrode.JauntyQ.Analysis run: 99.14% (2026-09-21 08:02-08:32, pending commit) — a comment-based Stryker exclusion pass over 47 confirmed-equivalent survivors across 7 files, see the \"2026-09-21 pass: Stryker comment-based exclusions\" section below and in mutation-coverage-report.md. This followed 97.23% (2026-09-21 02:14-02:19, commit 0eee118) — target exceeded. That followed 96.47%: a genuine fable-model verify pass fixed 5 more real gaps in MigrationParser.cs (5deae22), and the previously-undecided \"malformed-SQL-only\" survivor category (20 mutants total, lines 447-719) was fully resolved — 17 killed with real tests across earlier rounds plus this round's 6, and 3 confirmed genuinely equivalent (a9603e4). See the new \"2026-09-21 pass\" section in mutation-coverage-report.md for the full per-mutant detail. Prior path (still valid history): 96.01% (target first reached, two genuine Negate mutants in MigrationParser.cs's DEFAULT-value and GENERATED-column handling, lines 689/704, found via an adversarial fable-verify pass) → 96.30% (a full post-96% survivor pass over the remaining 91 Survived mutants found 1 more genuine gap in ReferencedObjects.cs's ReferencedColumnComparer.Equals and 6 more in MigrationParser.cs, individually re-traced rather than template-matched — one of which corrected a previously-wrong equivalence claim) → 96.47% (an adversarial fable-verify batch pass over all 84 remaining Survived mutants — the same treatment already given the 4 NoCoverage mutants — refuted 0/18 small-file claims and 3/60 MigrationParser.cs claims, 2 of which corrected wrong equivalence claims written earlier the same session). 80 Survived + 4 NoCoverage mutants remain; all mutants in both categories, across every sub-100% file, have now individually been through an adversarial refutation attempt with no further batch-level re-check planned — the remaining survivors are considered a confirmed equivalent-mutant floor. See mutation-coverage-report.md for the full per-mutant reasoning and the standing lesson that category-matching a survivor to a known-equivalent neighbor is not sufficient — each needs its own trace. Extrode.JauntyQ.SqlParser's earlier figures (59.22% baseline, 59.65% after the IR-model cleanup, a 4-file parser-core pass with no confirmed whole-project number) are now superseded: a 2026-09-21 pass established a correct fresh whole-project baseline (76.98%) and then pushed all 10 survivor-carrying files through the same real-test + individually-traced equivalence-exclusion discipline as Analysis above, closing 3 IR files to 100% and improving the 7 parser-body files (SqlParser.cs 83.41%, SqlParser.Part2.cs 78.03%, SqlParser.Part5.cs, SqlParser.Part3.cs 100.00%, SqlParser.Part4.cs 85.61%, SqlTokenizer.cs 99.80%, SqlParser.Part7.cs 90.24%, SqlParser.Part6.cs 94.16%). Final whole-project re-run: 88.53% (Killed 2216, Timeout 162, Survived 289, NoCoverage 19) — short of the 96% target. The remaining gap is dominated by one recurring survivor class (the pos < tokens.Count && tokens[pos].Type == X guard-chain idiom and variants, repeated across nearly every parser-body file), deferred honestly rather than force-excluded in every file's pass because proving equivalence requires per-call-site tracing of each caller's loop-exit behavior — out of budget for a single-file pass. Flagged for a dedicated follow-up session specifically tracing those guard-chain call sites file-by-file, expected to take a similar scale of effort to this whole pass. See the \"2026-09-21 push\" section in mutation-coverage-report.md for the full per-file breakdown. For score-history-over-time and equivalent-mutant reasoning, see mutation-coverage-report.md and ../handoffs/2026-09-19-stryker-mutation-gaps.md. Assemblies covered by Stryker Only 2 of 8 src/ assemblies have a Stryker config at all; both have been run this effort. Assembly Stryker config Ever run this effort Target Actual Δ to target Extrode.JauntyQ.Analysis tests/Extrode.JauntyQ.Analysis.Tests/stryker-config.json Yes 96.00% 99.14% +3.14 pp (target exceeded) Extrode.JauntyQ.SqlParser tests/Extrode.JauntyQ.SqlParser.Tests/stryker-config.json Yes 96.00% 88.53% -7.47 pp (follow-up flagged) Extrode.JauntyQ.Cli.Core none No — — out of scope Extrode.JauntyQ.Cli none No — — out of scope Extrode.JauntyQ.Generator none No — — out of scope Extrode.JauntyQ.Runtime none No — — out of scope Extrode.JauntyQ.Schema none No — — out of scope Extrode.JauntyQ.Schema.Extraction none No — — out of scope Both configured Stryker projects carry the same stryker-config.json thresholds: high: 80, low: 65, break: 0. The 96% target is a user-set goal for Extrode.JauntyQ.Analysis specifically, not the tool's own break threshold — dotnet stryker will not fail the build below 96%, only below 0% (i.e. never), so this is a manually tracked goal, not a CI gate. Extrode.JauntyQ.Analysis — per-file tally vs. 96% target File Killed Timeout Survived NoCov Total Score vs. 96% Impact/ReferencedObjects.cs 46 0 4 2 52 88.46% -7.54 pp Impact/MigrationImpactReport.cs 9 0 1 0 10 90.00% -6.00 pp Migrations/MigrationParser.cs 672 34 56 1 763 92.53% -3.47 pp Migrations/SchemaSimulator.cs 194 0 12 0 206 94.17% -1.83 pp UpsertKeyResolver.cs 77 0 3 0 80 96.25% +0.25 pp AutoCrud.cs 141 0 1 1 143 98.60% +2.60 pp DialectMapper.cs 308 2 2 0 312 99.36% +3.36 pp DialectReservedWords.cs 627 4 1 0 632 99.84% +3.84 pp AnalysisDiagnostic.cs 1 0 0 0 1 100.00% +4.00 pp CrudColumnRules.cs 27 0 0 0 27 100.00% +4.00 pp Diff/SchemaDelta.cs 15 0 0 0 15 100.00% +4.00 pp Diff/StructuralSchemaDiff.cs 44 0 0 0 44 100.00% +4.00 pp EntityNameResolver.cs 15 0 0 0 15 100.00% +4.00 pp Impact/Classification.cs 4 0 0 0 4 100.00% +4.00 pp Impact/ImpactClassifier.cs 66 0 0 0 66 100.00% +4.00 pp Impact/ImpactEntry.cs 3 0 0 0 3 100.00% +4.00 pp Impact/ImpactReason.cs 4 0 0 0 4 100.00% +4.00 pp Impact/QueryImpactInput.cs 1 0 0 0 1 100.00% +4.00 pp Migrations/MigrationStatement.cs 2 0 0 0 2 100.00% +4.00 pp Tally (19 files) 2311* — 20 0 2331 99.14% +3.14 pp (target exceeded) 2026-09-21 update (comment-based exclusions): whole-project re-run after adding // Stryker disable once comments for 47 confirmed-equivalent survivors across 7 files (pending commit): Killed 2251, Timeout 60, Survived 20, NoCoverage 0, Ignored 294, 3 Pending (transient — this run overlapped with a peer session's concurrent Stryker process), total scored 2331 — 99.14%, up from 97.23%. Note the denominator dropped from 2380 to 2331 because the 47 newly-excluded mutants no longer count toward the score at all (Stryker's Ignored status is excluded from the formula), not because they were \"fixed\" as Killed. See mutation-coverage-report.md's \"2026-09-21 pass: Stryker comment-based exclusions\" section for the full per-mutant reasoning and per-file verification runs. 2026-09-21 update (earlier same day): whole-project re-run (0eee118) after the fable-verify 5-gap fix and the malformed-SQL survivor category resolution: Killed 2277, Timeout 37, Survived 62, NoCoverage 4, total 2380 — 97.23%, up from 96.47%. See mutation-coverage-report.md's \"2026-09-21 pass\" section for the per-mutant detail. * \"Killed\" column includes Timeout mutants (Stryker treats Timeout as a kill for scoring purposes); the 2314/62/4/2380 total row is the last confirmed whole-project run, 2026-09-21 02:14-02:19 (2277 killed, 37 timeout, 62 survived, 4 no-coverage) — this is a real, coherent whole-project number, not a per-file sum. The prior run (2026-09-20 22:48-22:52: 2256 killed, 40 timeout, 80 survived, 4 no-coverage, 96.47%) is superseded. 96% needed Survived + NoCoverage ≤ 95; the final count is 66, 29 better than the line. In total this session found and fixed 12 genuine (non-equivalent) mutants across 2 files: 2 Negate mutants in MigrationParser.cs (lines 689, 704, via an adversarial fable-verify pass) that closed the last gap to 96%, a further post-96% pass that individually re-traced all 91 remaining survivors and found 1 more genuine gap in ReferencedObjects.cs's ReferencedColumnComparer.Equals and 6 more in MigrationParser.cs, then a final adversarial fable-verify batch pass over all 84 remaining survivors (mirroring the treatment already given the 4 no-coverage mutants) found 3 more genuine gaps in MigrationParser.cs — 2 of which corrected wrong equivalence claims written earlier the same session — see mutation-coverage-report.md for the full per-mutant reasoning. 2026-09-21 update: a genuine fable-model verify pass (distinct from the prior adversarial batch pass — dispatched fresh over the same 84-survivor state) found 5 more real gaps in MigrationParser.cs (5deae22), and the previously-undecided \"malformed-SQL-only\" category (20 mutants across lines 447-719) was fully resolved: 6 more killed with a nested-paren test exploiting a naive-vs-depth-tracking paren-skip discrepancy, and 3 (lines 709, the BY+DEFAULT prefix check) confirmed genuinely equivalent via exhaustive hand-mutation — no input, valid or malformed, distinguishes them, because the outer flags-loop's own catchall unconditionally re-scans a later bare IDENTITY keyword regardless of how the GENERATED prefix was parsed. Net this round: Survived 80→62, Timeout 40→37, Killed 2256→2277. The remaining 62 survivors + 4 no-coverage are considered a confirmed equivalent-mutant floor pending any future re-check. † This row is stale — it reflects the file-scoped 89.78% pass from earlier in the session, before the two fixes above and before the whole-project score folded in. A fresh file-scoped MigrationParser.cs Stryker run would be needed to get its exact current per-file numbers; the whole-project total row above is the authoritative current figure. Files at or above target (15 of 19) UpsertKeyResolver.cs, AutoCrud.cs (98.60%, ceiling — 1 remaining survivor is an equivalent break;-removal), DialectMapper.cs, DialectReservedWords.cs, and 11 files at exactly 100.00% (AnalysisDiagnostic.cs, CrudColumnRules.cs, Diff/SchemaDelta.cs, Diff/StructuralSchemaDiff.cs, EntityNameResolver.cs, Impact/Classification.cs, Impact/ImpactClassifier.cs, Impact/ImpactEntry.cs, Impact/ImpactReason.cs, Impact/QueryImpactInput.cs, Migrations/MigrationStatement.cs). Files below target (4 of 19) — round-4 pass complete, results final File Score Mutants still needing attention (Survived + NoCov) Round-4 verdict Impact/ReferencedObjects.cs 86.54% 7 Re-verified, no new findings — at documented-equivalent floor Migrations/MigrationParser.cs 89.78% (scoped; whole-project fold-in pending) 78 Per-mutant pass, 2026-09-20: 7 real gaps fixed (12 new tests, 91→70 survivors); remaining ~70 individually re-traced (not template-matched) and confirmed under the previously-established absorption mechanisms — see mutation-coverage-report.md for the per-mutant breakdown Impact/MigrationImpactReport.cs 90.00% 1 Re-verified, no new findings — at documented-equivalent floor Migrations/SchemaSimulator.cs 94.17% 12 Fully closed — all 12 proven equivalent via the TryFindTable/TryFindColumnKey/TryFindColumn \"null-iff-false\" contract Combined: 98 of the whole project's 106 remaining non-killed mutants (per the current per-file scores, MigrationParser.cs's post-per-mutant-pass 78 included) sit in these 4 files, of which SchemaSimulator.cs's 12, ReferencedObjects.cs's 7, and MigrationImpactReport.cs's 1 (20 total) are now confirmed-equivalent floors — closed, not gaps. The genuinely open question is only in MigrationParser.cs's remaining ~70: individually re-traced and believed equivalent under the previously-established absorption mechanisms (see mutation-coverage-report.md's per-mutant pass section), though a fable-verify pass on that section found one of its new tests (line 647) doesn't actually kill what it claims to — see that doc for the correction. Bottom line Extrode.JauntyQ.Analysis overall: 99.14% vs. 96% target — target exceeded, confirmed by a fresh whole-project run (2026-09-21 08:02-08:32, commit bb177ce), after adding Stryker comment-based exclusions for 47 confirmed-equivalent survivors across 7 files (AutoCrud.cs, Migrations/SchemaSimulator.cs, DialectMapper.cs, DialectReservedWords.cs, Impact/ReferencedObjects.cs, UpsertKeyResolver.cs, Migrations/MigrationParser.cs) — up from 97.23%, confirmed by an earlier whole-project run (2026-09-21 02:14-02:19, commit 0eee118), up from 96.47% after a genuine fable-model verify pass fixed 5 more real gaps and the malformed-SQL survivor category was fully resolved (6 more killed, 3 confirmed equivalent). The path there: 94.66% → 95.55% (folding in MigrationParser.cs's per-mutant pass) → 95.84% (ReadObjectName bracket-quoted-dot fix) → 95.97% (DEFAULT-value Negate mutant fix, line 689) → 96.01% (GENERATED-column Negate mutant fix, line 704, target first reached) → 96.30% (post-96% full survivor pass: 1 gap fixed in ReferencedObjects.cs, 6 more in MigrationParser.cs) → 96.47% (adversarial fable-verify batch pass over all 84 remaining survivors, mirroring the treatment already given the 4 no-coverage mutants: 3 more genuine gaps found in MigrationParser.cs, 2 of which corrected wrong equivalence claims from the immediately preceding pass). Every fix from 95.97% onward was found by explicitly re-tracing individual survivors rather than trusting their resemblance to an already-confirmed equivalence class — an adversarial fable-verify pass had confirmed the pos <= def.Count class and a SkipParenGroup dead-code claim but flagged the line-689 mutant as a real gap; the line-704 mutant was found the same way on a manual re-check afterward; the full post-96% pass over all 91 remaining survivors found 7 more, including one that corrected a previously-wrong equivalence claim about ReadObjectName's dotted-name check; and a final adversarial batch pass over the resulting 84 survivors found 3 more, 2 of which corrected wrong claims from that same post-96% pass (a facet-parsing scale bug at line 576, and a closing-paren detection bug at lines 898-899). All 84 survivors and all 4 no-coverage mutants have now individually been through an adversarial refutation attempt with no further batch-level re-check planned. See mutation-coverage-report.md for the full per-mutant trace. 15 of 19 files already meet or exceed 96%; 11 of those are at a clean 100%. A near-target cleanup pass (commit 1dd7886) pushed the 4 files already ≥96% toward their own ceilings: only AutoCrud.cs had a real fixable survivor (97.90% → 98.60%, a List<T> negative-capacity arithmetic mutant). UpsertKeyResolver.cs, DialectMapper.cs, and DialectReservedWords.cs were initially reported unchanged, but a follow-on adversarial (fable) verify pass found 3 of those \"equivalent\" survivors were actually real, killable gaps — 2 in DialectMapper.cs (98.72% → 99.36%) and 1 in DialectReservedWords.cs (99.68% → 99.84%), fixed in branch test/fix-mislabeled-equivalents. See the handoff doc's \"fable-verify pass\" update for the full per-mutant trace, including the corrected reasoning for the survivors that are genuinely equivalent. Combined with the 3 sub-96% files closed in round 4, 6 of the 7 sub-100% files (all but MigrationParser.cs) are now confirmed closed with adversarially-checked reasoning — no further test-writing can move any of them. MigrationParser.cs moved 87.02% → 89.78% (scoped) via a full per-mutant pass over the ~91 survivors (12 new tests, 7 real gaps fixed, 91→70 survivors), then two more genuine gaps were found and fixed afterward (the line-689 DEFAULT-value and line-704 GENERATED-column Negate mutants, both non-equivalent despite resembling already-confirmed equivalence classes) — this is what closed the last 1-mutant gap to 96% overall. A Stryker coverage-misattribution issue was also found during this pass (see mutation-coverage-report.md) — the tool's own survivor counts for this file may include false positives beyond what's been individually verified here. The whole-project run that folded these fixes into the overall score completed 2026-09-20 20:12-20:16, landing at 96.01% — see the total tally row above. Extrode.JauntyQ.SqlParser: the 2026-09-20 figures above (baseline 59.22%, IR-model cleanup 59.65%, 4-file parser-core pass with no confirmed whole-project number) are superseded. A 2026-09-21 pass established a fresh, correct whole-project baseline (76.98%) then pushed all 10 survivor-carrying files through real-test + individually-traced equivalence-exclusion passes — see the per-file tally below, now drawn from a single coherent whole-project run (tests/Extrode.JauntyQ.SqlParser.Tests/StrykerOutput/2026-09-21.13-28-05), not a mixed-date composite. Extrode.JauntyQ.SqlParser — per-file tally vs. 96% target Whole-project run 2026-09-21 13:28-13:38, 88.53% overall (Killed 2216, Timeout 162, Survived 289, NoCoverage 19, 2686 total scored). File Killed+Timeout Survived NoCov Total Score vs. 96% SqlParser.Part2.cs 239 57 9 305 78.36% -17.64 pp SqlParser.cs 699 131 8 838 83.41% -12.59 pp SqlParser.Part4.cs 238 40 0 278 85.61% -10.39 pp SqlParser.Part5.cs 255 39 2 296 86.15% -9.85 pp SqlParser.Part7.cs 111 12 0 123 90.24% -5.76 pp SqlParser.Part6.cs 145 9 0 154 94.16% -1.84 pp SqlTokenizer.cs 509 1 0 510 99.80% +3.80 pp IR/CteRef.cs, IR/QueryModel.cs, Token.cs, IR/OrderByRef.cs, IR/TableRef.cs, IR/LiteralBinding.cs, IR/JoinRef.cs, IR/ParameterRef.cs, IR/PerfHint.cs, IR/ColumnRef.cs, SqlParser.Part3.cs 178 0 0 178 100.00% +4.00 pp Tally (18 files, single coherent whole-project run) 2374 289 19 2682 88.51%* -7.49 pp *The 88.51% tally-row figure differs from the JSON's own reported 88.53% by a fraction of a point due to 4 mutants (CompileError/other non-scored statuses) landing in different per-file buckets than the whole-project denominator — both numbers point at the same result; treat 88.53% as authoritative. 3 tiny IR files (IR/ParameterRef.cs, IR/ColumnRef.cs, IR/OrderByRef.cs) closed to 100.00% via new default-value tests. SqlParser.Part3.cs also reached 100.00% — 45 real tests plus ~15 equivalence-exclusion comments (collision-checked, covering ~103 individual mutants) resting on the tokenizer's trailing TokenType.End sentinel invariant. The other 6 parser-body files improved substantially (78–94%) but each still has a meaningful deferred set — dominated by one recurring pattern, the pos < tokens.Count && tokens[pos].Type == X guard-chain idiom, left plain/undocumented (not falsely claimed equivalent) because proving equivalence needs per-call-site tracing of each caller's own loop-exit behavior, judged out of budget for a single-file pass every time it came up. Flagged for a dedicated follow-up session to close the remaining ~7.5pp gap by tracing that pattern file-by-file — see the \"2026-09-21 push\" section in mutation-coverage-report.md for the full file-by-file real-test vs. equivalence-exclusion vs. deferred breakdown."},{"u":"06-reference-schema-snapshot-format.html","t":"Schema snapshot format (.schema.json)","s":"Reference","h":[{"i":"serialization","t":"Serialization"},{"i":"top-level-object","t":"Top-level object"},{"i":"table","t":"Table"},{"i":"column","t":"Column"},{"i":"index","t":"Index"},{"i":"foreign-key","t":"Foreign key"},{"i":"procedure","t":"Procedure"},{"i":"procedure-parameter","t":"Procedure parameter"},{"i":"sequence","t":"Sequence"},{"i":"minimal-example","t":"Minimal example"}],"b":"Schema snapshot format (.schema.json) The snapshot is the committed contract between your database and the generator. jauntyq schema pull writes it; the generator reads it at build time and never touches a database. This page documents the on-disk JSON structure. Serialization System.Text.Json, written indented (WriteIndented = true). Property names are camelCase; deserialization is case-insensitive. Optional fields are omitted when null (maxLength, precision, scale, isUnicode, and the sequence minValue/maxValue/currentValue). bool facets that default to false (isNullable, isPrimaryKey, isIdentity, isRowVersion) are always written. Enums are written as strings (procedure-parameter direction). There is no explicit format-version field; the shape is versioned by the tool/generator pair. Snapshots pulled with an older CLI still load, fields added later (index metadata, value-safety facets) simply read as absent until you re-pull. Top-level object { \"dialect\": \"sqlserver\", \"tables\": { \"<name>\": { /* TableSchema */ } }, \"foreignKeys\": [ { /* ForeignKeySchema */ } ], \"procedures\": { \"<name>\": { /* ProcedureSchema */ } }, \"sequences\": { \"<name>\": { /* SequenceSchema */ } } } Key Type Notes dialect string One of sqlserver, postgres, mysql, sqlite. Drives dialect-specific SQL synthesis. tables object Tables keyed by name. foreignKeys array Flat list of FK relationships. procedures object Stored procedures keyed by name. Empty for SQLite. sequences object Sequence objects keyed by name. Populated only for SQL Server and PostgreSQL; empty otherwise. Table { \"name\": \"Products\", \"columns\": { \"<name>\": { /* ColumnSchema */ } }, \"indexes\": [ { /* IndexSchema */ } ] } Key Type Notes name string Table name. columns object Columns keyed by name. indexes array Indexes, key columns in key order. Absent/empty for snapshots pulled before index capture; the JNT8004 unindexed-filter check only sees indexes when present. Column { \"name\": \"ProductName\", \"dbType\": \"varchar\", \"isNullable\": false, \"isPrimaryKey\": false, \"isIdentity\": false, \"maxLength\": 40, \"precision\": null, \"scale\": null, \"isUnicode\": false, \"isRowVersion\": false, \"isComputed\": false } Key Type Notes name string Column name. dbType string Database type name (e.g. int, varchar, decimal, uniqueidentifier). isNullable bool Column allows NULL. isPrimaryKey bool Part of the primary key. isIdentity bool Auto-increment / identity column. maxLength int? String/binary length; -1 = unbounded (varchar(max), text, bytea). Omitted when not applicable. precision int? Numeric total digits. Omitted when not applicable. scale int? Numeric fractional digits. Omitted when not applicable. isUnicode bool? true for Unicode text (nvarchar/nchar), false for single-byte, omitted for non-text. isRowVersion bool Database-maintained concurrency token (SQL Server rowversion/timestamp). Kept as an explicit flag because SQL Server reports rowversion under the type name timestamp. isComputed bool Computed/generated column (SQL Server AS ... [PERSISTED], PostgreSQL GENERATED ALWAYS AS (...) STORED, MySQL GENERATED ALWAYS AS (...)). The database rejects an INSERT/UPDATE targeting it, so auto-CRUD and BulkInsert exclude it from their column/value lists the same way they exclude isIdentity/isRowVersion columns. The maxLength, precision, scale, and isUnicode facets feed the value-safety layer (compile-time literal checks, client-side write guards, and stable-plan parameter sizing). Index { \"name\": \"IX_Products_CategoryId\", \"columns\": [\"CategoryId\"], \"isUnique\": false } Key Type Notes name string Index name. columns array of string Key columns in key order. Only the leading column makes a filter seekable. For an index with hasExpressionKeyPart, this is only the real-column subset of the key, in relative order, possibly empty. isUnique bool Whether the index enforces uniqueness. hasPrefixKeyPart bool Any key part is a MySQL/MariaDB column prefix (INFORMATION_SCHEMA.STATISTICS.SUB_PART), e.g. UNIQUE (email(5)). columns still lists the full column names, but uniqueness is enforced over the truncated prefix only, never treat the index as equivalent to (or redundant against) the full-column index of the same columns. Absent/false in snapshots that predate the capture (2026-07-30). hasExpressionKeyPart bool Any key part is an expression (MySQL functional, PostgreSQL/SQLite expression index). The model cannot carry the expression, so columns must never be read as the full key: no seekability, coverage, or uniqueness conclusion may be drawn from it. A flagged unique index still proves a competing unique constraint exists, the reason it is captured at all. Absent/false in snapshots that predate the capture, which omitted such indexes entirely. Tables defined in migration or DDL files do not carry secondary unique-index metadata (inline UNIQUE constraints and CREATE [UNIQUE] INDEX statements are not simulated). Indexes captured in a pulled snapshot survive migration simulation unchanged. Foreign key { \"fromTable\": \"Products\", \"fromColumn\": \"CategoryId\", \"toTable\": \"Categories\", \"toColumn\": \"CategoryId\" } Each entry drives one FK loader (db.Products.GetByCategoryId(...)). Procedure { \"name\": \"GetProductsByCategory\", \"params\": [ { \"name\": \"cat_id\", \"dbType\": \"int\", \"direction\": \"In\", \"isNullable\": false } ], \"results\": [ { \"name\": \"product_id\", \"dbType\": \"int\", \"isNullable\": false }, { \"name\": \"product_name\", \"dbType\": \"varchar\", \"isNullable\": false, \"maxLength\": 40 } ] } Key Type Notes name string Procedure name. params array Parameters in declaration order (see below). results array of Column Columns of the first result set, in ordinal order. Empty when the procedure returns no rows. Reuses the column shape. Procedure parameter Key Type Notes name string Parameter name. dbType string Database type name. direction string In, Out, InOut, or ReturnValue. ReturnValue is accepted but never produced by jauntyq schema pull, none of the three extractors reads a procedure's return status, so it reaches the generator only from a hand-authored or externally produced snapshot. It generates the same shape Out does: an out parameter on the sync overload, a tuple element on the async one. isNullable bool Whether NULL is allowed. maxLength int? String/binary length; omitted when not applicable. Procedures are consumed by the -- @call directive to emit typed CommandType.StoredProcedure calls. Sequence { \"name\": \"order_number\", \"startValue\": 100, \"increment\": 5, \"minValue\": null, \"maxValue\": null, \"currentValue\": null } Key Type Notes name string Sequence name. startValue long Start value. increment long Increment step. minValue long? Minimum, omitted when not enforced. maxValue long? Maximum, omitted when not enforced. currentValue long? Last value at snapshot time; omitted when the catalog does not expose it. Present only for SQL Server (sys.sequences) and PostgreSQL. Each entry generates a typed db.Sequences.Next{Name}() accessor, see the sequences guide. Minimal example { \"dialect\": \"sqlserver\", \"tables\": { \"Products\": { \"name\": \"Products\", \"columns\": { \"ProductId\": { \"name\": \"ProductId\", \"dbType\": \"int\", \"isNullable\": false, \"isPrimaryKey\": true, \"isIdentity\": true }, \"ProductName\": { \"name\": \"ProductName\", \"dbType\": \"nvarchar\", \"isNullable\": false, \"maxLength\": 40, \"isUnicode\": true } } } }, \"foreignKeys\": [] }"},{"u":"06-reference-supported-sql.html","t":"Supported SQL surface","s":"Reference","h":[{"i":"the-shape-of-the-rule","t":"The shape of the rule"},{"i":"one-statement-per-file","t":"One statement per file"},{"i":"supported","t":"Supported"},{"i":"expressions-need-an-alias","t":"Expressions need an alias"},{"i":"not-supported","t":"Not supported"},{"i":"a-measured-gap","t":"A measured gap"},{"i":"escaping-a-quote-inside-a-string-literal","t":"Escaping a quote inside a string literal"},{"i":"things-that-are-not-the-parser","t":"Things that are not the parser"},{"i":"see-also","t":"See also"}],"b":"Supported SQL surface What JauntyQ's parser accepts, what it refuses, and which diagnostic you get when you cross the line. This page exists because the alternative is probing. A consumer report on 2026-08-17 described writing five variants of one query to discover by trial which shapes the generator would take, three of them rejected with a message that named the construct but not the boundary, and one (LATERAL) rejected with a message that pointed at their schema for a table the parser had invented. Reading a list is cheaper than that. Every claim here is measured, not remembered. The source is tests/Extrode.JauntyQ.SqlParser.Tests/SupportedSurfaceProbeTests.cs, which parses each shape below and asserts the outcome, plus RegistryParityTests, which asserts that every JNTxxxx code named on this page resolves to a real descriptor. A change that widens or narrows the surface turns those red. The shape of the rule JauntyQ parses SQL to model the result shape, which columns come back, from which tables, with what nullability, so it can emit a typed mapper. It is not a SQL engine and does not need to be: the text you write is the text that runs, verbatim. So the boundary is not \"what is valid SQL\", it is \"what can be modeled without guessing\". A construct is refused when the model JauntyQ would build from it could disagree with what the database actually returns, because a wrong model produces code that compiles and then throws at runtime. That is why UNION is an error and GROUP BY is not. One statement per file JauntyQ generates one method per .sql file from one statement. A file holding two statements is JNT1008, an error: a second SELECT's tables and columns are merged into the first statement's model, and a second write statement is dropped entirely, so the generated code matches neither. Split them. Supported Shape Notes SELECT, INSERT, UPDATE, DELETE One per file. INSERT ... SELECT Parses as an INSERT. RETURNING On any write statement; makes it row-returning, and composes with -- @first. JOIN, INNER / LEFT / RIGHT / FULL / CROSS Outer joins widen the joined table's columns to nullable in the generated row type. ON and USING (...) join conditions USING is expanded to the equivalent same-named-column equalities. Comma-separated implicit joins (FROM a, b) Every table is registered, not just the first. WHERE with = != <> < > <= >= LIKE BETWEEN IN (...) IS [NOT] NULL AND OR [NOT] IN (SELECT ...) and [NOT] EXISTS (SELECT ...) in WHERE Lifted and validated as their own scope. Correlated EXISTS included. GROUP BY and HAVING Including over a join. Aggregates, COUNT SUM AVG MIN MAX, and dialect functions such as bool_or COUNT(*) FILTER (WHERE ...) parses. ORDER BY with ASC / DESC, by column, ordinal, or output alias LIMIT / OFFSET, T-SQL OFFSET n ROWS FETCH NEXT m ROWS ONLY, and TOP n (including TOP n PERCENT and TOP n WITH TIES) See the pagination note below. A column genuinely named percent is still projected: PERCENT is read as a modifier only when another projection item follows it, since the tokenizer strips quoting and cannot tell [percent] from the keyword. DISTINCT CASE WHEN ... THEN ... ELSE ... END Needs AS alias, see expressions below. Window functions (... OVER (PARTITION BY ... ORDER BY ...)) Parse as aliased expressions; the type is not inferred from the schema, so declare it with -- @result. CAST, COALESCE, NULLIF Non-recursive CTEs (WITH x AS (...) SELECT ...) Each CTE is an in-scope virtual table for the final statement. Schema-qualified names (dbo.Products, public.users) The qualifier is stripped for schema resolution and preserved verbatim in the emitted SQL. Parameters @name Repeated use of one name binds one parameter. Views Readable like tables in every dialect. Writes to them are JNT2021, see below. The index and foreign-key analyses (JNT8004, JNT8006, JNT8007) stay silent on a view: it declares neither, so those checks would be reporting the absence of metadata rather than a real finding. Materialized views PostgreSQL only. They live in pg_class/pg_attribute and are invisible to information_schema, so a dialect whose extractor reads only the standard views cannot report them. SQL Server indexed views are deliberately not captured. Their columns carry the same type facets (length, precision, scale, Unicode) as a base table's, until 2026-08-18 they carried none, which silently disabled the JNT5001 literal-length check on them. Expressions need an alias Any projection item that is not a plain column reference must carry AS alias. Without one there is no name to give the generated property, and inventing one from the expression text produces a name that changes whenever the expression is edited. Missing alias is JNT3004. select u.id, count(*) as order_count -- fine select u.id, count(*) -- JNT3004 Not supported Shape Diagnostic Severity What to write instead UNION / UNION ALL / INTERSECT / EXCEPT JNT1006 Error Separate queries merged in application code. Only the first branch's column shape would be modeled, so a second branch with different nullability generates code that reads NULL as non-nullable. Scalar subquery in the projection list JNT1007 Error A JOIN with GROUP BY, or a CTE. Derived table in FROM (from (select ...) x) JNT1007 Error A CTE. LATERAL joins, in FROM or in a join JNT1009 Error A plain JOIN, a WHERE-clause IN/EXISTS, or a GROUP BY with aggregates. CROSS APPLY / OUTER APPLY (SQL Server) JNT1009 Error The same rewrites as LATERAL, this is T-SQL's spelling of it. DISTINCT ON (...) (PostgreSQL) JNT1009 Error row_number() over (partition by <the DISTINCT ON columns> order by <your ORDER BY>) in a CTE, filtered to 1 outside it; or two queries. Plain DISTINCT is unaffected and still supported. SELECT ... INTO <table> (T-SQL) JNT1009 Error Move the table creation into a migration and keep the SELECT here as a plain query. It creates a table rather than returning rows, so there is no result shape to map. PL/pgSQL's SELECT ... INTO <variable> belongs in a stored procedure. INSERT INTO is unaffected and still supported. Every target spelling reaches the same refusal on its own: T-SQL #temp and ##global, and PostgreSQL's INTO TEMP / TEMPORARY / UNLOGGED [TABLE] name. WITH RECURSIVE JNT1001 Warning Iterate in application code. More than one statement in a file JNT1008 Error One statement per file. JNT1009 is new in v0.3.0 and is why this page exists. LATERAL is not a tokenizer keyword, so it used to arrive in the table-name position as an ordinary identifier and the parser recorded a relation literally named lateral. What the consumer then saw was JNT2001: Table 'lateral' does not exist in schema, true of a table the parser had invented, and advice pointing at a schema that was perfectly correct. Missing grammar and a missing relation are different failures needing opposite responses, so they no longer share a code: JNT2001 now means only \"this relation is not in your snapshot\", and JNT1009 means \"JauntyQ has no grammar for this\". The guarantee is about the construct, not about where you wrote it. The first cut of this fix guarded only the join position, so FROM LATERAL (...), the comma form FROM a, LATERAL (...), and CROSS APPLY all still invented a relation, the fix held for the one shape that had been reported and nowhere else. Every relation position is covered now, and UnmodeledRelationTests has a case per position rather than trusting one to stand for the rest. A measured gap UPDATE ... FROM other_table parses without complaint but models only the target table, so the second relation is invisible to validation. It is neither supported nor refused, which is the worst of the three; it is recorded in the todo list and pinned by SupportedSurfaceProbeTests.UpdateFrom_ParsesButDoesNotModelTheSecondRelation. DISTINCT ON (PostgreSQL) was in the same state and is no longer: since 2026-08-18 it is refused as JNT1009 and appears in the table above. Why it was worth fixing rather than documenting: the ON and its parenthesized list glued onto the first projected column as one opaque expression, so the query modeled one fewer real column than it returns. Unaliased that reached JNT3004 (\"this expression needs an alias\"), loud, but blaming a column the consumer did not write. Aliased, it passed validation outright and the generated mapper typed its first property from expression-shape inference over the text ON ( ... ) <column>. Escaping a quote inside a string literal Double it. 'it''s' works on every dialect JauntyQ supports. MySQL's backslash form, 'it\\'s', does not: the tokenizer implements only the ANSI '' escape, so the backslash leaves the quotes unbalanced and the file is refused with JNT1002. That refusal is deliberate and the message names the backslash when it finds one, but it is a refusal, not a silent mis-parse, and there is no build in which a backslash-escaped file produces wrong SQL. Pinned by TokenizerTests's three backslash cases, which also pin why the fix is not simply \"honor the backslash\": 'C:\\' is a complete literal under ANSI rules and tokenizes as one today. Things that are not the parser Three refusals get mistaken for grammar gaps. A missing relation is JNT2001, and it means what it says: the table or view is not in your schema snapshot. Re-run jauntyq schema pull. Since v0.3.0 this code is never raised for a construct the parser failed to understand. An unindexed filter is JNT8004, a performance warning, not a rejection. The query is fine; the column you filter on has no index. Add the index, or accept the scan for this one query with -- @allow-unindexed <reason>. If your project sets TreatWarningsAsErrors, every JauntyQ warning arrives as a build error, that escalation is your setting, not JauntyQ's severity. Unstable pagination is JNT8009 / JNT8010, also warnings. A LIMIT/OFFSET page whose ORDER BY does not resolve to a unique key can return the same row on two pages and skip another, because the engine is free to break the tie differently per query. Add the primary key as the last ORDER BY term. See also Diagnostics, every JNTxxxx code. Directives, -- @result, -- @params, -- @allow-unindexed. Dialect differences, what the emitted SQL looks like per engine. Troubleshooting, symptoms mapped to causes."},{"u":"06-reference-troubleshooting.html","t":"Troubleshooting & FAQ","s":"Reference","h":[{"i":"nothing-is-generated","t":"Nothing is generated"},{"i":"first-find-out-whether-the-generator-ran-at-all","t":"First, find out whether the generator ran at all"},{"i":"jauntyqautocrud-or-jauntyqdialect-seems-ignored","t":"<JauntyQAutoCrud> or <JauntyQDialect> seems ignored"},{"i":"build-fails-after-a-schema-or-migration-change","t":"Build fails after a schema or migration change"},{"i":"jnt9002-table-already-exists-on-a-migration","t":"JNT9002: table already exists on a migration"},{"i":"jnt9003-in-a-ddl-project","t":"JNT9003 in a DDL project"},{"i":"jnt4003-parameter-type-could-not-be-inferred","t":"JNT4003: parameter type could not be inferred"},{"i":"jnt5001--jnt5002-on-a-literal","t":"JNT5001 / JNT5002 on a literal"},{"i":"a-query-runs-but-reads-the-wrong-column-or-the-shape-guard-throws","t":"A query runs but reads the wrong column, or the shape guard throws"},{"i":"jauntyq-schema-pull-fails-or-leaks-nothing-useful","t":"jauntyq schema pull fails or leaks nothing useful"},{"i":"mysql-bulkinsert-throws-at-runtime","t":"MySQL BulkInsert throws at runtime"},{"i":"native-aot-publish-warnings","t":"Native AOT publish warnings"},{"i":"suppressing-a-performance-warning-jnt8xxx","t":"Suppressing a performance warning (JNT8xxx)"},{"i":"faq","t":"FAQ"}],"b":"Troubleshooting & FAQ Common problems, why they happen, and the fix. For the full list of build diagnostics see diagnostics; this page maps symptoms to causes. Nothing is generated No db.* accessors exist / JauntyDb is missing. The generator only runs when it can see your files and schema through MSBuild. Confirm the AdditionalFiles globs are present and match your layout: db\\**\\*.sql and db\\schema\\*.schema.json. See configuration. Confirm the generator is referenced as an analyzer (OutputItemType=\"Analyzer\" for a project reference; the package wires this automatically). Confirm a schema source exists, a committed .schema.json, or db/ddl/*.sql plus <JauntyQDialect>. Without either you get JNT6001. Do a clean rebuild. Roslyn caches generator output; an incremental build can hide a fixed configuration issue. Inspect what was emitted. Set <EmitCompilerGeneratedFiles>true<\/EmitCompilerGeneratedFiles> and look under obj/<config>/<tfm>/generated/Extrode.JauntyQ.Generator/ to see the actual .g.cs files. First, find out whether the generator ran at all This is worth doing before anything above, because the two failures behind \"nothing is generated\" have opposite fixes and a screen of CS0246s looks identical either way. One file tells them apart. JauntyQShapeGuard.g.cs is a post-initialization output: Roslyn emits it before any of JauntyQ's pipeline runs, so it exists if and only if the generator loaded and started. It is also the only generated file that does not depend on your SQL or schema. What you see What it means Where to look next No JauntyQShapeGuard.g.cs at all The generator never loaded or never started. Not a schema, SQL or configuration problem, nothing of yours has been read yet. The analyzer reference itself. Roslyn reports a generator it cannot load as CS8784/CS8034, both warnings, so check the build log for those; the usual cause is a version or dependency mismatch between the generator and the compiler. JauntyQShapeGuard.g.cs present, nothing else The generator ran and emitted nothing. Your files or schema did not reach it, or every file was refused. The AdditionalFiles globs and the JNT diagnostics in the build log, per the list above. JNT0001 in the build output The generator threw. This is a bug in JauntyQ, not in your project. Nothing on your side. The message names the stage and the exception, please report it. Generated output may be partly or entirely missing, and the CS0246s are consequences of it. Without EmitCompilerGeneratedFiles, the same question is answerable from code: typeof(Extrode.JauntyQ.Generated.JauntyQShapeGuard) compiles if and only if the generator ran. Note that a generator which throws is also reported by Roslyn as a warning (CS8785) rather than an error, which is why the build can go on and leave you with nothing but CS0246s. JNT0001 exists to end that silence, but it can only fire for a throw JauntyQ catches, if the generator failed to load, it never got far enough to report anything, and the shape guard's absence is the tell. <JauntyQAutoCrud> or <JauntyQDialect> seems ignored The property must reach the analyzer as a CompilerVisibleProperty. The package ships this registration; if you consume via a project reference in a repository without Directory.Build.props, add: <ItemGroup> <CompilerVisibleProperty Include=\"JauntyQAutoCrud\" /> <CompilerVisibleProperty Include=\"JauntyQDialect\" /> <\/ItemGroup> Build fails after a schema or migration change The build validates every query against the effective (post-migration) schema, this is intended. A JNT2001/JNT2002 after a migration means a query still references a table/column the migration removed or renamed; fix the query or the migration together. See diagnostics for each code. JNT9002: table already exists on a migration db/migrations/ is for pending migrations only. Once a migration is deployed and you re-pull the snapshot, the snapshot already embodies it, so re-applying its CREATE fails. Archive the migration file out of db/migrations/. JNT9003 in a DDL project db/ddl/*.sql files are present with no snapshot, but JauntyQDialect is unset or not one of sqlserver/postgres/mysql/sqlite. Add it: <PropertyGroup><JauntyQDialect>sqlite<\/JauntyQDialect><\/PropertyGroup> JNT4003: parameter type could not be inferred The parser could not derive a parameter's type from its use (common with subqueries and expressions). Declare it with -- @params; the error message includes the exact line to add. JNT5001 / JNT5002 on a literal A string or numeric literal in your SQL cannot fit the target column (length/precision/range). This is a compile-time value-safety check, the value would truncate or overflow at runtime. Shorten the literal, or widen the column (and re-pull/re-migrate the schema). A query runs but reads the wrong column, or the shape guard throws The database has drifted from the snapshot (a column renamed or reordered in production without a re-pull). Run jauntyq schema verify to see the drift, then jauntyq schema pull to refresh the snapshot and rebuild. See the CLI reference. jauntyq schema pull fails or leaks nothing useful Errors are anonymized by default so connection-string secrets never reach logs. Set JAUNTYQ_VERBOSE=1 to see the full exception while debugging locally. MySQL BulkInsert throws at runtime MySqlBulkCopy needs LOCAL INFILE on both sides: AllowLoadLocalInfile=true in the connection string, and local_infile enabled on the server. See the bulk insert guide. Native AOT publish warnings JauntyQ's generated code and Extrode.JauntyQ.Runtime are trim/AOT-safe. Remaining warnings almost always come from the database provider; use an AOT-friendly provider stack (e.g. Microsoft.Data.Sqlite.Core + SQLitePCLRaw.bundle_e_sqlite3). Suppressing a performance warning (JNT8xxx) The JNT8xxx codes are advisory (the query still compiles). If a scan is intentional, suppress per-project in .editorconfig: dotnet_diagnostic.JNT8004.severity = none FAQ Does JauntyQ connect to my database at build time? No. Only the jauntyq CLI connects. The generator reads the committed snapshot. Is it a DbContext / ORM with change tracking? No. JauntyDb is a stateless router: no tracking, no caching, no unit of work. new one per request. Can I still write raw SQL? That is the whole model, every query is a .sql file. Auto-CRUD is a convenience layered on top and any synthetic is overridden by a same-named file. Does it support MariaDB? Yes, via the mysql dialect. See dialect differences. Where do I see exact generated signatures? Build and inspect the emitted .g.cs files, or read the API overview."},{"u":"06-reference-versioning-and-support.html","t":"Versioning & support policy","s":"Reference","h":[{"i":"version-scheme","t":"Version scheme"},{"i":"snapshot-format-compatibility","t":"Snapshot-format compatibility"},{"i":"framework-and-platform-support","t":"Framework and platform support"},{"i":"releases-and-changelog","t":"Releases and changelog"},{"i":"support-channels","t":"Support channels"},{"i":"reporting-issues","t":"Reporting issues"}],"b":"Versioning & support policy How JauntyQ is versioned, what compatibility you can rely on across upgrades, and how support is provided. Commercial terms (tiers, lapse) are in pricing; this page covers the technical policy. Version scheme JauntyQ follows Semantic Versioning (MAJOR.MINOR.PATCH): MAJOR, breaking changes to the generated API surface, the consumer MSBuild contract, or the snapshot format. MINOR, new features (new directives, new dialect capabilities, additional generated members) that do not break existing code. PATCH, bug fixes and diagnostics improvements with no API change. JauntyQ is currently pre-1.0 (0.x). While pre-1.0, minor versions may include breaking changes; each is called out in the CHANGELOG. 1.0 is a licensing boundary as well as a compatibility one. Free preview licenses, which make the paid commands free during the 0.x preview, are scoped to pre-1.0 releases: every 0.x build honors one permanently, and 1.0 declines it (see licensing and activation). The check reads the version compiled into the build via LicenseProduct.Version, which is generated from MSBuild's $(Version), so the 0.x → 1.0 bump is the only thing that has to happen for it to take effect, and there is no second constant to remember to change. The Extrode.JauntyQ.Generator and Extrode.JauntyQ.Runtime packages are versioned and released together; use matching versions. Snapshot-format compatibility The .schema.json snapshot is designed to be forward-tolerant: A generator reads snapshots produced by older CLI versions, fields added later (index metadata, value-safety facets) simply read as absent, and the features that depend on them stay off until you re-pull. Re-running jauntyq schema pull with a newer CLI upgrades the snapshot in place; the format has no explicit version field, and no manual migration step is required. A breaking change to the snapshot shape would be a MAJOR version and would be documented with an upgrade note. Practical guidance: keep the CLI and the generator/runtime packages on the same release, and re-pull the snapshot after upgrading to opt into new analysis. Framework and platform support .NET SDK 8.0 or later is required to build (the generator runs inside Roslyn). Generated code targets whatever framework your project targets. The generator is built against Roslyn 4.12, so the consuming toolchain must provide at least that compiler: .NET SDK 8.0.400+/9.0.100+, or Visual Studio 17.12+ on Windows. Older toolchains skip the analyzer (or warn) rather than run it. The generator package intentionally declares no NuGet dependencies and relies on the compiler host for System.Text.Json (pinned to the 8.0.5 version the supported Roslyn/VS toolchains ship). SDK-based dotnet build provides it in-box; on Visual Studio it comes from the VS install, another reason the 17.12+ floor matters. Generated code and Extrode.JauntyQ.Runtime are Native AOT and trim compatible; AOT support ultimately also depends on your database provider. Supported dialects: SQL Server / Azure SQL, PostgreSQL, MySQL / MariaDB, and SQLite. See dialect differences. Support for a new major .NET release is added after it ships; the minimum supported SDK is raised only on a MAJOR version. Releases and changelog Releases are published to NuGet.org (the core packages and the free tool) and to a private feed for subscribers (the paid packages), tagged v*. Every release is recorded in the CHANGELOG, grouped Added / Changed / Fixed, with any breaking changes and upgrade notes at the top of the entry. Support channels Support level follows your license tier (pricing): Community (free), GitHub issues, no SLA. Team / Business, email support bundled, 2-business-day response target. Enterprise, priority support, a custom SLA, and a named contact. On lapse you keep the versions you have (perpetual-use fallback) but lose updates, feed access, and support. Reporting issues Include the JauntyQ package version, the dialect, the relevant .sql file and snapshot excerpt, and any JNTxxxx code from the build output. For CLI problems, re-run with JAUNTYQ_VERBOSE=1 and include the detailed error (it omits connection-string secrets). Contact routes are on https://extrode.com."},{"u":"07-roadmap-roadmap.html","t":"JauntyQ Roadmap","s":"Roadmap","h":[{"i":"in-progress--next","t":"In progress / next"},{"i":"planned-spec-first","t":"Planned (spec-first)"},{"i":"shipped","t":"Shipped"},{"i":"deferred-future-not-now","t":"Deferred (future, not now)"},{"i":"non-goals-owned-elsewhere-or-out-of-scope","t":"Non-goals (owned elsewhere or out of scope)"}],"b":"JauntyQ Roadmap Living list of post-baseline work. Items are grouped by whether they are being built now, planned next, or deferred. Non-goals are recorded so scope stays honest. In progress / next (nothing currently in progress) Planned (spec-first) The differentiator (\"moat\") specs authored 2026-07-10 (007–012) have all been built and merged to dev (2026-07-11), see Shipped below. Nothing else is currently queued. Under evaluation (built, ship/hold decision pending): License enforcement. Signed offline jaunty.license.json, jauntyq activate and entitlement gating are built and live in Extrode.JauntyQ.Cli.Premium: schema verify, migrate impact, explain, registry and usage export consult the gate. The core codegen is never gated, and a lapse only warns, never hard-fails. What remains is commercial rather than technical. See docs/03-guides/licensing-and-activation.md. Dropped (for SQL Server): Bespoke Visual Studio extension for SQL Server. Visual Studio ships native SQL Server support (SQL Server Object Explorer, T-SQL IntelliSense, live database validation) out of the box. Building a JauntyQ-specific extension would duplicate that for the one dialect VS already covers well. Not worth building. Under consideration (future, not now — no commitment): Bespoke Visual Studio extension for PostgreSQL, MySQL/MariaDB, and SQLite. Unlike SQL Server, Visual Studio has no built-in editing or live validation for these three dialects; support today comes only from third-party extensions of uneven quality, maintenance, and pricing. A JauntyQ-authored extension giving these dialects the same .sql editing + live-validation experience SQL Server users get natively is a real gap and may be worth building later. No commitment, no timeline. Scoping (2026-09-19, training-knowledge based, not a live web search — verify before spec'ing): Reusable infra: Extrode.JauntyQ.SqlParser + Extrode.JauntyQ.Analysis are already offline, Roslyn-free, dialect-aware SQL tokenizer/parser/ validator libraries used today for build-time diagnostics. They lower the floor for autocomplete/inline-validation logic; the missing pieces are editor integration and live schema introspection, not SQL understanding. Landscape (VS-native, not VS Code — different extensibility model): no maintained free/OSS VS extension reaches SQL Server-level parity for any of the three dialects. Postgres and MySQL/MariaDB have only aging or commercial options (e.g. Oracle's \"MySQL for Visual Studio\", dbForge); SQLite has more actively maintained community tooling (e.g. SQLite/SQL Server Compact Toolbox) but scoped to data browsing, not schema-aware IntelliSense. Full IDEs (DBeaver, DataGrip, Azure Data Studio) cover all three well but are separate products, not VS extensions. Architecture: VS's newer LSP client support (Microsoft.VisualStudio.LanguageServer.Client) is the leverage point — one shared LSP host + a pluggable per-dialect backend (reusing SqlParser's existing dialect modes), rather than three independent heavyweight VSPackage/MEF extensions. Live schema introspection still needs each dialect's own ADO provider (Npgsql, MySqlConnector, Microsoft.Data.Sqlite) wired in separately — that part doesn't share. Effort: one dialect to IntelliSense + inline-validation parity (not full Server Explorer/execution-plan parity) is roughly 2-4 months for one experienced developer. All three, built LSP-first with a shared host, is closer to 1.6-2x a single dialect's effort rather than 3x — still a multi-quarter effort, not a side project. Full parity with SQL Server's native depth (Server Explorer-grade connection browsing, execution plan visualization, project-wide rename refactoring) is a dedicated-team, multi-quarter undertaking on top of that. Hardest parity gaps: (1) live graphical execution-plan visualization — no VS-native equivalent to render Postgres/MySQL EXPLAIN output against, though the existing jauntyq explain CLI's cached EXPLAIN output could seed this; (2) Server Explorer-grade live connection/object-tree browsing; (3) rename-across-references refactoring, which needs project-wide reference tracking beyond editor-level IntelliSense. Recommendation: not yet — needs more research. The reusable parser lowers the floor but this still competes with existing commercial tooling and unvalidated demand (how many JauntyQ prospects use non-SQL-Server dialects in Visual Studio specifically, vs. VS Code or Rider). Before spec'ing: a live web search to confirm/refresh the landscape survey above, and a demand check with prospects/customers. Shipped Migration Impact Analysis (003). Reuses the migration simulator + query validator to report which queries a pending migration set affects, classified SAFE / RISKY / BREAKING, at build time (JNT9004 RISKY warning) and via the offline jauntyq migrate impact CLI verb. The shared Roslyn-free engine now lives in Extrode.JauntyQ.Analysis.. Deeper query analysis (004). Added JNT8006 (join-not-a-foreign-key) and JNT8007 (ORDER BY without a supporting index) to the offline JNT8xxx perf pass, plus ORDER BY capture in the SqlParser IR. The N+1 heuristic from the original scope was split out as 008, which has since shipped too (see below).. Database contract testing (005). The structural CI-gate core shipped: the Extrode.JauntyQ.Schema.Contract NuGet (test-embeddable assertion API + a fixed breaking/compatible severity model) and an upgraded jauntyq schema verify (--format, --fail-on); the extractors were relocated out of the CLI so the core stays zero-dependency. The per-service contracts and central registry parts of the original vision were split into Planned specs 010 + 009 (and runtime startup verification + usage-aware severity into 011 + 012).. N+1 query heuristic (008). Build-time, offline JNT8008 (Warning) over the query corpus + FK graph: pairs a child point-lookup by foreign key with a parent-collection query and suggests a join or batched IN, with guards (already-IN, joined child, single-row-PK parent, no FK, project-level suppression). The perf check split out of 004.. Opt-in live EXPLAIN plan analyzer (007). jauntyq explain CLI verb only, so the Roslyn generator stays offline; lives in the non-core Extrode.JauntyQ.Explain library. Per-dialect estimate-only EXPLAIN (Postgres/SQLite/MySQL/SQL Server), capped, cached at .jaunty/explain-cache.json, report-only (exit 0; opt-in --fail-on), a missing/unreachable DB skips cleanly. Entitlement-gated.. Central schema-dependency registry (009). jaunty.registry.json naming schema snapshots + the services that depend on them, with registry resolve|dependents|validate|list, in the non-core Extrode.JauntyQ.Registry library. Additive/opt-in; the substrate for per-service contracts.. Per-service contracts (010). Registry-driven scope filter for schema verify (--service/--registry) that narrows a contract to the slice a service actually depends on (narrows, never widens), so another service's change does not fail this service's gate. Builds on 005 + 009.. Runtime startup verification (011). Opt-in boot-time StartupSchemaGuard (fail-fast / warn / skip per policy) in the non-core Extrode.JauntyQ.Schema.Contract package, with optional <JauntyQEmbedSnapshot> build wiring; Extrode.JauntyQ.Runtime stays zero-dependency.. Usage-aware severity (012). Opt-in, downgrade-only UsageAwareReclassifier jauntyq usage export (usage manifest from ReferencedObjects); schema verify --usage downgrades breaking drift on objects no generated query references and never hides a real break. Composes with 010 scope.. DDL-as-schema-source (sqlc-style). A project with no pulled JSON snapshot can define its base schema purely from CREATE TABLE / ALTER TABLE files under the db/ddl/*.sql convention, reusing the existing migration parser + schema simulator to build the schema from an empty database. Framed as an alternative input, not a replacement: when a .schema.json snapshot is present it stays authoritative and any db/ddl/*.sql files are silently ignored (no merge, no error). Because there is no snapshot to infer the dialect from, DDL mode requires the consumer to declare it in the .csproj via <JauntyQDialect>sqlserver<\/JauntyQDialect> (or postgres, mysql, sqlite); a missing/unrecognized value is a build error (JNT9003). Existing db/migrations/*.sql files still apply on top of the ddl-built base exactly as they apply on top of a snapshot. Known limitation: the migration parser does not model index DDL, so tables defined in DDL/migration files expose PK/identity/column-type info but no secondary unique indexes, the JNT8004 composite-index check and Upsert alternate-key resolution only ever see the PK for such tables. (Indexes, procedures, and sequences captured in a pulled snapshot survive migration simulation unchanged.) Provider-native bulk fast paths. BulkInsert(IEnumerable<Row>) / BulkInsertAsync (and their static overloads) now specialize the emission per dialect: Npgsql binary COPY (Postgres), SqlBulkCopy (SQL Server), and MySqlBulkCopy (MySQL). SQL Server and MySQL feed their WriteToServer through a single reflection-free, AOT-safe DbDataReader-over-IEnumerable<Row> adapter emitted once per table (ordinal-based switch, no reflection). sqlite and any unrecognized dialect keep the portable single-transaction prepared-command loop unchanged. Note: the MySqlBulkCopy path requires the consumer's connection string to set AllowLoadLocalInfile=true (and the server to allow LOCAL INFILE); JauntyQ cannot set this and documents it on the generated method. Sequences (SQL Server + PostgreSQL + MariaDB). jauntyq schema pull captures sequence objects and the generator emits typed db.Sequences.Next{Name}() / Next{Name}Async() accessors. Real/Oracle MySQL and SQLite have no true sequence object, so this is dialect-specific; MariaDB (which shares the mysql dialect string with real MySQL) is detected separately and does get accessors, using SELECT NEXTVAL(<seq>). (Broader schema-object generation, functions, enums, UDTs, remains deferred; see below.) API reference / JNT diagnostics reference docs. docs/06-reference/diagnostics.md (every JNTxxxx code, severity, meaning) and docs/06-reference/api-overview.md (shape of the generated surface, pointing to XML doc comments in src/Extrode.JauntyQ.Generator/CodeEmitter*.cs as the authoritative reference), alongside the CLI, configuration, snapshot-format, directives, and dialect references under docs/06-reference/. Full XML-doc-driven site generation (DocFX or similar) remains deferred; see below. SBOM generation. .github/workflows/release.yml generates a CycloneDX SBOM (dotnet CycloneDX) for every tagged release and attaches it alongside the packages, no signing cert needed for this part. Parser hardening (report M3). Unterminated-token diagnostics (JNT1002) and input-size caps (JNT1003, SqlTokenizer.MaxInputLength). Deferred (future, not now) Broader schema-object generation. Functions, enums, and UDTs (jOOQ-level completeness). Sequences already shipped (see above); this covers the remaining object types. Lower priority. Package signing. The core packages ship from NuGet.org over HTTPS with repository signing by the feed. Authenticode or NuGet author signing is a value-add warranted when an enterprise customer's policy demands it or when a native-AOT CLI binary ships; it would go through a cloud signer, never a stored .pfx. XML-doc-driven API reference site (DocFX or similar). A generated site built from the generator's XML doc comments; the hand-written docs/06-reference/ pages cover the reference need for now. Scoped (2026-09-19): JauntyQ's docs already run on a shared engine — the sibling repo ../docs (github.com/extrode/docs, née Beparey.DocsGen), which scripts/build-docs.sh invokes directly (../docs/src/Docs). That engine is a pure markdown-in/HTML-out static-site pipeline (Markdig + System.CommandLine only — no Roslyn, no reflection): it walks a numbered markdown tree and renders theming, sidebar nav, per-page TOC, search index, and Mermaid-to-SVG at build time. It has no XML-doc-comment or reflection-based API reference capability today — confirmed by grepping its entire source tree for XmlDoc/DocFX/Reflection/Roslyn with zero hits. So adopting a site generator is already done; generating reference pages from XML doc comments is not, and is a genuinely new component, not a config flag on the existing engine. Smallest-lift design: a converter that reads the generator's .xml doc-comment file + reflects over its public members (resolving <see cref>/<paramref> etc.) and emits pages into the same numbered-markdown-tree shape ../docs already consumes — reuses theming, nav, search, and versioning for free, and confines the new work to member enumeration + doc-comment parsing + a markdown template per member kind. Roughly comparable in scope to a small DocFX subset. Still deferred; no timeline. Non-goals (owned elsewhere or out of scope) Dynamic / runtime-composed SQL, this is Jaunty's job, not JauntyQ's. JauntyQ keeps the \"all SQL known and validated at build time\" contract. LINQ query composition, change tracking, lazy loading, entity graphs, EF Core's domain; deliberately rejected (\"SQL as source of truth\")."}];
